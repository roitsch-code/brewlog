# Bump this file (any change) on main to fire .github/workflows/chat-agent.yml.
# Route-around for Claude Code, whose token can't workflow_dispatch.
#
# Run 1 (2026-08-22) — first live measurement after the #544 guards. Result:
# home-standard 4/5 first-try, big-batch 3/5, narrowed-to-orea 2/5,
# travel-no-gooseneck 1/5. Every miss cost a repair round; 2/20 were still
# broken after it, all `dead-gap`.
#
# Run 2 (2026-08-23) — measures the pour-count-floors rewrite of the
# percolation rule, and is the first run whose report names WHAT the model
# wrote on its failing first try (shape + validator message), not just the
# code. Compare the first-try column against Run 1's numbers above.
