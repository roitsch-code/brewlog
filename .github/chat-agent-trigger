# Bump this file (any change) on main to fire .github/workflows/chat-agent.yml.
# Route-around for Claude Code, whose token can't workflow_dispatch.
#
# Run 1 (2026-08-22) — first live measurement after the #544 guards:
# home 4/5 first-try, big-batch 3/5, narrowed-to-orea 2/5, travel 1/5.
# 2/20 still broken after the repair round, all dead-gap.
#
# Run 3 (2026-08-23, #559) — pour-count floors: travel 1/5 -> 3/5,
# narrowed-to-orea 2/5 -> 4/5, home 4/5, big-batch 3/5. Nothing broken
# reached the timer. All six remaining first-try failures dead-gap.
#
# Run 4 (2026-08-23, this bump) — the floors were derived on the BARE
# brewer; with the Drip Assist the drawdown reserve is 7% not 33%, so the
# pour phase is ~40% longer and the same count leaves a hole. The table
# now has a disc column (+1 pour). Expect the three disc scenarios to move;
# big-batch is bare and was simply below the floor.
