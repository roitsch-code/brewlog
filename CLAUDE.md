# BrewLog — Claude Code Context

Personal coffee brew advisor & diary PWA. Next.js 14 App Router + Postgres + Claude AI + Hetzner.

---

## Operating mode — own the mechanics, keep asking (OVERRIDES the friction rules below)

The user runs this project from a phone, usually with no terminal. The thing to eliminate is **manual mechanical work on the user's side** — never the conversation. This section takes precedence over any "validate before shipping" instruction elsewhere in this file, but it does NOT tell you to stop asking questions.

- **Asking is a MUST, not friction.** The user wants to steer: clarify ambiguity, confirm direction, surface trade-offs, present real options for decisions (especially anything subjective, visual, product-shaping, or with no clear default). Erring toward asking on *what to build / which way to go* is correct and wanted. What the user is sick of is being *blocked* — not being *consulted*.
- **Never offload mechanics onto the user.** Do NOT hand them commands to type, do NOT expect them to `git pull` / push / merge / run a script / SSH / type anything in a terminal. YOU run git, YOU open and merge the PR, deploy is automatic, SQL migrations go through the GitHub Actions "Run SQL Migration" workflow. The only legitimate "you do it" is a setting that genuinely lives behind a web UI you can't reach (e.g. GitHub repo-settings toggles) — and even then, name the exact clicks and offer to walk them through it.
- **Ship the execution end-to-end.** Once a direction is agreed: make the change → `tsc` → commit → push → open the PR → enable auto-merge (or merge on green) → confirm `main` advanced. "Done" = merged + deploying, never "pushed to a branch" and never "here's the command, you run it".
- **Pause for decisions and for the genuinely irreversible.** Ask the user on real forks. Hard-stop only for wiping/overwriting production data, deleting things you didn't create, force-pushing `main`, rotating secrets, or spending money. Ordinary code / asset / config / prompt changes are revertible via git — execute them, then report.
- **A wall means route around it, not dump it on the user.** A tool is blocked? Find another path before surfacing it. When you do surface a wall, say what you already tried — don't make the user do the legwork.
- **Self-correct in place.** Made a mistake? Fix it and move on. No spiralling, no wall of hedging.

**What this does NOT relax:** the "never fabricate coffee parameters / facts" rule stays fully in force — but it means *look it up or mark it unverified and keep going*, never *halt the task*. Honesty about data (don't invent row counts, don't claim verified when you didn't check) is about not lying to the user.

---

## Infrastructure

| What | Detail |
|------|--------|
| **VPS** | Hetzner Cloud, host in the `DEPLOY_HOST` GitHub Actions secret, path `/opt/brewlog` |
| **Stack** | Docker Compose: `postgres`, `app` (Next.js), `caddy` (reverse proxy), `ofelia` (cron) |
| **Vercel** | **Deleted.** App is 100% on Hetzner. No Vercel, no Vercel env vars, nothing. |
| **Auto-deploy** | `.github/workflows/deploy.yml` — pushes to `main` trigger SSH deploy on VPS |
| **Auto-deploy secrets** | `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `DEPLOY_PATH` in GitHub repo secrets. `deploy.yml` additionally always-syncs `MISTRAL_API_KEY`, `MISTRAL_COACH_API_KEY` and `HEALTHSYNC_INGEST_SECRET` from repo secrets into the VPS `.env` on every deploy (strip + re-append, guarded so it can never clobber the other vars), and restarts ofelia so cron-config changes take effect |

**Manual deploy (fallback):** SSH into VPS → `cd /opt/brewlog && git pull origin main && docker compose build app && docker compose up -d app`

**Running a new SQL migration — the standard path is the marker file:** write the migration's filename into **`.github/migrate`** and merge — the `migrate-on-push.yml` workflow (push-trigger on that file) SSHes into the VPS and pipes it into psql. No button, no terminal; this exists because the integration token cannot `workflow_dispatch` (403). Remember Drizzle is column-strict: merge the migration marker BEFORE (or with) the code that reads the new column, and the marker keeps its last value afterwards — so migrations must be safely re-runnable (`IF NOT EXISTS` / zero-row-guard patterns). Alternative when a human is at the GitHub UI: the **"Run SQL Migration" workflow** (`.github/workflows/migration.yml`, manual `workflow_dispatch`; inputs `migration_file` + `ref`). Expect `INSERT 0 N` / `CREATE TABLE` in the run log.

Manual fallback (only if Actions is down — never hand this to the owner):
```bash
cd /opt/brewlog && cat src/lib/db/migrations/<file>.sql | docker compose exec -T postgres psql -U brewlog -d brewlog
```

**Type-check before every commit:** `npx tsc --noEmit`

**CI on every PR (`.github/workflows/ci.yml`):**
- **`check` job** — FOUR gates: `tsc --noEmit`, `node --test` over `src/` (pour math), `node tests/recipes/validate.mjs` (recipe-corpus validation — two-way technique cross-check, staged-temp ban; recipe→technique refs are a HARD error since 2026-08-18), and `node --test` over `tests/` (the dataflow suites). Fast quality gate.
- **`chat-e2e` job** (#545) — end-to-end proof the chat's recipe guards RUN: boots the real app + throwaway Postgres, points `ANTHROPIC_BASE_URL` at a scripted model (`scripts/chat-e2e-mock.mjs` — the SDK resolves the base URL from env, so no key, no cost) and drives the REAL route: broken recipe rejected, repair `tool_result` observed server-side, corrected recipe on the Brew button, twice-broken → no button + user told, emoji split across SSE deltas comes out clean. 16 checks; neutering the validator drops it to 7/16 (proven). Exists because unit tests can't see an unwired guard — exactly how #530/#535 shipped.
- **All 17 workflows** (`.github/workflows/`): `chat-agent.yml` (+ `.github/chat-agent-trigger` — live chat recipe-quality measurement: 4 scenarios × 5 runs on Sonnet through the real prompt + real validator; first run 2026-08-22, see the chat-guard Done entry), `smoke-recommend.yml` (+ `.github/smoke-recommend-trigger` — ONE real /recommend model call through the app's own callRecommendModel, on demand; the same steps also run as deploy.yml's `smoke` job after EVERY deploy since #553 — the gate the two-day temperature outage proved missing), `recommend-logs.yml` (+ `.github/recommend-logs-trigger` — read-only pull of the app container's recent recommend error lines off the VPS, for diagnosing a "Recommendation failed" from the actual error), `deploy.yml` (push→main → VPS deploy **+ the post-deploy `smoke` job**), `ci.yml` (PR checks), `migrate-on-push.yml` (+ the `.github/migrate` marker — the standard migration path), `migration.yml` (manual dispatch alternative), `server-diagnostics.yml` (SSH diagnostics incl. provider env readout), `loading-insights-refresh.yml` (monthly insight agent), `translate-flavor-notes.yml` + `field-zones-remap.yml` (one-shot admin triggers), `recommend-spike.yml` (+ `.github/recommend-spike-trigger`), `recommend-variety.yml` (+ `.github/recommend-variety-trigger` — measures recipe repetition at CANDIDATE level over 20 fed-back brews; the menu-level tests could not see it), `verify-cache.yml` (+ `.github/verify-cache-trigger` — live proof the /recommend system prompt is actually prompt-cached: call 1 writes, call 2 reads; offline size guard is `tests/dataflow/recommend-cache-prefix.test.mjs`), `ios-testflight.yml` (monthly TestFlight rebuild), `ios-bootstrap.yml`, `ios-dev-cert.yml`.
- **`screenshots` job** — boots the app against a throwaway Postgres (applies every migration to an empty DB), logs in via the **PIN path** (`POST /api/auth/login {type:"pin"}`; CI uses a throwaway `AUTH_PIN=1234` + test `AUTH_SECRET`, runs `next dev` so the session cookie isn't `secure`-gated over http), then drives a headless mobile-viewport Chromium (Playwright, installed ad-hoc — not a committed dep) through every key screen and uploads them as the **`app-screenshots`** artifact. This is how a human/Claude eyeballs layout on a PR without running anything; download the artifact from the PR's checks. A screen returning 5xx fails the job. AI sections render empty unless an `ANTHROPIC_API_KEY` repo secret is set (run passes either way). Screen list + opportunistic dynamic-route capture live in `tests/smoke/screenshot.mjs`.

---

## Project Structure & Key Files

### Pages (`src/app/`)

Light migration is **complete** as of PRs #134–#137. Every visited route lives inside the **`(light)` route group** (BTTS Light theme — Cream background, Fraunces/Chivo, anthracite foreground, generative Field) and inherits `LightShell` from `(light)/layout.tsx`. The `(light)` segment is URL-invisible — `/coffees` resolves through `(light)/coffees/page.tsx`. `LightShell` sets the `[data-light-scope]` data attribute used by the CSS shim in `globals.css`.

Every route is now Light, including `cafes/map` (headlined "Nearby") — the cream→transparent scrim at the top keeps the title legible against the warm-tinted Positron tiles.

| Route | Theme | Purpose |
|-------|-------|---------|
| `(light)/page.tsx` | Light | Home BTTS — daily greeting, Action Pill (Brew-Again candidates), inline AI chat over `/api/explore-agent` |
| `(light)/layout.tsx` | — | Wraps `(light)` group in `LightShell` (sets `[data-light-scope]`) |
| `(light)/past-conversations/page.tsx` | Light | Conversation history list (archived chats) |
| `(light)/past-conversations/[id]/page.tsx` | Light | Single past conversation thread (read-only replay) |
| `(light)/brew/new/page.tsx` | Light | Multi-step brew flow — routes `flowStore.step` to the right `LightStep*` component |
| `(light)/brew/[id]/page.tsx` | Light | Read-only session detail — Brew-method as headline, Field of the linked coffee, **2×2 stat grid** (Dose \| Grind on row 1, Water \| Temp on row 2 — PR #215, replaces the prior 3-up + Grind-below layout), then sections for brew notes / taste / reasoning |
| `(light)/coffees/page.tsx` | Light | Coffee library — searchable list with full-bleed bag-photo card (96 px left strip), brew count over Brew CTA in the right column |
| `(light)/coffees/[id]/page.tsx` | Light | Coffee detail — Field + rotation toggle + gated Brew CTA + **single coach card** between Roaster and Notes (rotation-only; reads the coffee's own `coffees.coach_insight` column — Opus-generated per-coffee insight via `CoffeeCoachCard`, migration 0015, replacing the old library-wide citationFields overlap that surfaced other bags' insights on the wrong coffee) + rating history + brew signatures |
| `(light)/coffees/drip/new/page.tsx` | Light | Drip-bag scan + log — single-serve sachet (e.g. INNO Signature Drip): scan identity, pick tasted flavours, 1–5 star rating. No recipe, no timer. Writes to the isolated `drip_bags` table (migration 0016) |
| `(light)/coffees/drip/[id]/page.tsx` | Light | Drip-bag detail — read-only identity + Field + bag/tasted notes + rating. Isolated from sessions/coffees/AI corpus |
| `(light)/cafes/page.tsx` | Light | Café Library — tabbed list (Cafés + Coffees tasted out), photo-strip cards in the Coffees tab |
| `(light)/cafes/place/[slug]/page.tsx` | Light | Single café detail + inline session edit panel |
| `(light)/cafes/coffee/[id]/page.tsx` | Light | Coffee tasted at an external location, cross-links to library coffee via `coffeeId` |
| `(light)/taste/page.tsx` | Light | Taste profile — Avg rating + rated count header, then **Coach** (top 3 of the `new` + saved-`trying` insights, two-stage workflow — New: Save to try / Confirmed / Doesn't apply; Saved: It helped / Didn't help / Skip; the back of the 5–8 queue slides into place when one is solved — PRs #215 + migration 0017), then **What you brew** (always-visible, no collapsible): FlavorWheel + top flavors + rating trend + body / acidity + best origins / processes / methods |
| `(light)/login/page.tsx` | Light | Passkey (WebAuthn) login UI + PIN fallback + reset path |
| `(light)/onboarding/page.tsx` | Light | **Deprecated** first-run equipment + grinder wizard — nothing routes to it; the profile is code-canonical (see the single-user Hard Rule). Kept only for legacy reach-by-URL. |
| `(light)/offline/page.tsx` | Light | Service-worker document fallback (`next.config.mjs` `fallbacks.document`). Shown when an uncached route is opened offline; links to `/coffees`. Safety net — the real offline path lives in the precached `/coffees` + `/brew/new` shell. |
| `layout.tsx` | — | Root layout: PWA meta tags, font preloads, `<ScrollContainer>` wrapper |
| `loading.tsx` | Light | Global loading state — Light CoffeeBeanGlow on inline cream bg (renders before LightShell mounts) |
| `(light)/cafes/map/page.tsx` | Light | Nearby — full-bleed Leaflet map (Positron tiles warmed via the `[data-light-scope]` sepia filter on `.leaflet-tile-pane`); floating header with the Light wordmark pattern + cream→transparent scrim so the title reads cleanly over the tiles |

Removed routes: legacy Dark `page.tsx` (replaced by `(light)/page.tsx`), `match/page.tsx` + `/api/match` (folded into `/api/explore-agent`), `explore/page.tsx` + `/api/explore` (replaced by the inline chat on home over `/api/explore-agent` — the API route itself is deleted, not just the page), `library/page.tsx` (the Coffee Library / Café Library picker — redundant once `NavigationOverlay` gained direct entries for both).

### API Routes (`src/app/api/`)

| Route | Purpose |
|-------|---------|
| `auth/login-challenge` | WebAuthn: generate login challenge |
| `auth/register-challenge` | WebAuthn: generate registration challenge |
| `auth/login` | WebAuthn: complete login |
| `auth/register` | WebAuthn: complete registration |
| `auth/logout` | Invalidate session cookie |
| `auth/status` | Check auth state |
| `auth/reset-passkey` | Re-enroll a passkey (clear + register) |
| `sessions` | ★ Core CRUD — GET (paginated feed) / POST new session |
| `sessions/[id]` | GET / PUT / DELETE individual session |
| `coffees` | GET library / **POST new coffee — the standalone creator (Aug 2026).** Before it, a `coffees` row could ONLY be born as a side effect of `POST /api/sessions`, so nothing could add a bag without brewing it (that's why the chat used to say "go log it by hand and come back"). Id = the shared derived slug `coffeeKeyFor(roaster, name)` (`src/lib/coffee/coffeeKey.ts`, extracted from the sessions route — **both writers must use it or the same bag forks two rows**). Creates with `sessionCount: 0` / `sessionIds: []` / `inRotation` (default true); computes `fieldZones` from the bag notes via `mapNotesToZones`; runs `roastDate` through `guardRoastYear` (`src/lib/coffee/roastDate.ts`). **Idempotent by design** — an existing bag MERGES (fills only empty fields, never overwrites, returns `created:false`), because the ActionPill's "added" state is local and lost on reload so a re-tap must be harmless. |
| `coffees/[id]` | GET / PUT / DELETE individual coffee |
| `coffees/compact` | POST-only, CRON_SECRET-gated **batch summarizer** (Ofelia, Mondays 03:00 UTC = 04:00/05:00 Düsseldorf): walks every coffee with ≥2 sessions and writes `writtenSummary` + `whatToExplore` per bag via Haiku (skips bags summarized <7 days ago). NOT a dropdown list — that's `loadCoffeeLibraryCompact()` in `src/lib/claude/coffeeLibrary.ts`. |
| `recommend` | ★ POST coffee + context → 2–4 AI brew recipe candidates. Each recipe carries a structured `pourSteps[]` (per-step action/grams/duration/temperature/notes; agitation emitted as explicit stir/swirl steps) alongside the legacy `pourSequence` string; each candidate carries `basedOn` (the reference recipe it adapts, or "Own recipe"). `pourSteps` is sanitised + action-normalised post-parse (`sanitizeRecipe`) — a malformed array is dropped, never fatal (PR #197/#198). **Runs on Opus by default** (back on Opus Aug 2026, PR #519, after three prompt instructions leaked on Mistral in two days) via `src/lib/ai/recommendProvider.ts`; Mistral is opt-in via `RECOMMEND_PROVIDER=mistral`, auto-fallback to Opus on a Mistral error. **Served as a background job** — the flow POSTs `/api/recommend/start` (detached ~1-min generation parked in an in-memory job store) and polls `/api/recommend/status`, so a backgrounded iOS PWA no longer kills the request; the synchronous `/api/recommend` POST is kept as a back-compat wrapper (both share `runRecommendation`). **Locked-method fallback (Aug 2026):** when a locked method has NO corpus recipe (the Orea Apex/Classic/Open bottoms), selection falls back to the best-fit portfolio and the prompt says to adapt the nearest cone rather than inject an empty block. deterministic post-parse guards — `vesselCapacity.ts` drops over-capacity-vessel candidates, `dripAssist.ts` (`stripProactiveDripAssist`) strips any "V60 + Drip Assist" candidate the model surfaced unasked (the prompt bans it, but Mistral honours buried negatives less reliably than Opus did), and a **large-batch grind guard** in `recipeFidelity.ts` coarsens (never lengthens — a longer clock over-extracts) any candidate scaled ≥1.3× a verified reference that kept a single-cup grind (grind-settings +20°/doubling; PR #480). **Repetition control — the working version is Aug 2026, NOT #480.** #480/#483/#486 all rotated ties and none of them changed the injected menu: rotating equal-scoring recipes reorders them, and the one-per-brewer loop then takes each brewer's BEST recipe, which is the same recipe in any order. Measured on the real selector: **1 distinct menu across 20 consecutive brews**, for two different coffees. What works is rotating **which recipe represents each brewer** (`pickRepresentative`, within 1 point of that brewer's best so fit still decides) plus **hashing the seed** (a ms timestamp `% groupSize` is degenerate — 86,400,000 divides 2,3,4,5,6,8,9,10, so consecutive days hit the same offset). Measured after: 19/20, 15/20, 18/20. Pinned by `tests/dataflow/recipe-menu-variety.test.mjs`, which also asserts a clear winner still leads and the menu stays in best-fit order. |
| `analyze-bag` | Claude Vision → coffee identity from bag photo. Tasting notes are returned in **English** (translated from the bag's language). Does NOT generate follow-up questions — the client builds deterministic, field-targeted ones (`src/lib/scan/clarifications.ts`). The old `analyze-bag/clarify` Haiku round-trip route was **removed** (July 2026). |
| `analyze-url` | Scrape & analyze a coffee product page URL |
| `brew-insight` | AI terrain/pattern one-liner for post-brew screen |
| `taste-summary` | AI written summary of taste evolution across sessions |
| `greeting` | ★ Haiku daily-starter for the BTTS Home — time-of-day-aware, references rotation bags. Cached client-side by `(date, time-bucket)` |
| `conversations` | GET list / POST new conversation |
| `conversations/[id]` | GET / PUT / DELETE individual conversation thread |
| `conversations/active` | GET the currently-active conversation (live thread on /home) |
| `conversations/archive` | POST → move the active conversation to past-conversations |
| `conversations/cleanup` | POST (cron-auth) — Ofelia daily at 04:00 UTC, deletes archived conversations older than 7 days. Live conversation (archivedAt IS NULL) is NEVER touched. Messages cascade-delete via the conversation_messages FK. (PR #217) |
| `explore-agent` | ★ Agent loop with tool-use (`search_places`, `fetch_page`, `analyze_image`, `suggest_navigation`, `start_brew`, `remember_advice`, `add_coffee`) — powers the inline chat on `(light)/page.tsx`. `start_brew` (PR #199/#200) is a terminal action tool: when the agent has laid out a complete recipe for a library bag it hands the exact recipe (dose/water/ice/temp/grind/`targetTimeSec`/`pourSteps` + method/title/basedOn) to the brew timer so the user lands straight in Step "brew" — no context, no re-recommendation. Its `destination` is set from the TOOL NAME (the input has no destination field — that omission caused the no-op fixed in #200). Per-turn context now includes recipe names + a "Most Recent Brew" block, all read from the actually-brewed candidate. **start_brew id availability + sanitation (PRs #410/#413, June 2026):** the chat can only `start_brew` a bag it has an `id` for, so the per-turn context injects BOTH the ★-rotation bags AND the recent library (`loadCoffeeLibraryCompact(200)` merged with `loadRotationCoffees()`, deduped), each carrying its `[id:…]`. **The cap was 50 until Aug 2026 — once the library passed 50 bags the regression came straight back for the oldest unstarred ones, silently; 200 clears the whole library at ~30 tokens a line.** Recent brews are now read SERVER-side (`loadRecentSessions(20)`, `src/lib/claude/sessionCorpus.ts`) instead of trusting the client's POSTed array, which was capped at 5 — the roaster + variety prior blocks derive from that window and were starved by it. The client array remains the fallback for a failed DB read. Without the recent-library half a freshly-scanned-but-unstarred bag had no id and the model fell back to a `coffee_library` link / `brew_again` — the "chat won't open the recipe" regression (#247 had narrowed the chat context to rotation-only on June 6; #413 restored the recent-library injection while keeping #247's ★-rotation discipline for "what should I brew?"). The prompt now makes `start_brew` **mandatory** whenever the reply lays out a recipe for a library bag — a `coffee_detail`/`coffee_library` link or `brew_again` as the CTA for a written recipe is forbidden; `brew_again` is scoped to "wants to brew a bag but you did NOT write a recipe". The chat's raw `start_brew` recipe is run through `cleanChatRecipe()` server-side — `sanitizePourSteps()` (shared `src/lib/utils/pourSteps.ts`, action-normalized exactly like `/recommend`'s `sanitizeRecipe`) + a derived `pourSequence` backstop + `reconcileWaterToPourPlan()` — so the brew timer renders its pour guide instead of a blank screen (the AeroPress "no steps" bug, #410). **Since 2026-08-21 it is ALSO validated** (`recipe/validateRecipe.ts`): a recipe that isn't brewable goes back to the model as an error `tool_result`, it gets ONE repair round, and a second failure means no Brew button rather than an unpourable recipe on the timer. Until then this path had one guard where `/recommend` has ten. **`remember_advice`** is a second terminal action tool (chat→recommendations bridge): when the chat works out durable, parameter-level guidance for a specific library bag, it surfaces a **tap-to-save** "Remember this for …" pill (`ActionPill`). Tapping POSTs to `/api/insights`, which writes a `status='trying'` / `source='user-confirmed'` insight row (so `/recommend` + the brew Context reminder pill apply it next time that coffee is brewed) AND writes the same note into the targeted coffee's `coach_insight` column (so it becomes that bag's card on `/coffees/[id]`). The chat names the bag in the observation text (per-coffee targeting) and supplies `citationFields` (recommend ranking). Nothing is written until the user taps — the chat never silently persists. **`add_coffee`** is a third terminal action tool (Aug 2026) — the chat puts a NEW bag INTO the Coffee Library itself. Before it, the chat could read a bag off a photo perfectly and then had to tell the user to go log it by hand in the scan form and come back (there was no standalone coffee creator at all — see the `coffees` POST row). It surfaces a tap-to-add pill; tapping POSTs the read bag to `/api/coffees`. The bag photo is attached SERVER-side from the turn's `attachedImageUrl` (never echoed by the model — a drifted URL would put the wrong picture on the coffee). **A brand-new bag has no id until it exists**, so `remember_advice` cannot target it — that is why `add_coffee` optionally carries `observation`/`suggestion` and the SAME tap also writes the coach note (chained to `/api/insights` with the id the create returned). The prompt requires asking ONCE for what's genuinely missing (roast date above all), accepting "not sure", never inventing a field, and using the roaster + name exactly as printed (they form the id). Payload mapping is pure + unit-tested in `src/lib/chat/addCoffee.ts`. **Caveat:** the saved note lives in TWO places with independent statuses (the insights row and the coffee's `coach_insight` card) and they are NOT synced — "Doesn't apply" on the /taste card clears the insights row but the coffee-detail card keeps showing until acted on there too, and vice versa. **Variety + exploration (2026-08-21):** the chat had NO anti-repetition machinery at all — the full 135-recipe corpus arrives cached and byte-identical every turn with no scoring or rotation, and the recent-brews block existed only so it could QUOTE the last recipe. Added: `src/lib/chat/todaysAngles.ts` (a small UNCACHED block running the same scorer as /recommend, seeded per day × per bag, naming concrete angles for the rotation bags — rotates daily, stable within a conversation, every name a real corpus recipe), `coffees.whatToExplore` finally plumbed through `CompactCoffee` onto each library line, coach insights injected (the prompt had claimed "recent research insights" for months while nothing was), and `recentReferenceNames` (moved to `historyUtils.ts`) as a "steer away unless asked to repeat" block. The system prompt gained an **Exploration posture** section (one optional variation line per recipe, exactly ONE variable, grounded in a technique id) and its "never free-hand a breakdown" rule was **scoped back to arithmetic**: parameter-level exploration is always open, and a self-authored experiment is allowed when labelled as one, never attributed to a person, with round cumulative milestones stated as a running total (`cleanChatRecipe` re-checks server-side). `max_tokens` 2000→2500. Tests assert the ROUTE imports and pushes each block, not just that the functions work. **Since #545 the prompt + tool definitions live in `src/lib/chat/agentPrompt.ts` and the per-turn builders + `cleanChatRecipe` in `src/lib/chat/agentContext.ts`** (byte-identical extraction) so the live measurement harness fires the real thing; the route imports both. |
| `research` | Deep-research agent. **Dormant since 2026-08-18 (owner decision):** its Ofelia cron was removed — it wrote news/hints/questions that no reader consumed since the four knowledge GET routes were deleted. The route + `src/lib/knowledge/{news,hints,questions}.ts` writers still exist but nothing schedules or calls them. |
| `preferences` | GET / POST user preferences (equipment, grinder, location) |
| `roasters` | GET / POST roaster profiles |
| `roasters/generate` | AI-generate roaster style summary |
| `places` | GET / POST café locations (auto-geocodes via Nominatim/OSM on POST) |
| `cafes` | GET aggregated café summary across sessions (visit count, avg rating, last visited) |
| `upload` | Multipart photo → Hetzner S3, returns URL |
| `voice/synthesize` | POST text → ElevenLabs TTS audio |
| `voice/transcribe` | POST audio → ElevenLabs Scribe STT transcript. Sends `tag_audio_events=false` (+ a `stripAudioEventTags()` backstop) so the "I'm listening" earcon / kettle clatter isn't transcribed as e.g. `(computer chirp)` |
| `insights` | ★ Coach observations over the full session corpus. **Mistral Large (Opus fallback)** via `src/lib/ai/coachProvider.ts` (`src/lib/claude/insights.ts`; was Opus until PR #463). GET = cache-aware regeneration + full list for /taste (client-side status filtering); a `?status=` filtered read exists but currently has no UI consumer (both per-coffee surfaces read `/api/coffees/[id]/insight` since migration 0015). PATCH `{ id, status }` advances an insight through a **two-stage** workflow (migration 0017): **New** (`new` → Save to try / Confirmed / Doesn't apply) then **Saved** (`trying` → It helped=`confirmed` / Didn't help=`doesnt-apply` / Skip=`snoozed`). `snoozed` rows set `snoozed_until` (default +7 days) and are hidden until it passes, then resurface and regen treats them like `new` — EXCEPT `source='user-confirmed'` rows, which regeneration never deletes (June 2026 fix; previously Save → Skip → 7 days could silently delete a hand-saved note, because PATCH also used to reset `source` to `'opus'` on snooze). Confirm is the only transition that changes `source` (→ `user-confirmed`). The regeneration only replaces `status='new'` (and expired-snooze opus) rows; user-acted rows are preserved and re-emitted similar observations inherit the existing status (text-match on first 80 chars). **POST** writes a chat-authored coach note (tap-to-save from the home chat's `remember_advice` pill, and the post-brew Summary insight card) to **two** places: (a) an insights row (`status='trying'` or `'confirmed'` / `source='user-confirmed'`), deduped on the first 80 chars and anchored to the current corpus `latestSessionMs` so it never freezes regeneration; and (b) the targeted coffee's `coach_insight` column so the note becomes that bag's card on `/coffees/[id]` — a precise per-coffee write that supersedes the auto-generated per-coffee insight (rotation-gated card; a note saved for an out-of-rotation bag won't render a card). The two copies' statuses are NOT synced (see explore-agent row caveat). (PR #215, migrations 0014 + 0017) |
| `coffees/[id]/insight` | GET / PATCH — the **per-coffee** coach card (`coffees.coach_insight`, migration 0015, **Mistral Large (Opus fallback)** via `src/lib/ai/coachProvider.ts` from `src/lib/claude/coffeeInsight.ts`; was Opus until PR #463). GET returns the cached insight, regenerating when this coffee has a newer session AND status is `new`/`doesnt-apply` (never while `trying`/`confirmed` — don't move the card under the user). PATCH advances the card's own status. Consumed by `CoffeeCoachCard` (/coffees/[id]) and the /brew/new Context reminder pill. |
| `admin/prewarm-coffee-insights` | POST (CRON_SECRET bearer) — one-shot Opus pre-warm of per-coffee insights for every rotation coffee, so /coffees/[id] cards appear instantly after migration 0015 / a rotation batch. Preserves `trying`/`confirmed` cards. |
| `admin/translate-flavor-notes` | POST (CRON_SECRET bearer) — one-shot retroactive backfill (PR #473): translate existing scanned flavour notes to English (`coffees.bag_flavors` + `drip_bags.bag_notes`) for bags scanned before PR #471 translated at scan time. Writes a row ONLY when translation changed it (targeted by id, never a wipe); leaves `commonNotes` + drip `flavor_notes` alone; idempotent (English→English is a no-op). Via `src/lib/scan/translateNotes.ts` (Haiku; returns the original array on any failure — never invents/drops). Trigger: the `translate-flavor-notes` workflow (SSH → `docker compose exec app curl`, ~5-min retry to outlast the deploy). **Already run on prod 2026-07-03: 49 rows, 15 translated.** |
| `admin/remap-field-zones` | POST (CRON_SECRET bearer) — one-shot re-map of every coffee's `field_zones` after a zone-palette change (e.g. the `cool-berry` blue-berry pass). |
| `recommend/start` + `recommend/status` | ★ The background-job pair for `/recommend` (see the `recommend` row). `start` kicks off a detached generation and returns a `jobId`; `status` polls it. In-memory job store (`src/lib/recommend/jobStore.ts`, 10-min TTL); single-user, one process. |
| `live-activity/start` + `live-activity/end` | ★ The iOS Live-Activity push backend (G3). `start` registers the activity push token + the per-step schedule; the server pushes each brew step over APNs so the lock-screen timer advances while the phone is locked. `end` clears it. See docs/ios-shell-roadmap.md. |
| `loading-insights` | GET — live rows of the **auto-refreshed loading-screen insight pool** shown during the recipe-crafting wait (`LightStepRecommend`). Defensive: returns `[]` on any failure (incl. pre-migration) so the static `COFFEE_HINTS` seed always covers it. requireAuth-gated. |
| `loading-insights/refresh` | ★ POST (CRON_SECRET) — the **insight agent**. Generates short headline lines grounded in the verified corpus (recipes/varieties/techniques) + brew aggregates + live `web_search` (each web line must carry a verbatim quote), runs every candidate through a deterministic gate (`src/lib/insights/loadingInsightLint.ts`) **and** a model claim-check, inserts survivors, retires oldest over a 150 cap. Full-auto, **NO human review** — the machine gate replaces it so nothing ungrounded reaches the screen (the "never fabricate" rule). Table self-bootstraps (`CREATE TABLE IF NOT EXISTS`). Monthly via `.github/workflows/loading-insights-refresh.yml` (SSH → `docker compose exec app curl`, reuses deploy key). **Full reference: `docs/loading-insights.md`.** |
| `coach-question` | POST — post-rating micro-dialogue (Sonnet). `LightStepLog` calls it only when its local ambiguity check fires, which since Aug 2026 is THREE signals: a ≥20% timing over/under-run, bitter-at-a-low-rating, or muddy-at-a-high-rating. **"Improved while cooling" was a fourth and is not one any more** — it is a Yes/No the user just answered in that same form, so it fired on nearly every brew and the model, holding it as its only signal, asked about cooling every time (the "it ALWAYS asks the same thing" report). It remains available as context for a question raised by a real signal. The route also **refuses to repeat itself**: it loads the questions already answered (`loadRecentSessions`), tells the model not to ask them or a rephrasing and that asking nothing is preferred, and enforces that in code via `isRepeatQuestion` (`src/lib/coach/questionRepeat.ts`, tested) — an empty response 204s and the client saves silently. The answer is stored as `tasteResult.coachAnswer` and IS read by: the coach's session formatter in `insights.ts` (so it can shape insight rows, which `/recommend` then reads as its COACH INSIGHTS block — an indirect, generation-lagged path) and `buildHistorySummary` (`historyUtils.ts`, covered by `tests/dataflow/coach-answer-reaches-prompts.test.mjs`) — **whose only production consumer is `loading-insights/refresh`, NOT `/recommend`** (verified 2026-08-18: the earlier "→ /recommend" claim here was wrong). **Direct `/recommend` wiring shipped 2026-08-18 (owner Go):** `buildMeasuredFeedback()` (`historyUtils.ts`) builds a MEASURED BREW FEEDBACK block — sessions carrying a `coachAnswer` and/or an Acaia `flowAnalysis`, current coffee first, "" when none — interpolated into `/recommend`'s user message next to the COACH INSIGHTS block. Pinned by `tests/dataflow/measured-feedback-in-recommend.test.mjs`, which asserts BOTH the function's output AND (source-level) that `recommend.ts` imports and interpolates it — a function-only test is exactly what hid the original gap. Before Aug 2026 nothing read the answer at all, while this row and the route's docstring both said otherwise. |
| `drip-bags` | GET list / POST — single-serve drip-bag documentation records (migration 0016). Isolated from sessions/coffees/the AI corpus (mirrors the `cafe-visits` precedent) so they never skew `/recommend`, `/insights`, `/taste`, or the Café Library |
| `drip-bags/[id]` | GET / DELETE individual drip-bag record |
| `cafe-visits` | GET / POST — visit-only café logs with binary thumbs rating (independent of brew sessions) |
| `cafe-visits/[id]` | DELETE — remove a logged visit |
| `admin/seed` | Populate knowledge base (run once on new installs) |

### Components

**Flow steps (`src/components/flow/`):**

All Light. The Dark `Step*.tsx` files (`FlowShell`, `StepMode`, `StepScan`, `StepContext`, `StepRecommend`, `StepBrew`, `StepLog`, `StepSummary`, `StepMatchResult`) were deleted in PR #95 (~4,300 lines) when `/brew/new` cut over to Light.

| Component | Purpose |
|-----------|---------|
| `LightStepMode.tsx` | Home Brew / Coffee Shop / Taste Match selector |
| `LightStepScan.tsx` | ★ Camera / photo upload / URL / manual + AI bag extraction + roaster Q&A (1400+ lines — biggest step) |
| `LightStepContext.tsx` | Occasion, water amount, time, mood, equipment, goal. **Occasions = 6 (June 2026):** Morning Ritual, Deep Focus, Social, Experiment, Summer Time, **Cold Brew** ("After Dinner" removed). **Time = Normal / Special (June 2026, renamed from Quick/Normal):** "Special" is a fast shot (≤~150s, the old "Quick"); long steeps come ONLY from the Cold Brew occasion. Picking **Cold Brew** hides the Time card (the steep is the time) and stamps `timeAvailable: "long-steep"`; `/recommend` then routes to the cold-brew recipe block + the brew step renders `ColdBrewSteep` instead of the live timer. Goal picker has 6 options incl. `aromatic` (PR #192). Method list incl. "V60 + Drip Assist" emergency-only option (PR #193). Locking a method sets `context.preferredMethod` → hard-filters recipe selection. **Coach reminder pill** above the selectors when the chosen coffee's own coach card (`/api/coffees/[id]/insight`) is `status='trying'` — quiet read-only nudge, per-coffee by id, NO attribute-overlap matching (that pre-0015 mechanism surfaced other bags' insights on the wrong coffee and is gone). (PR #215 + migration 0015) |
| `LightStepRecommend.tsx` | 2–4 AI recipe candidates with reasoning. Selecting a candidate sets `brew.selectedCandidateIdx` so Brew/Log/Summary read THAT candidate's recipe by index, not by method name (PR #193 — fixed alternative inheriting the primary's temp/grind when both share a method). Shows the candidate `title` + `based on …` reference name (PR #198). |
| `LightStepBrew.tsx` | ★ Circular timer + **step-by-step, method-aware** pour guide (Web Audio cue + vibrate on step change). Shows the recipe **name** (candidate `title` + `based on …`). Two renderers, chosen per recipe (PR #197): **`LivePourSequence`** for percolation (cumulative-grams pours, drawdown reserve) and **`StepGuide`** for immersion/AeroPress/iced/staged (setup card, steep countdown, action cards for invert/flip/press/drain/bypass). **Agitation is recipe-driven** (PR #198): the swirl/stir button shows only where the recipe calls for it — no stray swirl on a reduced-agitation recipe. Reads the recipe via `selectedCandidateIdx`; per-pour temperatures + notes render per step. **Live Acaia scale coaching** (PRs #330/#411): when the scale is connected, the active **pour** step shows a `CoachCue` ("Steady" / "Ease off" / "Overshot +Xg" with the running grams) from `coachFlow` (`src/lib/brew/flowCoach.ts`, over the canonical `buildBrewTimeline`). #411 lifted the prior percolation-only gate so `coachFlow` coaches ANY step carrying a cumulative-grams target — so immersion/AeroPress water-pour steps get the same scale guidance as a V60 (the `StepGuide` renderer now also receives `coach`); steep/press/drain steps (no grams target) stay time-only. Off-native / no scale connected → no cue, exactly as before. **#443:** the active step is TIME-based (`getActiveIdx`). **PR #471 + #484 (tare, July 2026):** the `CoachCue`/`WeightHold` show **water poured** = live `scale.weight` minus a tare baseline captured ONCE at brew start (the vessel already on the scale becomes the zero, so an un-tared scale reads correct water-poured). #484: the baseline stays FIXED after Start — it does NOT chase later weight jumps, so moving the brewer for a swirl can't rebase the zero (that live-rebasing was the +296.7g phantom-overshoot bug from #483). Genuine mid-brew mass events are folded post-brew by `rejectNonPourJumps`, which tells a returning transient (swirl) from a sustained change by looking ahead up to 8 s for a return — a window, not the single next sample, which is what let multi-second swirls through until Aug 2026. The top `ScalePanel` still shows the RAW reading (physical-scale mirror). No peak/median — the weight-gating was removed in #443. |
| `LightStepLog.tsx` | Post-brew: flavor wheel, star rating, tasting notes |
| `ScalePanel.tsx` | Acaia scale card on the brew screen — connect + RAW live weight + Tare (native shell only; see the G1 notes) |
| `ColdBrewSteep.tsx` | Cold-steep renderer (recipe card → Start steep → countdown → "Leave it steeping") — replaces the live timer for cold-brew recipes |
| `BlendComponentsEditor.tsx` | Blend-components editor (2+ components, migration 0021) — consumed by `LightStepScan` + `LightStepContext` |
| `LightStepSummary.tsx` | Review + save session |

**Light UI primitives (`src/components/ui/light/`):**
`LightShell` (wraps `(light)` group, sets `[data-light-scope]`), `LightFlowShell` (drives `useFieldConfig` per step, scrolls top on step change), `Field` (reads FieldContext → renders the **living motion stack** fixed -z-10: directional `composeFieldGradient(zones, rotation)` base + `FieldBlobs` (the **murmuration** colour masses) + `FieldGrain` + finger-following `FieldBloom`, driven by `useFieldMotion` CSS vars — see docs/liquid-design.md), `FieldBlobs` (the murmuration layer — all colour masses ride ONE shared slow `field-flow` sweep so they flow together + turn direction, with small per-mass `murmur-*` drift; co-located keyframes) / `FieldGrain` / `FieldBloom` (the Field motion layers), `HaikuStarter` (home welcome-haiku — shimmer → scattered per-word spring entrance → dissolve → per-word touch lens), `Card`, `Section`, `Footnote`, `Chip`, `Hero` (eyebrow + Fraunces 40px question), `CTA` (anthracite button + cream text), `CTAWarmth`, `ActionPill` (Brew-Again candidates on home), `ChatInput`, `ChatThread`, `AttachmentSheet`, `NavigationOverlay` (full-screen menu — Home / Past Conversations / New Session / Coffee Library / Nearby / Café Library / Taste Profile), `ReferenceCoffeePicker`, `StarRating` (rotation toggle + log rating), `CircularTimer` (Light fork — anchored to Date.now, visibility-snap), `CoffeeBeanGlow` (anthracite stroke fork), `ConnectionStatus` (top-center pill rendered by `LightShell` — shows Offline / Syncing / "didn't sync — tap to retry"; owns the offline-save flush, re-checking `navigator.onLine` on mount + `visibilitychange` rather than the unreliable iOS online event), `LiquidHeadline` (reusable per-word scatter entrance + reverse per-word exit — backs the animated Hero questions + the recipe insight; see docs/liquid-design.md), `BagPhoto` (the ONE shared rounded-3xl inset bag-image treatment + cream scrim — coffee-detail / Save-brew / scan preview all use it), `CraftingStatus` (recipe-screen status line: black, sentence-case card-title style, cycling phases + animated 1-2-3 dots), `RecommendJobWatcher` (mounted in `LightShell` — polls `/api/recommend/status` for the background recipe job).

**Shared / Dark-era UI primitives (`src/components/ui/`):**
`Button`, `CoffeeBeanGlow` (kept for `PhotoUpload`; CSS shim `[data-light-scope] { filter: brightness(0) }` inverts it to anthracite at the consumer), `FlavorWheel` (now Light palette in place — canvas transparent, cream-glass panels, anthracite text/icons), `BrewMethodIcon` (inverted via the same shim), `NumberStepper`, `PhotoUpload`, `PlaceSearch`, `ProgressDots`, `StarRating` (still consumed by `CafeMap` only), `ThinkingDots`, `WaveformBars`.

Removed during the Light cleanup (PR #137): `BottomNav`, Dark `Chip`, `RadarChart` — all orphaned once their consumers migrated.

**Layout (`src/components/layout/`):**
`ScrollContainer` (root 100dvh wrapper with hidden scroll — no more allowlist, no nav-padding reserve), `BottomSpacer`

**Session:** `SessionCard` (Light, consumed only by `/coffees/[id]` All-brews list — Brew-method as headline, Field's cream-glass cards, swipe-to-delete with rust-red destructive button)
**Cafés:** `CafeMap` (Leaflet — consumed by `(light)/cafes/map`, now Light with warmed Positron tiles)
**Coach (`src/components/coach/`):** ONE file, `CoachCard.tsx`, exporting BOTH `CoachCard` (presentational card with the two-stage footer — New: Save to try / Confirmed / Doesn't apply; Saved: It helped / Didn't help / Skip — consumed by `/taste` queue) and `CoffeeCoachCard` (per-coffee card that reads the coffee's own `coffees.coach_insight` column — migration 0015 — a coach-generated insight specific to THIS coffee; rotation-only).
**Native (`src/components/native/`):** `NativeWidgetBridge` (mounted in `LightShell` — btts:// deep-link listener + pushes the rotation to the widget App Group; inert off the shell). The old library-wide `citationFields`-overlap matching was replaced by 0015 because it surfaced other bags' insights on the wrong coffee. PR #215.

### `src/lib/`

```
lib/
├── coffeeHints.ts          # ★ COFFEE_HINTS — static seed + offline floor for the recipe-screen insight pool (docs/loading-insights.md)
├── insights/
│   └── loadingInsightLint.ts  # ★ Deterministic gate for the loading-screen insight agent — shared SoT (agent + read route + screen + CI)
├── auth/
│   ├── requireAuth.ts      # Server helper: throws if no valid session cookie
│   └── session.ts          # JWT session cookie create/verify (jose)
├── ai/
│   ├── recommendProvider.ts # ★ The ONLY place that picks the /recommend model: Mistral Large by default (≈¼ Opus cost, PR #455), Opus via RECOMMEND_PROVIDER=anthropic or when MISTRAL_API_KEY absent; auto-fallback to Opus on a Mistral error. Same byte-identical SYSTEM_PROMPT for both.
│   └── coachProvider.ts     # ★ The ONLY place that picks the COACH model (insights + coffeeInsight): Mistral Large when MISTRAL_COACH_API_KEY set, else Opus; COACH_PROVIDER=anthropic forces Opus; auto-fallback to Opus on a Mistral error. Own key = own "BrewLog-Coach" Mistral workspace so coach spend bills apart from /recommend (PR #463). Byte-identical prompts. Sibling of recommendProvider; chat (explore-agent, Sonnet tool-loop) deliberately NOT routed here.
├── claude/
│   ├── recommend.ts        # ★ generateRecommendation() — user-message assembly + recipe generation. Model call goes through ai/recommendProvider.ts (Opus default, Mistral opt-in); deterministic post-parse guard chain: guardRecipeFidelity() → stripMinimalAgitationSwirls() (utils/agitationGuard.ts) → calibrateTargetTimes() (measured timing deltas) → guardVesselCapacity() (utils/vesselCapacity.ts) → stripProactiveDripAssist() (utils/dripAssist.ts) → normalizeGrindToGrinder() (utils/grindUnit.ts). Prompt blocks: roaster prior, coffee tasting history, COACH INSIGHTS, MEASURED BREW FEEDBACK (buildMeasuredFeedback, 2026-08-18), knowledge layer (varieties/recipes/techniques), own-reference recipes, method recency, diversity + recent-recipes notes, timing calibration. rotationSeed = the latest session's timestamp (July 2026 fix — the earlier pastSessions.length saturated at the client cap).
│   ├── recommendPrompt.ts  # ★ The /recommend SYSTEM_PROMPT + RECOMMEND_MODEL ("claude-opus-4-7") + the literal NICHE° GRIND REFERENCE block (hardcoded for prompt-cache stability, pinned to grindSettings.ts) + a GENERATED Comandante-clicks block (Aug 2026 — the hand-copied one had drifted in clicks the way the NICHE table drifted in degrees). Carries NO recipe numbers since Aug 2026: the dozen fully-numbered championship/Chemex/Origami/iced/cold-brew recipes were byte-identical on every call while the per-turn library rotated, so the model kept picking from the fixed list — concrete recipes now come ONLY from the user message. tests/dataflow/grind-reference-consistency.test.mjs scans the WHOLE prompt for stale grinds and asserts the deleted blocks stay deleted.
│   ├── sessionCorpus.ts    # ★ loadRecentSessions(n) — server-side session reads, one indexed query on createdAtMs (explore-agent takes 20, runRecommendation 400; client arrays are the fallback)
│   ├── ownReferenceRecipes.ts # ★ The user's own ≥4★ brews (with a reproducible pour plan) as selectable /recommend references — ≤3, one per bag×brewer, same-bag first; the fidelity guard deliberately does NOT bind to them (pinned by test). **Framing corrected Aug 2026 (owner decision):** the block used to say "repeating a result they already loved is usually the right answer" — with exact numbers, every brew, never rotating — while RECENTLY RECOMMENDED in the SAME message asked for different references; the concrete instruction won and today's 4★ became tomorrow's guaranteed repeat. Now it is a BASELINE TO BUILD ON ("it already worked — what's missing from a 5★?"), verbatim repeats need a reason, and `formatOwnReferencesForPrompt(refs, recentlyRecommended)` marks an entry just served with "build on it, don't re-serve it".
│   ├── measuredGrind.ts    # ★ buildMeasuredGrind() + formatMeasuredGrindForPrompt() — the MEASURED GRIND block (2026-08-23): the user's OWN logged brew.grindSettingUsed, pooled by brewMethodKey and volume-bucketed like the timing calibration (±20%/±60g), median + range + the ≥4★ subset, ≥3 samples per bucket, max 4 brewers, rendered in the grinder's own unit. REPORTED, never enforced — grind is bean-dependent, so a deterministic override would flatten the variation a recipe exists to express (the pre-per-field fidelity-guard mistake). Closes the gap that every per-method row in grindSettings.ts except the V60 is an ESTIMATE while the real settings were logged on every brew. Tests: tests/dataflow/measured-grind.test.mjs (function AND route interpolation; proven to fail when unwired).
│   ├── methodRotation.ts   # buildMethodRecency() + demoteBrewers() — the METHOD FIT & FRESHNESS block + tie-scoped menu demotion (never a ban; inactive on locked method / cold brew). Tests: tests/dataflow/method-rotation.test.mjs
│   ├── recipeFidelity.ts   # ★ Deterministic backstop (PR #266): after the model returns candidates, reconcileToReference() snaps a candidate back toward the VERIFIED corpus recipe it's `basedOn` when it drifted beyond tolerance, scaled to the user's batch. **PER-FIELD since Aug 2026:** it used to be all-or-nothing — ONE drifted dial replaced temp AND grind AND time AND the whole pour plan, so a deliberate adaptation that was 7°C off came back as the literal published recipe, and two candidates that both tripped it came back IDENTICAL (the guard against fabrication was erasing legitimate variation). Now one drifted dial is corrected alone (time-only keeps the authored pours for percolation — the timer re-derives pour timing; immersion-shaped plans still full-snap, their durations must sum to the total), while ≥2 dials OR a restructured pour plan (new `pourCount` drift signal, tolerance 2) still snaps the whole signature, so the documented "10 pulses → 3 pours" mangle is still caught. Verified-refs only, 0.5–2.5× scale only, skips iced/bypass. Caught the Kasuya Super-Coarse "+50ml→+1:15" mangle. **Large-batch grind guard (PR #480):** even when NOTHING else drifted, a candidate ≥1.3× the reference water that kept a single-cup grind gets its grind ONLY coarsened to the batch value (mid + 20°×(doseRatio−1), grind-settings +20°/doubling); total time + pour plan untouched — the coarser grind carries the flow, a longer clock would over-extract. **Drip-Assist offset (Aug 2026):** `reconcileToReference` now takes the candidate's `method`; when it names the disc the reference grind it compares and snaps to carries **+5°** (`DRIP_ASSIST_GRIND_OFFSET_DEG`, `utils/dripAssist.ts` — the delta both docs/grind-settings.md and the prompt's NICHE° block agree on; absolute baselines differ, the offset doesn't). Fixes two paths to the same reported defect ("Drip Assist war drin aber Rezept nicht angepasst"): the prompt asked for the offset and nothing enforced it (a soft POSITIVE instruction leaks like the soft negative did), and — quieter — a full snap rewrites grind whenever TIME or TEMP drifted and rewrote it to the BARE number, so a wrong clock silently undid a right grind. Applies at any volume (the disc changes flow at every batch size), only moves a grind >4° under target, never touches time or temp. Tests: tests/dataflow/recipe-fidelity.test.mjs + recipe-batch-grind.test.mjs + drip-assist-grind.test.mjs. **Clicks bug fixed 2026-08-21:** `parseGrindDegrees` read "26 clicks" as 26 DEGREES. Both prompts ask for clicks when the Comandante is the grinder, and the corpus states degrees — so every travel recipe looked ~350° too fine, tripped the grind window, and had its grind silently overwritten with the published degree value. Now converts via `clicksToNiche` (the scales can't overlap by magnitude, same property `grindUnit.ts` relies on). Caught only because the new chat validator printed the comparison it was making. **The chat is no longer uncovered** — see `recipe/validateRecipe.ts`.
│   ├── analyzeBag.ts       # Vision prompt + BagAnalysisResult type
│   ├── insights.ts         # ★ Coach orchestrator — NB `serialiseSessionForCoach` (exported for tests) is the line the model literally reads; its `tasteBits` (body/acid/sweet/bitter/finish/clarity + astringency) was assembled and joined into NOTHING until 2026-08-23, so no per-session sensory reading ever reached the coach. — cache-aware regeneration over the full session corpus; preservation tiers (user-confirmed rows are never deleted). Model call routes through ai/coachProvider.ts (Mistral Large, Opus fallback; PR #463)
│   ├── coffeeInsight.ts    # ★ Per-coffee coach card generator — backs /api/coffees/[id]/insight + the prewarm admin route (migration 0015). Model call routes through ai/coachProvider.ts (Mistral Large, Opus fallback; PR #463)
│   ├── escher.ts           # Pattern/terrain interpreter (Escher insights)
│   ├── extractor.ts        # Cross-session pattern extraction
│   ├── brewSignature.ts    # Weighted brew signature per coffee/method
│   ├── patterns.ts         # Pace, craft approach, occasion patterns
│   ├── historyUtils.ts     # ★ buildTimingStats + measuredTimeDelta (timing calibration) + buildHistorySummary (per-session lines incl. coachAnswer + measured pour — consumed by loading-insights/refresh) + buildMeasuredFeedback (the /recommend MEASURED BREW FEEDBACK block, 2026-08-18) + buildRecentRecipes (chat)
│   ├── userProfile.ts      # ★ loadUserProfile() reads preferences table; formatProfileForPrompt() builds the cached "About you" system block (equipment + water + TASTE PREFERENCES + grind table). NB formatProfileForPrompt has exactly ONE production consumer — the chat (explore-agent). /recommend gets taste only as body=/acidity= off the DB row, /greeting reads just profile.equipment; verified 2026-08-24
│   ├── coffeeLibrary.ts    # loadCoffeeLibraryCompact() + formatter — last 30 bags for "what should I open?" questions
│   ├── translate.ts        # Tasting notes ↔ SCA flavor wheel taxonomy
│   └── parseJson.ts        # Safe Claude JSON parsing with Zod
├── types/
│   ├── session.ts          # ★ Core data model (all interfaces). BrewRecipe carries optional structured `pourSteps: BrewPourStep[]` (preferred over the legacy `pourSequence` string) using the `BrewStepAction` union (bloom/pour/final/stir/swirl/wait/press/invert/flip/drain/bypass/melodrip/agitate-bed). RecommendationCandidate carries `basedOn` (stable reference-recipe name). selectedCandidateIdx on BrewLog = the brewed candidate.
│   ├── chatActions.ts      # Chat action-pill types (start_brew / remember_advice / add_coffee payloads) — consumed by explore-agent + chat/addCoffee
│   ├── coffee.ts           # Coffee-specific types
│   ├── preferences.ts      # UserPreferences interface
│   ├── cafes.ts            # CafeSummary + PlaceCoordinates
│   └── dripBag.ts          # DripBag interface — single-serve drip-bag records (isolated from sessions/coffees)
├── db/
│   ├── schema.ts           # Drizzle table definitions (15 tables — see the Database tables section)
│   ├── client.ts           # Lazy Drizzle client + pg Pool
│   └── helpers.ts          # rowToSession, rowToCoffee converters
├── db/migrations/
│   ├── 0000_init.sql                      # All core tables + indexes (only file registered in meta/_journal.json)
│   ├── 0001_add_places.sql                # creates places table; historic seed data, irrelevant in prod
│   ├── 0002_add_place_coords.sql          # lat/lng columns on places
│   ├── 0004_add_cologne_places.sql        # (0003 is intentionally absent)
│   ├── 0005_cologne_specialty_places.sql
│   ├── 0006_add_what_to_explore.sql       # preferences column for explore prompts
│   ├── 0007_add_conversations.sql         # conversations + conversation_messages tables
│   ├── 0008_add_field_zones.sql           # coffees.field_zones jsonb (Field v1.1 persistence)
│   ├── 0009_add_in_rotation.sql           # coffees.in_rotation boolean (rotation marker)
│   ├── 0010_rename_rvtc.sql                # bulk rename "Rösterei Vier / The Commonage" → "RVTC"
│   ├── 0011_add_cafe_visits.sql            # cafe_visits table — visit-only logs + binary thumbs rating
│   ├── 0012_add_lessons.sql                # lessons table (PR #210). DEPRECATED — feature replaced by insights in #211; table preserved but unused
│   ├── 0013_add_insights.sql               # insights table + indexes (PR #211, coach observation corpus)
│   ├── 0013_add_lesson_questions.sql        # questions/answers cols + 'pending' status on the (now-removed) lessons "ask before rating" flow — dead alongside the lessons table
│   ├── 0014_add_insight_status.sql         # status workflow column on insights (`new`/`trying`/`confirmed`/`doesnt-apply`), PR #215
│   ├── 0015_add_coffee_coach_insight.sql    # coffees.coach_insight jsonb — per-coffee Opus insight (CoffeeCoachCard source)
│   ├── 0016_add_drip_bags.sql               # drip_bags table — single-serve drip-bag documentation records, isolated from the corpus
│   ├── 0017_add_insight_snooze.sql          # insights.snoozed_until + 'snoozed' status (two-stage coach workflow, +7-day skip)
│   ├── 0018_add_loading_insights.sql        # loading_insights table — auto-refreshed loading-screen insight pool (also self-bootstrapped by the refresh route)
│   ├── 0019_add_coffee_bag_flavors.sql      # coffees.bag_flavors — the flavors printed ON the bag, promoted from session JSONB
│   ├── 0020_clear_rotation_coach_insights.sql
│   ├── 0021_add_coffee_components.sql       # coffees.components jsonb — blend components (2+); scalars become a comma-joined summary
│   ├── 0022_set_la_cabra_terra_blend.sql    # one-off data fix; the zero-row-is-a-no-op guard pattern (CI applies migrations to an EMPTY db)
│   └── 0023_add_coffee_variety_region_roast.sql # coffees.variety/region/roast_level + backfill from sessions (applied 2026-08-09: 55 rows → 45 variety / 48 region / 49 roast_level)
│   # NOTE: 0001+ are applied manually via `psql` on the VPS — Drizzle journal does not track them.
│   # A migration ships itself: write its filename into `.github/migrate` and merge — the
│   # `migrate-on-push.yml` workflow SSHes in and runs it. No button, no terminal.
│   # Applying schema/code that references a new column BEFORE running the migration on the VPS
│   # makes Drizzle SELECT 500 (column-strict). Always migrate VPS first, deploy code second.
│   # The real places dataset (6,202 rows, verified 2026-05-09) lives only in Production; no seed file in Git.
├── field/                     # ★ Generative Field v1.1 — coffee-driven background gradient
│   ├── types.ts               # FieldZone, FieldZoneId, FieldConfig types
│   ├── zones.ts               # 7-zone perceptual palette (fruity-bright, fruity-deep, floral, nutty-cocoa, spice-earth, sweet-caramel + cool-berry). cool-berry = the one COOL zone, hue 210–244 = real BLUE for BERRIES (blueberry/blackberry/blackcurrant/cassis). Grape/plum stay warm-red in fruity-deep ("blue for berries, purple is for grape/plum" — June 2026). Saturation floors cranked in the Big-Sur pass.
│   ├── defaultZones.ts        # Fallback composition for coffees with no Field yet
│   ├── curatedFields.ts       # ★ CURATED_FIELDS (8 elegant 2–3-flavour combos) + getGeneralField() — the general/home background picks the NEXT combo per app-open (localStorage counter, module-memoized, SSR-safe). Wired as the FieldContext fallback default so every non-coffee screen cycles.
│   ├── composeGradient.ts     # ★ zones + rotation → CSS gradient. DIRECTIONAL (June 2026 murmuration rework): a diagonal linear base blending the 2–3 dominant zones + two large soft corner masses anchoring one diagonal seam + a pale light-ribbon highlight (the "Mac wallpaper" look) — NOT the old 5 scattered radial hotspots. Big-Sur dials (BLOB_ALPHA 0.85, BASE_DESAT 0, etc.) at the top.
│   ├── schema.ts              # Zod schema for persisted field_zones
│   ├── mapNotesToZones.ts     # Haiku call: tasting notes → weighted zone composition (blueberry/cassis → cool-berry)
│   ├── cache.ts               # sessionStorage cache keyed by session id — /coffees/[id] pre-warms,
│   │                            /brew/[id] reads on mount so the cup's Field paints from frame 1
│   │                            instead of flashing default while the coffee fetch resolves
│   └── FieldContext.tsx       # React Context Provider + useFieldConfig() hook. Fallback default + unmount-reset target = getGeneralField() (the per-app-open curated combo), not the static DEFAULT_FIELD_ZONES.
│   # Consumed by <Field> (src/components/ui/light/Field.tsx), LightFlowShell, and the
│   # /coffees/[id] + /brew/[id] detail pages (which both call useFieldConfig directly).
│   # LightFlowShell rotates 25° per brew step (scan 0° → context 25° → recommend 50° → brew 75° → log 100° → summary 125°).
├── knowledge/
│   ├── insights.ts / news.ts / hints.ts / questions.ts   # (alerts.ts deleted 2026-08-18 with the webhook)
│   ├── sca/                # ⚠ ORPHANED — SCA Foundations corpus that fed the deleted lessons distiller; zero consumers since #211 (grep 2026-08-23). Delete or rewire; nothing reads it today.
│   ├── recipes/            # ★ Structured recipe corpus — 145 recipes total (8 championship + 92 reference + 45 experimental; +10 Orea Wide 2026-08-31 in `oreaWide.ts`). **Orea 1→11:** the corpus held ONE Orea recipe (measured 0 Orea appearances / 40 brews), so `oreaWide.ts` adds 10 real, named, sourced OREA V4 Wide recipes (orea.uk guides + named authors — SO Soft/Apex, OG Base+Easy Does It/Classic, Dara+Dara-for-Two+Bypass+Techno/Open, Four Six+Fine+Aussie/Fast), full pour sequences, `verified:false` (honest provenance AND keeps the fidelity guard from snapping them; grind = real Comandante clicks + Niche DERIVED via grindUnit's anchored map, no fabricated Niche — OREA publishes none). championship.ts = WBrC/WAC champions (Kasuya 2016, Du 2019, Medina 2023, Wölfl 2024, Stanica WAC 2024, Nemo Pop WAC 2025 — plus Harris 2017 + Rojewska 2018, real single-competitor finals routines carried `verified:false` = 8 championship. The composite championship "Wendelboe AeroPress" was REMOVED Aug 2026 (#528); the surviving reference-category `wendelboe-aeropress` in expanded.ts is owner-verified `verified:true` since 2026-08-18 — see the #528 Done entry.). reference.ts + expanded.ts = Hoffmann V60/Clever/AeroPress/Moccamaster/Iced, Kasuya 4:6, April V60 (Rolf), Gagné, Perger, Rao, Hatakeyama, Wallgren, Asser Daily Pour-Over (Christensen) + dozens more. markusAdditions.ts = 45 user-supplied recipes (category "experimental"). Full pour mechanics with per-step durations, attribution, sources, verified flag. **Cold brew (June 2026):** 8 long-cold-steep recipes tagged `occasions: ["cold-brew"]` — Hoffmann Fine+Finings (75g:1L RTD), Specialty RTD 1:10, Counter Culture 1:8, Stumptown, AeroPress Overnight, Clever (capped ≤450ml), Toddy, AeroPress cold-immersion. Big-batch immersion uses the new `cold-brew-jar` brewer (always in CANONICAL_EQUIPMENT — everyone owns a jar); the Clever/AeroPress cold brews stay on their own vessels at REAL capacity (a 900ml Clever is a hard error — fixed). `scoreRecipe()` hard-partitions cold steeps (totalTimeSec ≥ 3600 OR occasion `cold-brew`) from the rest, so a 12h steep never leaks into a hot turn and a cold-brew occasion never pulls a hot pour-over. **Since Aug 2026 it partitions ICED the same way** (occasion `summer-time` ⟷ the 8 iced-tagged recipes; it was only a soft +2 before, which is why the prompt had to carry its own numbered iced list — that list is now deleted and the per-turn library IS the iced menu). NO staged/multi-temperature recipes in the corpus — every recipe brews at ONE constant temperature; the Cold-Brew Hot-Bloom variant + the optional 20:80-saline bitterness finish live in the `/recommend` prompt only (hot-bloom uses two temps → not a corpus recipe; it is the ONE sanctioned exception to the no-staged-temp rule, cold-brew only). selectRecipes() injected into /recommend per turn. **Equal best-match ranking (PR #193):** scoreRecipe() has NO pedigree/verified bonus — every recipe competes purely on context match (roast/process/variety/goal/occasion). **Menu variety (Aug 2026 — supersedes #480's tie-rotation, which never worked).** `rotateTies()` still rotates equal-score groups, but that ALONE cannot change the menu: the one-per-brewer loop takes each brewer's best recipe, which is order-independent. The lever that works is `pickRepresentative()` — it rotates WHICH of a brewer's recipes stands in for it, restricted to recipes within `REP_SCORE_TOLERANCE` (1) of that brewer's own best, so it only ever swaps between genuinely interchangeable options. Seeds are hashed via `mixSeed()` first (a raw ms timestamp mod a small group size is degenerate; `mixSeed` is exported since Aug 2026 — the whole turn shares ONE seed so the menu rotation and the brewer demotion can't fight). The result is re-sorted by score, because the prompt tells the model the library order is best-fit-first. Which brewers appear, and in what order, is unchanged. **This layer works and is not the thing to fix again** — measured 19/20 distinct menus, pinned by `recipe-menu-variety.test.mjs`, which since Aug 2026 also runs the PRODUCTION feedback shape (`recentReferenceNames` + `demoteBrewers` accumulated over 20 fed-back brews; the isolated call it measured before is a shape production never runs). When repetition is reported next, measure the CANDIDATES (`scripts/recommend-variety-sim.mjs`), not the menu. **Method lock:** when the user locks a method in the flow (context.preferredMethod), selectRecipes hard-filters to that brewer (via brewersFromMethod) and returns the best N recipes FOR THAT METHOD; with no lock it's the one-per-brewer diversity portfolio. **Full mirror:** @./docs/recipes-full.md (partially stale — predates the experimental additions); @./docs/coffee-experts.md is the curated summary (partially stale). (May 2026 source-audit: removed Turbo V60 — espresso-origin; replaced fabricated "Rolf Minimum Variables" with April's real house V60; corrected Perger to 12g/200g; flagged Hatakeyama numbers unsourced.) **June 2026 primary-source audit (PRs #265–#277, owner-supplied transcripts + web):** every named-expert recipe re-verified against the originator's own video/page. Corrected the worst "different recipe wearing the author's name" cases — Gagné AeroPress (was 80°C low-temp → his real HOT 100°C/18g:260g/10-min), Rao (was "Rule of Thirds" equal-thirds → his actual spin-method 20g:330g, he opposes >2 pours), Hoffmann AeroPress (was inverted → his UPRIGHT Ultimate), Hoffmann Moccamaster (was 8:00 → his measured 3:30 for 750g), Hatakeyama (unsourced reconstruction → real 2024 JBrC: 15g:240g/85°C/coarse). New entries: Hoffmann Japanese Iced V60 (pour-over), Nemo Pop WAC 2025, Stanica's separate inverted-Melodrip recipe (his WAC winner is the upright Flow-Control one — both kept, cross-referenced). Kasuya 4:6 temp 92→93. Fabricated water ppm (Wölfl/Kasuya "55ppm") removed.
│   ├── varieties/          # ★ ~25 WCR-grounded variety priors. Bourbon family (Bourbon, Caturra, Catuai, Mundo Novo, Pacas, Yellow Bourbon, Pink Bourbon), Typica family (Typica, Java, Maragogype, Sumatra), Ethiopia landrace (Heirloom, Wush Wush, Chiroso, Sidra), Geisha, SL series (SL28, SL34, Ruiru 11, Batian), F1 / disease-resistant (Castillo, Tabi, Centroamericano), Pacamara, Mokka. Pink Bourbon flagged per WCR 2024 finding it's NOT a Bourbon mutation. Sidra origin marked as disputed. getVarietyPriorsForBag() injected into /recommend + /explore-agent.
│   ├── techniques/         # ★ 25 atomic brewing moves citable by id — 16 named-expert + 9 general/foundational (added June 2026 in the tag cleanup so every recipe references a real id, not free text). Named: Temperature (turbo/boiling-water-coarse-grind [espresso-origin, Hedrick popularised], Gagné second-sweet-spot), agitation (Rao spin, Hoffmann swirl-not-stir, Perger high-extraction, minimal-agitation, Peng Melodrip, Bailey/Hoffmann water-first), pour-pattern (Kasuya 4:6, Rao thirds), pre-brew (Wallgren sieving, Hatakeyama roast-tailored filter), post-brew (Hoffmann/Stanica bypass, flash chilling), AeroPress inversion, Hendon low-mineral water. General (verified:false, no single originator): bloom, pulse-pouring, immersion-steep, central-pour, spiral-pour, continuous-pour, machine-drip-brew, batch-scaling, flat-bed-pour. Each cross-references exemplifying recipe IDs. Compact id+description list injected per turn. The cross-reference is gated BOTH ways by tests/recipes/validate.mjs: every recipe technique id must exist in techniques, AND every technique exemplifiedBy id must exist in the recipe corpus (reverse check added June 2026 after the staged-temp purge + inversion-exemplar fix left dangling/contradictory exemplars undetected). (staged-temperature + three-roast-layering removed June 2026 with the staged-temp recipes.)
├── roasters/priors.ts      # ★ 50+ curated roaster style priors; getRoasterPrior() + formatRoasterPriorForPrompt() consumed by /recommend AND /explore-agent
├── constants/
│   ├── brewMethods.ts / flavorTaxonomy.ts / scaFlavorWheel.ts
│   └── grindSettings.ts    # ★ Single source of Niche Zero degrees — replaces hardcoded copies in CLAUDE.md / docs / prompts
├── theme/
│   └── gradients.ts        # Shared gradient tokens (CSS strings) used across pages
├── storage/
│   ├── s3.ts               # Hetzner Object Storage (S3-compatible)
│   ├── idb.ts              # ★ Tiny promise-based IndexedDB wrapper — DB `brewlog-offline`, two stores (brewable, pendingSessions). Backs the offline brew feature.
│   ├── offlineLibrary.ts   # ★ Caches coffees + their TOP-2 best-rated recipes (deduped) for offline re-brew. Warmed in background on online /coffees loads.
│   └── saveQueue.ts        # ★ Offline save queue — parks the /api/sessions POST body in IDB; flushQueue() drains it (never drops a brew on failure).
├── brew/                      # ★ Canonical brew timeline + live scale coach (PRs #328/#330/#331, June 2026)
│   ├── timeline.ts         # buildBrewTimeline(recipe) — the SINGLE "intended flow", a normalize layer over pourSequence.ts (pourStepsFromStructured / buildGuideSteps / parsePourSteps). Returns the renderer-ready pourSteps/guideSteps/proseSequence AND a normalized steps[] + expectedGramsAt() + activeStepAt() the flow coach builds on. hasGramsCurve flags percolation. Golden test: tests/dataflow/brew-timeline.test.mjs.
│   ├── flowCoach.ts        # coachFlow(timeline, elapsed, started, liveGrams, samples) → one teaching cue ("Steady"/"Ease off"/"Overshot +Xg") comparing the Acaia weight stream to the intended flow. Coaches ANY step with a cumulative-grams target — percolation AND immersion/AeroPress pour steps (#411 dropped the old percolation-only gate); steps with no grams target (steep/press/drain) return "none". #443: selects the active step via the TIME-based `activeStepAt` (matching the card); the `settledGrams` median + the `weightedActiveIdx` step-pick were removed. PR #471/#484: the caller (`LightStepBrew`) passes **tare-netted** liveGrams/samples (raw scale minus a baseline captured ONCE at brew start and then held FIXED — #484 dropped the live rebasing so a swirl can't shift the zero). **PR #489: the target pour rate is PER-RECIPE, not a global 4 g/s** — `intendedPourDurationSec(grams, authoredSec)` (pourSequence.ts) gives grams ÷ the pour's own authored time (Kasuya 6 g/s, Hoffmann ~10) when that reads as a real pour (≥2 g/s), else the ~4 g/s house fallback; "Slower" fires only above 1.5× the recipe's own rate (clamped 2–11 g/s). The corpus median is 5.2 g/s — 4 was never universal. Tests: tests/dataflow/brew-flowcoach.test.mjs.
│   └── flowAnalysis.ts     # ★ Post-brew pour analysis — analyzeFlow() (steadiness CV, overshoot, rejectNonPourJumps swirl-vs-mass-event handling) + formatMeasuredPour() (the measured-pour phrase in the coach + /recommend prompts). Tests: tests/dataflow/brew-flow-analysis.test.mjs
├── coffee/
│   ├── coffeeKey.ts        # ★ coffeeKeyFor(roaster, name) — THE shared derived coffees.id slug; sessions route + POST /api/coffees must both use it or one bag forks two rows (byte-parity test vs the original expression)
│   ├── blend.ts            # componentsOf()/describeBlend()/deriveIdentitySummary() — blend components (migration 0021) → scalar summaries + /recommend's BLEND note
│   └── roastDate.ts        # guardRoastYear() — roast-date current-year guard (PR #216)
├── coach/questionRepeat.ts # isRepeatQuestion() — deterministic no-repeat gate for /api/coach-question (tested)
├── chat/agentPrompt.ts     # ★ The home chat's AGENT_SYSTEM_PROMPT + TOOLS, extracted from explore-agent/route.ts (#545, byte-identical move) so the live measurement harness (scripts/chat-agent-sim.mjs) fires the REAL prompt — the same split /recommend has had since recommendPrompt.ts. Content tests read it here; wiring tests still assert the route imports it.
├── chat/agentContext.ts    # ★ The chat's four per-turn context builders + cleanChatRecipe + grinderFromConversation (#545, same extraction). recipeLibraryBlock() (the cached 135-recipe corpus block, grouped by brewer with the V60-imbalance disclaimer) is memoized here once per process.
├── chat/addCoffee.ts       # Pure payload mapping for the chat's add_coffee action → POST /api/coffees (unit-tested)
├── chat/stripEmoji.ts      # ★ Emoji stripper for the chat's SSE stream (2026-08-21). The brand bans emoji, the prompt said so, one shipped anyway — the chat was the only one of 7 AI surfaces with no output-side check. Matches whole surrogate PAIRS (removing half leaves a broken glyph) and holds back a trailing lone high surrogate across chunk boundaries. Deliberately emoji-only: a regex for exclamation marks would eat "3:30!". **Since 2026-08-23 it also passes arrows (U+2190–21FF) and ✓ through** — house typography the model legitimately writes ("Bloom → 108g", "= 600g ✓"); the original ranges (mirrored from loadingInsightLint) were deleting them from every chat reply, and the live measurement's "emoji" column was counting them as violations. Tests: tests/dataflow/chat-emoji-strip.test.mjs
├── recipe/validateRecipe.ts # ★ THE shared recipe validator (2026-08-21) — the checks /recommend's guards perform, expressed as QUESTIONS so the chat can use them. Checks the RENDERED timeline (buildBrewTimeline), never the authored steps: the reported dead gap was DERIVED, not written, so an authored-step check passes it. Catches pourability (>~11 g/s = not a pour), dead gaps (>75s), grind unit, disc offset, reference drift (by calling reconcileToReference, so the two surfaces can't disagree on "drifted") and vessel capacity. VALIDATES rather than repairs because the surfaces differ — /recommend corrects in place and the user only sees the result; the chat has already said the numbers out loud, so a silent rewrite splits message from timer (#198). Tests: tests/dataflow/validate-recipe.test.mjs (anchor = the real reported recipe)
├── taste/brewContextInsights.ts # ★ "What works for you" conditional findings (origin×process segments, ≥4.5★ vs ≤3.5★, silence over noise) + segmentLabel() + formatContextFindingsForGreeting() (the greeting's MEASURED CONTRAST block). **Reads what the user ACTUALLY did** (`brew.actualTempC` / `grindSettingUsed` first, recipe as fallback — until 2026-08-23 it read the recipe alone, making the whole section a study of the RECIPES, blind to every deviation at the grinder or kettle). **Water is a first-class separator** (~220 ppm daily vs ~73 ppm clarity blend), competing with the numeric dials for the same two clauses; needs a ≥40-point split with both waters genuinely used, and an unrecorded water is never guessed into the daily bucket. Tests: tests/dataflow/brew-context-insights.test.mjs
├── health/healthsyncPush.ts # One-way caffeine push bridge; ml only from a REAL figure (brew water → recipe water), otherwise caffeine_ml omitted (PR #481)
├── recommend/
│   ├── run.ts              # ★ runRecommendation() — the shared engine behind POST /api/recommend AND /recommend/start (server-side loadRecentSessions(400), insights read, calls generateRecommendation)
│   └── jobStore.ts         # In-memory background-job store for /recommend/start + /status (10-min TTL; single-user, one process — deliberate, not sloppy)
├── coldBrew/coldBrew.ts    # Cold-brew steep reminder engine — one iOS local notification at finish time, localStorage-persisted, resumable via flowStore.resumeColdBrew()
├── audio/listeningCue.ts   # The ascending "I'm listening" earcon (E5→B5) played when voice capture arms
├── craftingPhases.ts       # buildCraftingPhases(coffee, context) — the recipe-wait status walk over the bean's REAL stored factors (no fabrication). Tests: tests/dataflow/crafting-phases.test.mjs
├── heroQuestions.ts        # nextHeroQuestion() — rotating Hero question variants (localStorage)
├── native/                 # ★ iOS-shell bridges — ALL feature-detected, no-ops on the Safari PWA (see docs/ios-shell-roadmap.md): brewNotifications.ts (step boundaries + orphan sweep; lock-screen scheduling removed 2026-06-15), brewHaptics.ts (Taptic 3-2-1 + step buzz), brewWatch.ts (watch schedule handoff), liveActivity.ts + liveActivityPush.ts + liveActivitySchedule.ts (G3 Live-Activity APNs push backend), widgetBridge.ts + widgetDeepLinks.ts (G2 widgets + btts:// deep links), geo.ts (native geolocation — the web API silently never resolves in the remote-URL WKWebView), acaia/ (the ported Beanconqueror scale protocol — see its README; never reconstruct it from memory)
├── flow/
│   └── brewAgain.ts        # ★ Shared brew-flow entries — startBrewAgain() (online → Step "context") + startBrewAgainOffline() (seed cached recipe → Step "brew") + startBrewFromChat() (seed the chat's exact recipe as a 1-candidate recommendation → Step "brew"; backs the chat `start_brew` button, PR #199). Used by /coffees list, /coffees/[id], ActionPill.
├── scan/
│   ├── clarifications.ts   # ★ Deterministic post-scan follow-ups (PR #471) — buildClarifications() asks ONLY about fields the scan left empty (variety/notes/region/process/origin, never roast date/score); applyClarificationAnswer() maps an answer straight onto its field (no model call). Replaced the model's free-form questions + the Haiku /api/analyze-bag/clarify round-trip. Tests: tests/dataflow/scan-clarifications.test.mjs.
│   └── translateNotes.ts   # translateNotesToEnglish() (Haiku) — backs the retroactive /api/admin/translate-flavor-notes backfill (PR #473). Never invents/reorders/drops; returns the original array verbatim on any failure or length mismatch.
└── utils/
    ├── cn.ts / safeFetch.ts / formatTime.ts
    ├── pourSequence.ts     # ★ Pure pour timing. parsePourSteps() (tolerant of inline @temp/notes annotations) → percolation PourStep[]; pourStepsFromStructured() builds the same from a recipe's structured pourSteps AND derives per-pour agitation from adjacent stir/swirl steps; buildGuideSteps() + hasImmersionShape() drive the immersion StepGuide. PourStep carries temperatureC/notes/agitation + pourDurationSec (the recipe's own authored pour time, threaded to the flow coach). hasImmersionShape() (PR #488): a `wait` signals immersion ONLY when it ends in extraction (drain/press/flip/invert/bypass) with no more pouring after — a leading Bloom-Rest wait no longer misroutes a percolation brew. intendedPourDurationSec()/MIN_POUR_RATE_GPS (PR #489): the coach's per-recipe pour-rate source (grams ÷ authored time when ≥2 g/s, else the POUR_RATE_GPS=4 fallback).
    ├── pourSteps.ts        # ★ Shared pour-step sanitizer (PR #410). normalizeStepAction() (synonym → exact BrewStepAction, e.g. "Plunge"→press, "Steep"→wait) + sanitizePourSteps() (zod-validate + action-normalize + ≥2-step guard) + pourSequenceFromSteps() (legacy grams-string backstop). ONE source of truth used by BOTH /recommend (sanitizeRecipe) AND explore-agent (cleanChatRecipe), so a chat-authored recipe routes + renders exactly like a /recommend one. Tests: tests/dataflow/chat-recipe-sanitize.test.mjs.
    ├── resolveRecipe.ts    # ★ resolveBrewedRecipe(session) — single source of truth for "the recipe the user ACTUALLY brewed" (the selectedCandidateIdx candidate, NOT primaryRecipe). Used by chat history, timing stats, offline cache, brewSignature, brew detail so the primary-vs-selected bug class (PR #193, #198) can't recur per-call. brewedRecipeName() = title (+ "based on …").
    ├── vesselCapacity.ts   # ★ THE single source of truth for every brewer's brew-volume window — `VESSEL_CAPS` (owner-measured min/max, 2026-08-31): AeroPress ≤230 · Clever ≤450 · Origami ≤500 · Kalita ≤450 · Orea ≤450 · V60 ≤550 · Chemex 350–750 · Moccamaster 500–1000 · cold-brew jar ≤1000. Two guards read it: `vesselOverflow(method, waterGrams)` (recipe pours >max or <a batch min — post-gen candidate drop) and `vesselCannotServe(method, targetMl)` (the requested serve volume is above max OR below a batch min — the selector's hard filter, `helpers.ts`; renamed from `vesselTooSmallForTarget`, now BOTH directions). recommend.ts's HARD CAPACITY CONSTRAINT + the UI method chips + the /recommend prompt's HARD CAPACITY LIMITS all read/mirror these same numbers — they drifted before (UI "Clever 400ml" vs code 450). Tests: tests/dataflow/vessel-capacity.test.mjs.
    ├── grindUnit.ts        # ★ normalizeGrindToGrinder(grindSize, grinder) — /recommend backstop (PR #518). The flow's grinder picker sets context.grinder and the prompt says "NEVER Niche°" / "NEVER clicks" outright; it leaked anyway. Converts instead of flagging, because a wrong UNIT is worse than a wrong number (406 is not a setting a Comandante has). Map = the owner's measured anchors 380°=23 clicks / 400°=29 (~3.3°/click, grindSettings.ts), both pinned by test. The scales can't overlap by magnitude (Niche mid-300s–400s, clicks teens–forties), so a bare number is unambiguous. Right-unit values returned untouched incl. wording; unparseable ("medium-coarse") left alone. Tests: tests/dataflow/grind-unit.test.mjs.
    ├── brewMethodKey.ts    # ★ brewMethodKey(label) — brewer-family key for timing calibration + brewSignature ("Orea Classic" == "Orea V4 Classic"; the Drip Assist stays part of the key). Tests: tests/dataflow/brew-method-key.test.mjs
    ├── agitationGuard.ts   # stripMinimalAgitationSwirls() — drops swirl/stir/agitate-bed steps from minimal-agitation brewers in /recommend (PR #488). Tests: tests/dataflow/minimal-agitation-guard.test.mjs
    ├── dripAssist.ts       # ★ stripProactiveDripAssist(candidates, locked) — deterministic /recommend backstop (PR #458). Drops any "V60 + Drip Assist" candidate when the user did NOT lock it (relabels to plain V60 only if that would empty the list). The prompt already bans proactive Drip Assist, but Mistral leaks it where Opus didn't — this enforces the rule in code. No-op when locked. Tests: tests/dataflow/drip-assist-guard.test.mjs.
    └── pourSequence.test.mjs  # node --test suite — pour math + tolerant parse + structured/guide + agitation + resolveBrewedRecipe
```

### Other key files

| File | Purpose |
|------|---------|
| `src/store/flowStore.ts` | ★ Zustand brew flow state (**localStorage**-persisted since the offline work — survives a mid-brew reload; "New Session" in `NavigationOverlay` calls `reset()` so it never resumes a stale draft) |
| `src/hooks/useOnline.ts` | Connectivity boolean (seeds `true` for SSR, then `navigator.onLine` + online/offline events). Used by the `/coffees` pages for the offline read path. |
| `src/hooks/useWakeLock.ts` | Keep screen on during active brew — iOS shell via the app-local `ScreenAwake` plugin (`window.Capacitor.Plugins.ScreenAwake`, #443), Web Wake Lock API fallback elsewhere; re-asserts on visibility + every 15 s |
| `src/hooks/useVoiceCapture.ts` | Mic recording + level metering for inline-chat voice input (BTTS Home). Plays the "I'm listening" earcon (`src/lib/audio/listeningCue.ts` — ascending E5→B5, distinct from the brew timer's descending step cue) the instant the mic arms. An `autoListenSignal` prop on `ChatInput` arms it from the Siri / Action-Button `btts://voice` deep link |
| `src/hooks/useVoicePlayback.ts` | Streaming TTS playback for inline-chat voice output (BTTS Home) |
| `src/hooks/useAcaiaScale.ts` | Acaia BLE scale lifecycle (scan/connect/stream via `src/lib/native/acaia/`) — native shell only, powers `ScalePanel` + the live flow coach |
| `src/hooks/useBrewStepHaptics.ts` | Taptic 3-2-1 countdown + strong buzz at each brew step (native shell) |
| `src/hooks/useBrewLiveActivity.ts` | Starts/updates/ends the iOS Live Activity + registers its push token with `/api/live-activity/start` |
| `src/hooks/useFieldMotion.ts` | ★ Living-Field motion driver — one rAF loop writes `--field-*`/`--ptr-*` CSS vars (pointer lean, scroll parallax, tap swell, finger bloom) on the Field root; layers read them, zero React re-render. Reduced-motion-gated. See docs/liquid-design.md |
| `src/hooks/usePresence.ts` | Generic delayed-unmount `(present, exitMs) → {mounted, state}` — keeps a node mounted through its exit animation (haiku dissolve). Replaces framer-motion AnimatePresence |
| `src/middleware.ts` | ★ **The cookie gate for the whole app, `/api/*` included** — don't read a route's own file and conclude it's public. Its matcher covers every path except `_next/static`, `_next/image`, `favicon.ico`, `sw.js`, `manifest.json`, `icons`, `screenshots`; `STATIC_PATHS` waves those assets through (via the bare `/_next` prefix, so ALL of `/_next/*`), and `PUBLIC_PATHS` exempts exactly five prefixes — `/login`, `/api/auth`, `/api/research`, `/api/admin`, `/api/loading-insights`. Everything else needs a `cf_session` cookie that `jwtVerify`s against `AUTH_SECRET`, or it gets a **redirect to `/login`** (a 307, not a 401 — an API caller sees the login page, which is worth knowing when debugging a "why did my fetch return HTML"). All five exempt prefixes enforce their own auth: `research` + every `admin/*` + `loading-insights/refresh` on `CRON_SECRET`, the `loading-insights` GET on `requireAuth`. So **18 of 50 API routes call `requireAuth`; the other 32 are cookie-gated here, not open** (the 18th is `auth/register`'s conditional re-enrol check from #525; the count dropped 51→50 when `webhooks/coffee-alert` was deleted 2026-08-18). (Verified Aug 2026 while correcting a wrong in-session claim that four now-deleted GET routes had been unauthenticated.) |
| `.claude/hooks/session-start.sh` | Web Claude Code session bootstrap — runs `npm install` so tools work on cold start (gated on `$CLAUDE_CODE_REMOTE`) |
| `scripts/seed-insights.mjs` | Populate knowledge base (run once on new installs) |
| `scripts/migrate-firestore-to-postgres.mjs` | One-time Firebase → Postgres migration |
| `scripts/migrate-storage-to-s3.mjs` | One-time local storage → S3 migration |
| `scripts/rebuild-coffees-table.mjs` | Recompute coffee aggregates |
| `scripts/geocode-places.mjs` | Geocode places.address via Nominatim (OSM); ~2 hrs for 6k+ rows due to 1 req/s rate limit |
| `scripts/backfill-field-zones.mjs` | One-shot — call Haiku Messages API over plain `fetch` to map `coffees.tastingNotes` → `field_zones`. Ran on prod 2026-05 (23/23). Uses raw `fetch` because the Next.js standalone Docker image does not expose `@anthropic-ai/sdk` in `node_modules`. |
| `docker-compose.yml` | 4-service stack: postgres, app, caddy, ofelia |
| `.dockerignore` | Excludes `native/`, `node_modules`, `.next`, `.env*` from the Next.js build context. |

### Database tables (Drizzle + Postgres)

`sessions`, `coffees`, `auth_credentials`, `auth_challenges`, `preferences`, `roasters`, `knowledge`, `coffee_alerts`, `places`, `conversations`, `conversation_messages`, `cafe_visits`, `insights`, `drip_bags`, `loading_insights` (15 tables; `lessons` from PR #210 also exists but is read-by-nothing post-#211 — its `/lessons` page, `/api/lessons` route, and `src/lib/claude/lessons.ts` distiller were all removed, only the table + migrations 0012/0013_add_lesson_questions remain as dead schema).

Recent additions:
- `coffees.field_zones jsonb` (migration 0008) — persisted Field composition per coffee
- `coffees.in_rotation boolean NOT NULL DEFAULT false` (migration 0009) — star toggle for "currently brewing this bag"
- **Migration 0010** (applied 2026-05) — bulk-rename roaster variants `"Rösterei Vier / The Commonage"` / `"RVTC – Rösterei Vier / The Commonage"` → `"RVTC"` across coffees + sessions JSONB + roasters priors cache.
- **Migration 0011 + new `cafe_visits` table** (applied 2026-05) — schema: `id`, `cafe_name`, `location`, `rating` ('come-back' | 'wont-return'), `notes`, `visited_at`, `visited_at_ms`. Visit-only café logs without an attached brew session. Binary thumbs rating since there's no brew context for stars. Aggregated into `/api/cafes` so visit-only places appear in the Café Library.
- **Migration 0013 + new `insights` table** (applied 2026-06) — schema: `id`, `observation`, `suggestion`, `citation_fields jsonb`, `latest_session_ms`, `source` ('opus' | 'user-confirmed'), `status` (added by 0014), `dismissed_at` (legacy, replaced by status), `user_note`, `created_at`, `updated_at`. Multivariate coach observations over the full session corpus. (PR #211)
- **Migration 0014 — `insights.status text`** (applied 2026-06-02) — workflow state machine `new`/`trying`/`confirmed`/`doesnt-apply`. Indexed. Default `'new'`. Orchestrator (`src/lib/claude/insights.ts`) only replaces `status='new'` rows on regeneration; user-acted rows (trying/confirmed/doesnt-apply) are preserved, and re-emitted similar observations inherit the existing status. `/recommend` + `/greeting` filter `status != 'doesnt-apply'` AND hide actively-snoozed rows (an expired snooze flows back in). (PR #215)
- **Migration 0015 — `coffees.coach_insight jsonb`** — per-coffee Opus insight (`{observation, suggestion, status, generatedAtSessionMs, generatedAt}`, type `CoffeeCoachInsight` in `schema.ts`). Backs `CoffeeCoachCard` on `/coffees/[id]`. Regenerates when the coffee gets a newer session, EXCEPT while status is `trying`/`confirmed` (don't move the card under the user). Replaced the prior library-wide citationFields-overlap matching that surfaced other bags' insights on the wrong coffee.
- **Migration 0016 + new `drip_bags` table** — single-serve drip-bag documentation records (id, roaster, name, origin/region/variety/process/roast_level, bag_notes, flavor_notes, rating, free_notes, bag_photo_*, field_zones, ai_extracted, created_at(_ms)). Fixed brew (200 ml through the built-in filter) → no recipe, no timer. **Deliberately isolated** from sessions/coffees/the AI corpus (mirrors `cafe_visits`) so drip bags never skew `/recommend`, `/insights`, `/taste`, or the Café Library. Surfaced only in the Coffee Library (flagged) + their own `/coffees/drip/[id]` detail. Type `DripBag` in `src/lib/types/dripBag.ts`; routes `/api/drip-bags` (+ `[id]`).
- **Migration 0017 — `insights.snoozed_until` + `'snoozed'` status** — the coach card became a **two-stage** workflow: New (`new`) → Save to try / Confirmed / Doesn't apply, then Saved (`trying`) → It helped / Didn't help / Skip. **Skip** = remind-me-later → `status='snoozed'`, `snoozed_until = now()+7 days`; hidden until it passes, then resurfaces and regen treats it like `new`. The status CHECK constraint was rebuilt to allow the new value.
- **One-shot data fix** (applied 2026-06-02) — Friedhats Quiquira + Policarpo Yossa Rojos roast date bumped from `2025-05-18` → `2026-05-18` in both `coffees.latest_roast_date` and `sessions.coffee` JSONB. Bag-scanner had defaulted to last year on ambiguous month/day stamps; PR #216 closes the loop in code so it can't recur.

- **Migration 0023 — `coffees.variety` + `coffees.region` + `coffees.roast_level`** (applied 2026-08-09). Until then these three lived ONLY inside a session's `coffee` JSONB, so **a coffee could not describe itself without a brew attached** — which is why `/coffees` "Brew this" hardcoded `roastLevel: "Light"`, why `coffeeInsight.ts` had to dig variety out of the latest session, and why `getVarietyPriorsForBag()` got `undefined` for every brew started from the library. Backfilled from existing sessions taking, **per column, the most recent session that actually CARRIES that field** — not simply the newest, because a Brew-Again save carries no scan identity and would blank out a variety the original scan established. Measured on prod: **55 coffee rows → 45 variety / 48 region / 49 roast_level**. Writers use an only-when-carried policy (same as `bagFlavors`), so a note-less save can never wipe them. Readers prefer the session identity and fall back to the row.

All migrations applied manually on the VPS — see migration NOTE above.

### Key dependencies

| Package | Version | Role |
|---------|---------|------|
| `next` | 14.2.35 | Framework |
| `@anthropic-ai/sdk` | 0.80.0 | Claude API |
| `drizzle-orm` | 0.36.0 | ORM |
| `pg` | 8.13.0 | Postgres driver |
| `zustand` | 5.0.12 | State management |
| `zod` | 4.3.6 | Schema validation |
| `@simplewebauthn/server` | 13.3.0 | Passkey auth |
| `jose` | 6.2.2 | JWT |
| `leaflet` | 1.9.4 | Maps |
| `@aws-sdk/client-s3` | 3.700.0 | S3 uploads |
| `@ducanh2912/next-pwa` | 10.2.9 | PWA / service worker |

---

## Current Status — Snapshot August 2026 (newest entries first)

### ✅ Done

**Recipe diversity round N+1: the "Orea geht unter" was CONTENT-STARVATION, not a cap — plus vessel caps unified + the everyday amount is now 450 (2026-08-31, PR pending)**
- **The owner was furious the same recipes keep coming (Hoffmann Clever water-first "aus den Ohren", Orea "komplett unterrepräsentiert"). Measured it instead of guessing.** Ran the recipe-per-brewer count and the `recommend-variety-sim.mjs` baseline: over **40 brews (2 coffees × 20), Orea appeared 0 times.** Cause is not the selector or the caps — **the corpus holds exactly ONE Orea recipe** (`orea-v4-fast`), and none for Apex/Classic/Open. You cannot select what does not exist. Clever (13 recipes) is in nearly every menu; scenario 2's lead was identical 20× in a row. So the complaint is real and datenbelegt, and the true cause was different from every prior round's assumption.
- **Orea fix, part 1 — 10 REAL recipes added (the content fix), Orea 1→11.** `src/lib/knowledge/recipes/oreaWide.ts` — real, named, sourced OREA V4 Wide recipes (owner-commissioned research; orea.uk guides + named authors) with full pour sequences, one primary bottom each so all four are covered: **Apex** SO Soft · **Classic** OG Base #2, Easy Does It · **Open** The Dara, Dara for Two, The Bypass, The Techno · **Fast** The Four Six, The Fine, The Aussie. `verified:false` — NOT a blocker: fully selectable AND it stops `reconcileToReference` snapping their numbers. Grind is the real Comandante clicks in `referenceSetting` + Niche DERIVED via grindUnit's anchored map (23cl=380°, ~3.3°/click) — **no fabricated Niche** (OREA publishes none anywhere; the Hard Rule's one real bite here, handled by converting not inventing). The 4:6 entry is OREA's Orea-Fast adaptation, cross-referenced to the corpus's V60 `kasuya-4-6-standard`, not a duplicate. Owner rejected the `verified` gate as a blocker outright.
- **Orea fix, part 2 — prompt adaptation (belt + braces).** The `/recommend` prompt's OREA V4 block gained item 4: the Orea is under-DOCUMENTED, not unsuitable; when the fit calls for a bottom the library still lacks, ADAPT the best-fit pour-over reference onto it and cite it in `basedOn` (goal→bottom: clarity/aromatic→Apex, balanced/sweetness→Classic, body/fine-grind→Open, fast turbulent naturals→Fast). With 10 real recipes now in the menu this is a fallback, not the whole fix.
- **Measured:** the selector now surfaces Orea — with v60/clever demoted, the menu leads `kalita · origami · orea-apex · orea-classic` (all score 6) where a score-5 filler used to sit. `method-rotation.test.mjs`'s over-strict "identical score profile" invariant was corrected to the real property ("demotion never REGRESSES a rank's score") — it only held before because the corpus had no competitive Orea. **Still live-only:** the model's actual pick rate of Orea in real candidates (a `--live` run / real brews).
- **Owner rejected family-grouping:** the 4 Orea bottoms (on the V4 **Wide** body — "Wide" is the MODEL, not a 5th bottom) and Origami cone/wave stay SEPARATE brewers, because each delivers a genuinely different brew. Clever left as-is for now (dilutes once Orea/Origami actually compete; re-measure before nerfing).
- **Vessel caps unified into ONE source (`VESSEL_CAPS` in `vesselCapacity.ts`), owner-measured min/max:** AeroPress ≤230 · Clever ≤450 · Origami ≤500 · Kalita ≤450 · Orea ≤450 · V60 ≤550 · Chemex 350–750 · Moccamaster 500–1000 · cold-brew jar ≤1000. The UI method chips, recommend.ts's HARD CAPACITY CONSTRAINT (now DERIVED from `VESSEL_CAPS`, not hardcoded) and the prompt's HARD CAPACITY LIMITS all read/mirror these — they had drifted (UI "Clever 400ml" vs code 450; UI "Orea/Kalita ~500" vs code unbounded). `vesselTooSmallForTarget` → `vesselCannotServe` (now flags BOTH a too-big target and a below-min batch target).
- **Brew-amount presets: `Small 350 / Big 450 / Custom / Surprise` (was Big=520).** 450 is the owner's most-brewed amount, and 520 was silently excluding too much: at 520 the serve-volume filter hard-drops Clever (450), Origami (500 in old code 450) AND AeroPress — at 450 the whole pour-over kit (V60/Orea/Kalita/Clever/Origami/Chemex) stays in and only AeroPress + Moccamaster correctly drop out. This alone brings Origami back.
- **Verified:** `tsc` clean, 446 dataflow + 50 src tests, corpus 0/0. `vessel-capacity.test.mjs` rewritten for the new min/max caps + rename + a "450 keeps the pour-over kit" pin. NOT verified (unreachable from a session): the Orea-adaptation effect on real recipes (live-only) and the on-device feel.

**The Apple Watch app is DEAD — I never got its background haptics working, and it drained the battery (2026-08-29, PR #567 — retired; do NOT rebuild)**
- **Honest verdict: this is a feature I failed to ship.** The whole point of the watch app was a wrist buzz at each brew step while the phone screen is on (the wake-locked case the phone's own Taptic + notifications can't cover). Across **twelve+ TestFlight builds (8 → 20)** I never made watchOS deliver that reliably in the background, and the final attempt made it worse — so the owner and I killed the entire app. It is not paused, not "revisit later" — it is removed. If the wrist buzz is ever wanted again it is a **fresh design problem** (a real always-on companion workout app), not a tweak to resurrect.
- **Why it never worked (the actual physics I kept fighting):** watchOS does not let a normal app fire haptics in the background. Builds 8–19 kept a bare `HKWorkoutSession` alive, which grants *some* background time but gets suspended a minute or two in — so the buzz died after ~the 2nd pour every time the wrist dropped. Every workaround I tried to keep the session alive either **didn't buzz** (physical-therapy extended-runtime session; scheduled local notifications, which watchOS batches ~15–18 s late; APNs push, same latency) or **cost the battery** (build 20's live `HKLiveWorkoutBuilder` collecting HR + active energy continuously to stay alive — drained the watch nearly flat in a few hours AND still stopped buzzing after the 2nd pour). Build 8 briefly *seemed* to work and the owner even signed it off — but it never survived a full wrist-down brew in real use. **watchOS background step-haptics are not achievable for this app's shape.**
- **The most expensive lesson banked (it cost ~7 builds, 12→19):** build 8 was the closest-to-working version and I **replaced its working core** (HKWorkoutSession + `WKInterfaceDevice.play`) to chase peripheral complaints (sync/pre-warning), regressing the one thing that worked, then spent builds chasing my own regression. **Never swap out a confirmed-working mechanism to fix a peripheral complaint — fix the peripheral thing on top of the working core.**
- **What was removed (#567):** web — `useBrewStepWatch` + `src/lib/native/brewWatch.ts` deleted, their calls pulled from `LightStepBrew.tsx`, so the phone no longer hands the watch a schedule and **the battery drain stops on the next web deploy** (no TestFlight needed — the phone was the trigger). Native (next shell build) — the `BTTSWatch` target, its sources, `BrewWatchPlugin.swift` + its `MainViewController` registration all gone; the CI embed-assertion flipped to assert NO watch app is embedded. **Load-bearing subtlety kept:** `native/scripts/add_watch_target.rb` was NOT deleted — it also wires the App-target plugins ScreenAwake + WidgetBridge + LiveActivity + MainViewController that the shell still needs — it was **gutted to remove any watch target** and wire only `[ScreenAwakePlugin, MainViewController]`, keeping its filename because CI + `mac-build-upload.sh` call it by name.
- **What replaces it (both reliable):** the **phone Taptic** (3-2-1 countdown + strong step buzz, `useBrewStepHaptics`) and the **Live Activity** (lock screen / Dynamic Island). Those stay.
- **Owner instant relief:** delete BTTS from the Apple Watch (iPhone Watch app → BTTS → uninstall); otherwise the next deploy already stops the drain and the next TestFlight ships a watch-free shell.
- **Doc note:** the full build-by-build history + the retirement decision live in `docs/ios-shell-roadmap.md` (G4 Tier 2 entry, marked RETIRED, + the 2026-08-29 session-log entries). Stale CLAUDE.md references to the watch hook were removed in this same edit.

**The corpus is selection-biased, and nothing in the app knew it (2026-08-24, PR pending)**
- **The trigger was a Coffee Rose research thread** (Café Imports' patent-pending CATA cupping wheel, `WO2025129065A1`). Conclusion on copying it: the *scoring engine* is the least interesting part — its coefficients are trade secret (the published application shows the architecture, one worked example scoring 82.28, and no weights), and it is **user-INDEPENDENT by design** because they have many cuppers who must agree. Its author says outright he would rather talk about profiles than scores, and he keeps a separate "smile-o-meter" for *do I like it* — which is exactly what the 1–5 stars already are. **Stars stay.** They are also the label that makes descriptors learnable at all; replacing them with a derived score deletes the only independent ground truth. A 100-point scale would also make the real problem worse, not better: the ratings are tightly clustered near 4★ (see `brewContextInsights.ts`), so more decimals turn scatter into fake signal. What is worth taking from the Rose is the *input* structure (per-descriptor intensity, connotation), not the output number — **not built, deliberately deferred**; see the open item.
- **The actual find came from the owner, not the research.** Asked whether his flavour notes were in a vocabulary rut, he pointed out they cluster because *he buys the coffees he likes* — "ich mag keine Ananas-Kaffees, zumindest nicht für zuhause". That is **selection bias**: a flavour he avoids never enters the log as a negative example, so every "what works for you" finding, coach insight and `/recommend` prior is computed over a set he pre-filtered for liking. The app can describe what works *inside* his taste and structurally cannot find its edges. **At n=1 the fix is not machinery, it is a sentence** — you do not infer a preference from data when you can ask the one user. The whole Coffee Rose scoring apparatus collapses, for this project, into three lines in `CANONICAL_PROFILE`.
- **Shipped:** the dislike (his words only — pineapple, at home, soft), its **SELECTION-not-brewing** scope (a bag he already owns still gets brewed as well as it can be; `/recommend` must not try to suppress the fruit), and the caveat that makes the rest honest — **absence of a flavour in the log is NOT evidence of a dislike.** Deliberately its own line rather than an append to `Avoids:`, which filters PROCESSING and roast: a washed or honey coffee can be loudly pineapple and pass that filter untouched.
- **Correction banked (claimed wrong in-session before checking):** the profile block does **not** reach `/recommend`. `formatProfileForPrompt` has exactly one production consumer, the chat; `/recommend` sees taste only as `body=`/`acidity=` from the DB row and `/greeting` reads `profile.equipment` alone. That still makes the chat the right target — this steers *which bag*, and `/recommend` writes recipes for a bag already chosen. **Lesson repeated: grep the consumer before saying a block "goes to" a surface.**
- **A bad test caught before it shipped:** the first proposed rut-check was "look at Top flavors on /taste". It cannot work — clustering from *describing* and clustering from *buying* have the same signature there. The discriminating measurement is *within* one bag across many brews (do the notes move with the recipe), visible on `/coffees/[id]`. Curiosity, not a work item.
- **Verified:** `tsc` clean, 50 src + **437** dataflow tests, corpus validation 0/0. The new suite (`tests/dataflow/taste-preference-reaches-chat.test.mjs`) covers content AND route wiring and was **proven to bite before commit** — stripping the profile lines fails 5 of 7, cutting the route's `text: profileBlock` fails the 7th.
- **Open, not built:** per-descriptor **intensity + connotation** on the flavour wheel. Argument for: `flavorNotes: string[]` is binary, so the coach reads `[citrus]` with no strength and no sign, and cannot tell which of three notes was the problem. Arguments against, honestly weighed: it partly duplicates the sensory axes already logged (`body`/`acidity`/`sweetness`/`bitterness`/`astringency`, live since #563), it triples taps in `LightStepLog` at the worst moment, and the statistical payoff is months out because only NEW brews carry it. The Rose's own version is stronger than a naive slider — intensity is *relative to an 80-point reference coffee* rather than absolute — and its connotation is fixed in the LEXICON, not per taster (their reasons for that: many cuppers, Monday must equal Tuesday; none apply at n=1). If it is ever built: connotation first, per tasting, one bit.

**The coach never saw a single taste reading; and the taste analysis was studying the recipes, not the user (2026-08-23, PRs #562 + #563 — owner picked these three off the review's leftovers)**
- **THE find (#563) — `tasteBits` was assembled and joined into NOTHING.** `serialiseSessionForCoach` (`insights.ts`) builds a per-session sensory string — `body=` `acid=` `sweet=` `bitter=` `finish=` `clarity=` — and never put it in the line's `parts` array. **`git log -S 'tasteBits.join'` returns no commit: dead since the formatter was written.** So the surface whose entire job is reasoning across taste axes has been inferring taste from the flavour-note list and the star rating alone, on every session, forever — while the sensory fields sat one line above, fully populated. Same class as the sessions-schema strip found this morning: collected carefully, prepared carefully, delivered nowhere. Found only because wiring astringency put me on that exact line. Before/after, same session: `… m=V60 15g/250g … · [citrus]` → `… m=V60 15g/250g … · body=medium acid=high bitter=harsh clarity=clean astringent=notable · [citrus]`.
- **#563 also closes astringency itself.** It is the clearest OVER-EXTRACTION marker the log collects — the dry, puckering finish of a grind too fine, water too hot, or contact too long — and `bitter=harsh` alone does not separate that from a dark-roast bitterness no brew can fix, which is exactly the ambiguity `/api/coach-question` exists to resolve. Added to BOTH readers: the coach line, and `buildSensoryPatterns` (`historyUtils.ts`), whose filter also *required* clarity/sweetness/bitterness — so a tasting where astringency was the only thing recorded contributed nothing at all. **No prompt text changed:** the coach prompt carries no token legend, and `astringent=notable` is at least as self-explanatory as the `fit=` it already reads — so this is a wiring fix, not an AI-behaviour change.
- **#562 — "What works for you" was a study of the RECIPES.** `dialsOf` took temperature and grind from the recipe, never from `brew.actualTempC` / `grindSettingUsed`. Every deviation was invisible: the grind nudged two degrees coarser at the grinder, the kettle landing at 96 instead of 94 — and a segment where the owner consistently corrected the same way looked like it had no separation. Now actual-first with the recipe as fallback; both fields are pre-filled from the recipe and editable, so a followed-to-the-letter brew produces a byte-identical sentence (pinned) and they diverge exactly when the user did something. Dose/water follow the same rule, so a café brew with no recipe attached can contribute its ratio.
- **#562 — water finally competes.** ~220 ppm BWT daily vs the ~73 ppm clarity blend is one of the strongest levers in this setup, recorded on every brew and analysed nowhere. It is now a first-class separator ranked against the numeric dials for the same two clauses (so the sentence stays readable). **Deliberately different statistics:** water is a CHOICE, not a reading, so it cannot use `MIN_DELTA`'s scatter logic — the test is how often the clarity blend appears among the clear hits versus the clear misses. `MIN_WATER_SPLIT` = 40 percentage points (with 4–6 brews a side, less is one or two brews); both waters must genuinely appear in the segment (`MIN_WATER_USES` = 2), or a user with one habit gets told his habit is the reason; and **an unrecorded water is never guessed into the daily bucket** the way `brewSignature` does — guessing there manufactures a 100-vs-0 split out of silence. The greeting's MEASURED CONTRAST block prints it as exact percentages, consistent with its no-paraphrase rule.
- **Every new test counter-checked before committing** (the producer-only lesson, now habitual): reverting the actual-values read fails 2, disabling the water block fails 3, removing the `tasteBits` join fails 3, removing astringency from both readers fails 4. `tsc` clean, 50 src + 430 dataflow tests, corpus validation 0/0.
- **Test-infrastructure note worth keeping:** `insights.ts` reaches the DB client through its module graph, so bundling it for a test needs `external: ["pg", "pg-native", "drizzle-orm", "drizzle-orm/*"]` AND the bundle written inside the project (`node_modules/.cache/…`) so Node can still resolve those at runtime. A `/tmp` bundle cannot. First test here to need it; reach for this pattern the next time a module with a DB import needs covering.
- **Owner explicitly descoped** the 7% Drip-Assist drawdown estimate ("geht mir am Arsch vorbei") — it stays an estimate, and one measured disc brew (timer-promised vs last drip) is still all it would take.

**The chat's pour rule was aimed at an output the model never writes; and the grind stopped being an estimate (2026-08-23, PRs #559 + #560 — owner Go on both)**
- **#559 — why "no dead air" could not land.** Run 1's measurement (see the entry above) left one failure mode standing: `dead-gap`, and only on the road (travel-no-gooseneck 450ml **1/5** first-try, narrowed-to-orea **2/5**, vs home 4/5). The prompt already said "no dead air … spread the water across the clock" — but **the gaps are DERIVED, not authored**: `buildPourOver` reserves the drawdown and spaces the remaining pours in proportion to their size, so the rule asked the model to reason about an output it never writes. What it actually controls is HOW MANY pours share the clock.
- **The floors were computed, not guessed** — the real validator over the real timeline, across water × time × pour-count (350/450/600 g, 3:00–5:30, 3–7 water steps). With evenly split pours **bloom+2 trips `dead-gap` from 4:00 at 350 g, 4:30 at 450 g and 5:00 at 600 g, while bloom+3 and up never trips in that range**. So the prompt now carries a three-row table — 3 water steps up to 3:30, **4** up to 5:00, **5** beyond — stated as FLOORS with one more always safe, so it can't be read as a target that lands on the edge. Second half of the same failure: **padding the clock** for a bigger batch. More water means more pours in roughly the same time, not more minutes (and a longer contact time over-extracts — the opposite of what a big batch needs). The prompt-content test asserts the rule's SUBSTANCE (the table, "FLOORS, not targets", the clock-padding clause), not the old wording.
- **A measurement bug found while reading my own report:** the sim's "Validator findings across all runs" line tallied `r.problems`, which is the **FINAL** round's verdict — so a repaired run contributed its post-repair state and **first-try failures were never reported at all** (run 1's `{"dead-gap": 2}` was the 2/20 that survived repair, not what the model wrote first). The report now separates first-try from post-repair AND prints the **shape** of every first-try failure — water-step count, cumulative grams, authored pour seconds — next to the validator's own message, so the next prompt fix targets what the model wrote instead of what we assume it wrote. **MEASURED, run 3 (2026-08-23, run 32655003905) — the road cases roughly doubled and nothing broken reached the timer:**

  | scenario | run 1 first-try | run 3 first-try |
  |---|---|---|
  | travel-no-gooseneck 450ml | 1/5 | **3/5** |
  | narrowed-to-orea 450ml | 2/5 | **4/5** |
  | home-standard 350ml | 4/5 | 4/5 |
  | big-batch 600ml | 3/5 | 3/5 |

  **Still failing after the repair round: 0** (run 1: 2). **Emoji: 0/20** (run 1: 12/20 — that was #557's arrows, now correctly not counted). Excluded brewer re-offered 0; disc used 5/5 in both gooseneck-less scenarios. All six remaining first-try failures are `dead-gap`, and the new shape diagnostics named the cause in one read: **the floors were derived on the BARE brewer and applied to disc recipes.** With the Drip Assist `drawdownReserveFrac` is **0.07, not 0.33**, so the pour phase is ~40% longer and the same pour count leaves a much bigger hole — all three gooseneck-less failures had dutifully followed the bare-brewer count (4 steps at 450 g / 4:30, which the disc needs 5 for). The two big-batch failures were the plainer kind: 3 water steps at 600 g, below the floor. Re-derived across both methods (350/450/600 g × 3:30–6:40 × 3–8 steps) and corrected the same day:

  | total time | bare brewer | with the Drip Assist |
  |---|---|---|
  | up to 5:00 | 4 (bloom + 3) | **5** (bloom + 4) |
  | over 5:00 | 5 (bloom + 4) | **6** (bloom + 5) |

  Plus a "land ON the floor and a brisk pour tips you over it, prefer one above" line — the live failures included a 450 g/4:30 disc recipe that authored 25 s pours where the floor assumed ~30 s, which is exactly how a marginal recipe crosses the line. **Lesson worth keeping: a threshold derived under one configuration is not a rule until it is re-derived under the others** — I shipped bare-brewer arithmetic into a prompt whose hardest scenarios are all disc scenarios.
- **#560 — the grind was an estimate while the real thing sat in the DB.** Every per-method row in `grindSettings.ts` **except the V60** is marked `confidence: "estimate"` (re-based from an older baseline, never measured), while `brew.grindSettingUsed` has recorded the setting actually ground at on every brew since the flow shipped, next to the rating the cup earned. Timing already learned this way (`measuredTimeDelta`); grind did not — so the model kept being handed an estimate for the exact brewer the owner owns and has brewed on dozens of times. New `src/lib/claude/measuredGrind.ts` pools the owner's OWN sessions by `brewMethodKey` (Orea Classic ≡ Orea V4 Classic; the Drip Assist stays its own bucket — the disc changes flow) and buckets by volume on the timing calibration's tolerance (±20% / ±60 g), because grind scales with dose. Reports median, range, and **separately the ≥4★ window** (the actual preference signal); ≥3 samples per bucket, max 4 brewers, narrowed to one when the flow locks a method, rendered in the grinder's own unit.
- **REPORTED, never enforced — deliberately unlike the timing calibration.** Grind is bean-dependent: a 6-week-old natural and a fresh washed Kenyan legitimately want different settings on the same brewer at the same volume, so forcing a number would flatten exactly the variation a recipe exists to express — the mistake the fidelity guard made before it went per-field. The block says outright it is "a centre of gravity, not a target". No invented slope (Hard Rule): every number is a median or an extreme of the owner's own logged settings; nothing measured → empty block → the general table applies exactly as before. `docs/grind-settings.md` now says which of the two wins where.
- **Test discipline held both times:** the grind tests cover the function AND the route interpolation, and the wiring assertion was **proven to fail with the block unwired** before being committed — the producer-only gap that shipped #530 and #535. Verified throughout: `tsc` clean, 50 src + 415 dataflow tests, corpus validation 0/0.
- **Honest limits:** the floors are derived for EVENLY split pours — a deliberately lopsided plan can still stall, which is why the validator stays the net and the floors are a floor. And #560 reads `grindSettingUsed`, which is PRE-FILLED from the recipe and editable, so a followed-recipe brew logs the recipe's grind: the block says "what you ground at", which is the truth, not "what you independently chose".

**Full-app review: every saved brew was silently dropping its scale measurement — the sessions schema lagged the type by three fields (2026-08-23, PRs #556–#560)**
- **The occasion:** owner asked for a ground-truth review ("Sind wir richtig gut aufgestellt? Ist alles physikalisch korrekt? Wird die App iterativ schlauer?"). Four parallel verification agents audited physics/scaling math, the learning loop, the chat guards, and corpus/prompt/doc truth; every finding was re-verified in the main session before acting. Baseline at start: `tsc` clean, all 429 tests green, corpus validation 0/0.
- **THE find — `POST /api/sessions` stripped `brew.selectedCandidateIdx`, `brew.flowAnalysis`, `brew.flowSource` on every save.** The zod schema listed 13 brew fields; the `BrewLog` type has 16, and zod removes unknown nested keys SILENTLY (reproduced in-session against the installed zod 4.3.6). The identical bug was found and fixed once before for `result.*` — the comment documenting that incident sits directly below the brew schema; `brew.*` never got the same fix (git history: the three fields were never in the schema). Consequences until now: (a) the entire Acaia pour analysis was display-once — shown on the Summary card from the in-memory draft, then thrown away, so the coach's `pour[…]` token and the MEASURED BREW FEEDBACK pour half read `undefined` on every production session (the #530/#535 wiring was real but starved at the source); (b) `resolveBrewedRecipe` permanently ran its legacy method-name fallback for all 14 consumers — which picks the WRONG candidate's numbers exactly on locked-method brews, the #193 bug class the helper exists to prevent. **Fixed:** the three fields added (flowAnalysis deliberately as `z.custom<FlowAnalysis>` passthrough — a structured copy would re-create the strip one level down), plus **compile-time parity guards** for brew/result/coffee: `tsc` now fails, naming the missing keys, whenever a Session sub-type gains a field the schema doesn't know. Proven the guard bites: added before the fields, tsc failed naming exactly the three. **Unrecoverable honesty note: all sessions saved before this fix permanently lack these fields** — the data never reached the DB; measured-pour personalization starts accumulating from the first post-deploy scale brew.
- **Physics/math audit verdict: no bug in the formulas.** Pour spacing, reserves, per-recipe coach rates, fidelity per-field snap, batch grind guard, unit conversion (anchors round-trip exact), vessel caps, timing calibration — all correct against the measured anchors. One real (low-severity) defect found + fixed: `analyzeFlow`'s per-pour `intendedPourSec` still used the house 4 g/s while `expectedGramsAt` and the live coach honour the recipe's authored pour time since #489 — a Kasuya 60g/10s pour read as a 15s pour, under-reporting drift. Now shares `intendedPourDurationSec` (test added).
- **Test-gap closures (the producer-only failure class, again):** `calibrateTargetTimes` — the function APPLYING the measured timing delta — had zero tests (only its producer `measuredTimeDelta` was pinned); now exported + 6 consumer tests (`time-calibration-apply.test.mjs`). And `cleanChatRecipe`'s route wiring had no assertion (unwiring it kept CI green while re-opening the #410 blank-pour-guide bug) — pinned in `chat-recipe-validation.test.mjs`.
- **Corpus find (#558, behavioral):** `wendelboe-v60-light` carried `verified: true` with only a generic YouTube citation — no in-session cross-check on record, and the #528 entry explicitly says it stays `verified:false`. Since `verified: true` makes it a SNAP TARGET for `reconcileToReference` (the april-1-2-3 problem), the flag goes to false. Not a re-opening of the closed Wendelboe topic — it enforces the recorded decision.
- **Chat find (#557, behavioral):** the emoji stripper's ranges include arrows (U+2190–21FF) and dingbats (U+2300–27BF incl. ✓) — the model routinely writes "Bloom → 108g" / "= 600g ✓" and users read "Bloom  108g". The live measurement's "emoji" column (12/20 runs) was counting exactly these, not faces. Arrows/✓ are house typography (the watch's own step format is "Pour 2 → 180g"); the stripper now passes them and keeps stripping real emoji.
- **Doc-truth pass (this entry's PR):** the chat first-try measurement banked (was "NOT yet measured"); `chat-e2e` CI job + `chat-agent.yml` documented (17 workflows now, incl. the morning's `smoke-recommend.yml`/`recommend-logs.yml`); `agentPrompt.ts`/`agentContext.ts`/`chatActions.ts` added to the inventory; the false "recommend re-hydrates the full coffee row" claim fixed (run.ts re-hydrates only commonNotes/writtenSummary); exploration-slot wording ("third brew" → 4th); four stale in-code comments corrected (proportional spacing, per-recipe coach rate, pre-#527 grind numbers in dripAssist.ts, "133 recipes"); `scaledRefGrind`'s linear-vs-anchors nuance documented at the formula (the down-scaling branch never executes — model guidance only).
- **Flagged, deliberately NOT acted on:** `src/lib/knowledge/sca/` is fully orphaned since the lessons removal (zero consumers — delete or rewire, owner's call); `/api/coach-question`'s decline paths return 500 not the documented 204 (`Response` with a body + status 204 throws; client behavior identical — cosmetic); ~~`result.astringency` is collected but read by no prompt builder~~ (**closed 2026-08-23, #563** — and wiring it exposed a bigger one: the whole per-session sensory block was dead. See that entry); the chat's grind-unit check only fires when the grinder is named in-conversation, and the chat has no `vesselTooSmallForTarget` / no measured-timing calibration (matrix documented in the #544 entry's spirit — defensible N/A-ish, revisit if they bite); `TRANSIENT_DECAY`'s halfway-back discriminator and the Summary steadiness binary (0.3 vs the exported 0.15/0.35 bands) have no dedicated tests. **Chat first-try quality on the road (1/5, 2/5) was the top open quality lever — owner Go given the same day, shipped as #559; see the next entry.**
**The chat's Brew pill was dead for any bag not in the library — the model invented ids that look exactly right (2026-08-23, PR #555)**
- **The report,** an hour after the /recommend outage: "the direct link from chat to the brew timer doesn't work anymore." Screenshot: a full DAK Cassis recipe with a "Brew DAK Cassis — Orea Classic 450 ml" pill under it.
- **Diagnosed from the production DB, not guessed** (the pills are persisted in `conversation_messages.actions`; a read-only SELECT was added to `recommend-logs.yml`, PR #554): the 08:33 pill carried `id = dak_coffee_roasters__cassis`, **`in_library = FALSE`** — DAK Cassis isn't in the library (the chat was asking for the roast date to ADD it), and the model **invented the id**. Devious detail: ids are `coffeeKeyFor` slugs, so the guess was byte-identical to the id the bag WOULD get — it *looked* perfect and pointed at nothing. The tap fetched the non-existent coffee, threw the fully validated recipe away (the old `ActionPill` guard required `action.id` before doing anything), and navigated to a blank flow. Every earlier pill (Lush Lemons ×3, Taba, Berry Swirl — all `in_library = TRUE`) worked, which is why this only surfaced on the first-ever new-bag recipe. **Not a regression from the 08-21 work — a day-one gap in the chat→timer design** (#199/#200), and a third instance of the known pattern: a soft prompt prohibition ("do NOT start_brew a bag with no id") that the model leaked around.
- **The fix makes the pill work WITHOUT a library row** instead of forbidding harder — brewing from roaster+name is the app's oldest supported path (POST /api/sessions creates/merges the coffee row on the same slug). Three layers: (a) **`src/lib/chat/chatBrewTarget.ts`** — `resolveStartBrewTarget()` gates every start_brew in the route against the ids THIS turn's context offered (`knownCoffeeIds`); an unknown id is stripped when roaster+name are present (or resolved to the library row the names mean), and bounced back through the #544 repair machinery when they aren't — an invented id can no longer reach a pill. (b) The start_brew tool + prompt now carry **roaster/name (+ origin/process)** for not-yet-added bags — own commit, AI-behavior rule. (c) **`ActionPill`** synthesises the identity via `chatBrewIdentity()` when there's no id or the fetch fails, so the tap still opens the timer with the chat's exact recipe; the brew's save then puts the bag in the library. `droppedBrew` now carries its reason so the user hears the right explanation.
- **Tests** (`tests/dataflow/chat-brew-target.test.mjs`, 12): the anchor is the REAL dead payload from the DB and it must be rejected; both consumers are pinned source-level (#530/#535 lesson), and the pill pin was verified to FAIL against the pre-fix `ActionPill`. Two `chat-recipe-validation` pins updated for the new branch shape (target check ordered before recipe check).
- **Caveat:** pills persisted BEFORE this deploy carry neither roaster nor name — the owner's existing Cassis pill stays dead; re-asking for the recipe produces a working one.

**`/recommend` was DEAD for two days — one deprecated parameter, and no gate in this repo could see it (2026-08-23, PRs #550 + #551)**
- **The report:** "Recommendation failed / Try again" on the recipe screen, at 08:26 on a Sunday morning, with the owner standing at the grinder. Tapping Try again failed the same way.
- **The cause, read from the server log rather than guessed** — twice, at the exact minutes he tried (06:26:00 and 06:26:30 UTC = 08:26 Düsseldorf): `400 invalid_request_error: "`temperature` is deprecated for this model."`, `x-should-retry: false`, request_id `req_011CeKCmfevNu3a5ExiiRg43`. **PR #541 added `temperature: 0.8` to `callAnthropic`. `claude-opus-4-7` rejects the parameter outright**, so every recipe request 400'd from the 2026-08-21 deploy onward and no retry could ever have helped. Not intermittent, not load, not the model — 100% of calls, for two days.
- **Why nothing caught it.** `tsc` clean, 379 tests green, CI green, deploy green, the whole time. **Nothing between a merge and production ever puts a real request in front of the real API**, so a parameter the API rejects is invisible to every gate that exists here. The failure was also structurally silent on the client: the generation runs detached in `/api/recommend/start`, so the user only ever sees the generic "Recommendation failed" — the actual 400 lives in the app container's log and nowhere else.
- **Fixed (#551):** the parameter is gone, with a DO-NOT-RE-ADD comment carrying the exact API error, because the deleted code had a persuasive written rationale for setting it and would otherwise read as an improvement to restore. Sampling is the model's own; the variety levers #541 actually shipped are input-side (rotated menu, mixed seed, exploration slot) and unaffected, as are the ten deterministic guards. Mistral's `temperature: 0.5` untouched — that path accepts it.
- **`tests/dataflow/recommend-request-shape.test.mjs`** stubs `fetch` beneath the SDK and asserts the REAL serialized request body carries no `temperature` (nor `top_p`/`top_k`), and that it is still the cached-prefix Opus call. Verified BOTH ways in-session: fails with the parameter re-added, passes without. **Honest scope, stated in the test itself: it would NOT have caught the original break** — the shape was deliberate; only the live API knew it was rejected.
- **#550 — `recommend-logs.yml`,** a read-only push-triggered workflow (`.github/recommend-logs-trigger`; `.github/**` is paths-ignored by deploy, so firing it never redeploys) that pulls the app container's recommend errors, container status, restart count and memory off the VPS. Output filtered to pipeline/error lines and truncated to 300 chars/line because the repo — and therefore the run log — is PUBLIC; it never reads the `.env`. This is what turned a guess into a diagnosis in one run, and it is the tool to reach for first the next time a surface fails only in production.
- **The gap is CLOSED (owner Go, same day):** `deploy.yml` now carries a **`smoke` job** (`needs: deploy`) that runs `scripts/smoke-recommend-call.mjs` after every deploy — ONE real Opus call through the app's OWN `callRecommendModel` (bundled from `src/`, so parameters/model id/system-block shape/key/quota are all validated by the live API). A rejected request fails the deploy run loudly, minutes after the merge, instead of surfacing on the owner's next brew. Cost ~$0.20/deploy, owner-approved. The standalone `smoke-recommend.yml` (dispatch + `.github/smoke-recommend-trigger` bump) stays for on-demand runs between deploys — keep the two step-for-step in sync. Skips green with no `ANTHROPIC_API_KEY`; prints usage + shape only, never the key or recipe text (public run logs). Note its limit honestly: it exercises the model call from the runner at the deployed commit, NOT the VPS container — the deploy job itself already fails if the container doesn't come up. **The rule stands: any change to a model call — parameters, model id, provider, prompt structure — is unverified until a real request has been made.** A green test suite says nothing about it.
- **Lesson worth keeping: "stated rather than inherited" is not automatically an improvement.** #541 made an implicit default explicit — a change that reads as pure hygiene, was reasoned about carefully in a long comment, and was never once executed against the API it was sent to.

**Watch app refresh: orchid murmuration field + the transferUserInfo backlog fixed (2026-08-22, PR #548 — merged, TestFlight build green + uploaded; on-device verify pending)**
- **Report:** the Apple Watch app still wore the pre-July layout (pale rose→peach→coral stripe + plum ink — icon and widgets moved to the saturated murmuration look in July) and "läuft nicht gerade rund".
- **Layout:** `native/ios/App/BTTSWatch/ContentView.swift` now carries a `MurmurField` (same construction as `BTTSWidget.swift`) in the **orchid** combo — the actually-shipped app-icon field (see the correction note below) — with text on the on-dark **cream** token instead of plum (the saturated field is a dark surface; the widgets' anthracite fails contrast on its deep blue/magenta regions). Owner chose orchid + cream when asked (bigsur/berry were the offered alternatives). Display hierarchy unchanged (recipe eyebrow / NOW / step serif / big countdown).
- **The "nicht rund" root cause was on the PHONE side** (`BrewWatchPlugin.swift`): the ~3-s schedule re-send loop pushed EVERY payload through `transferUserInfo` — a persistent FIFO that was never pruned. One 4-min brew queues ~80 identical payloads; whatever the watch didn't consume live was replayed as a backlog at its next launch (the build-18 diagnostic literally showed `rx 46`), costing Bluetooth/battery on both sides and making the watch-app launch sluggish. Fix: before each queue, cancel all outstanding NOT-yet-transferring userInfo transfers, so the queue holds at most the latest payload plus one in flight (the in-flight one is left alone — cancelling it every 3 s could starve a slow link so no payload ever completes). All three delivery channels stay; the confirmed-working build-18/19 mechanism is untouched. "end" now implicitly clears queued "start"s of the finished brew.
- **Documented cleanup closed:** the six leftover `@Published` diagnostic fields + `testBuzz()` in `BrewWatchModel.swift` (the roadmap's "cosmetic TODO at the next watch build") are removed — they also fired a SwiftUI invalidation on every 3-s re-send for nothing. An already-running workout session when a new brew replaces a winding-down one is no longer logged as an error. os.Logger lines kept.
- **Verified:** `tsc` clean, 50 src + 379 dataflow tests + corpus validation green (no web code touched); removed-symbol grep clean. Swift compilation could NOT be checked in-session (no Xcode) — the compile gate was the TestFlight run itself: run 32594677138 green with the Archive / Export / IPA-shape-verify / **Upload to TestFlight** steps each individually confirmed success (21:45–21:49 local, 2026-08-22). **NOT verified:** the on-device look and the launch-backlog behaviour — owner checks after installing the build.
- **Correction to this file (icon variant):** the July-2026 icon entry below says the shipped icon is the "bigsur" combo — that became stale: after PR #491 the icon was re-tinted to **"orchid"** (pink/magenta/lilac/blue, NO orange — owner found the orange too harsh) via a later trigger-bump build; `scripts/generate-app-icon.py`'s default variant is `orchid` and its comment marks it "the SHIPPED icon". The two home-screen WIDGETS still use bigsur (rotation) + berry (scan) — only the icon moved.
- **Process note (owner called it out, rightly):** the field-combo and text-colour picks are exactly the subjective visual decisions the Operating-mode section says to ASK about — they were built first and asked about after. The answers happened to match the build; next visual fork gets the question BEFORE the code.

**The CHAT had one guard where /recommend has ten — the round-1 fix was aimed at the wrong half (2026-08-21, same day)**
- **What happened:** hours after the variety work shipped, the owner used the CHAT on the road and got a recipe that could not be brewed: an Orea V4 Classic + Drip Assist at 450 ml whose **final pour was 225 g — half the water — in the 15 s the clock had left**, after a **two-and-a-half-minute hole** where nothing happens. With a Brew button under it. His words: "Genau der Müll wie vorher. Hast du nur recommend oder auch den Chat repariert?"
- **The honest answer, and the process lesson:** the chat WAS touched in round 1 — but only for **variety** (todaysAngles, whatToExplore, insights, anti-repetition). Every defect in his screenshots was a **quality** defect. Round 1 measured `/recommend` and shipped, while the complaint was about a surface whose recipe quality nothing had ever checked. **The gap was documented in this very file for weeks** ("the chat's `start_brew` path runs no fidelity guard at all") and was read past. Lesson: when a complaint names a surface, fix and MEASURE that surface — a measurement on the neighbouring one is not evidence.
- **The structural cause.** `/recommend` runs **ten** deterministic guards; the chat's `start_brew` ran **one** (step sanitation). Verified by grep: `normalizeGrindToGrinder`, `reconcileToReference`, `vesselOverflow`, `vesselTooSmallForTarget`, `stripMinimalAgitationSwirls`, `stripProactiveDripAssist`, `calibrateTargetTimes`, `calibrateDripAssistFinish` each had **exactly one call site, all in `recommend.ts`**. The surface most likely to be used away from home, on unfamiliar kit, was the least protected one.
- **Every defect traced to a sentence /recommend has and the chat didn't.** The pour arithmetic: `/recommend`'s prompt carries "a single pour of ~200 g+ cannot be poured gently in the time a drawdown allows (~4 g/s)" **and** "bloom + 3–5 pulses, never one giant final pour" — **neither existed in the chat prompt**. The Hatakeyama-with-"geduldige Pours" suggestion to a man with no gooseneck: the "Kettle & pour control" section drew exactly one consequence (put the disc on it) and framed the disc as fully restoring pour control, so nothing said a recipe whose TECHNIQUE needs a gooseneck is off the table. The Clever offered after "Nein. Orea.": **the prompt actively asks for it** ("Immersion … worth one clause"), and the constraint-override rule was explicitly *profile*-scoped, so narrowing a set the user owns overrode nothing. "Apex oder Classic?" handed back to him: **no rule anywhere told the chat to make the brewing call itself**, and three rules licensed asking — with the full flow ranking of both bottoms in its own context.
- **Shipped — `src/lib/recipe/validateRecipe.ts`,** the same checks as questions rather than corrections. It validates the **RENDERED timeline**, which is the load-bearing detail: the model never wrote that dead gap, it was DERIVED (three pours over a 3:30 clock leaves a hole), so an authored-step check would have passed the recipe. **Validate, not repair**, because the surfaces differ: `/recommend` corrects in place and the user only ever sees the result (unchanged — it is measured and it works), while the chat has already spelled the numbers out in prose, so a silent rewrite splits the message from the timer — a defect this repo shipped once (#198). A failed recipe goes back to the model as an error `tool_result` naming each problem; **one** repair round; still failing → **no Brew button**, and the user is told why.
- **A real `/recommend` bug fell out of it.** `parseGrindDegrees` read "26 clicks" as 26 degrees. Both prompts ask for CLICKS on the Comandante and the corpus states degrees, so **every travel recipe** looked ~350° too fine, tripped the grind window and had its grind silently overwritten. Found only because the validator printed the comparison it was making.
- **`april-1-2-3` downgraded to `verified: false`.** The owner supplied April's own recipe page: it publishes **no "1-2-3" method at all** (April Brewer 12 g:200 g @92 °C, TWO 100 g pours; Hybrid 14 g:240 g; Sustainable 13 g:200 g @94 °C). The entry was 15 g:250 g @96 °C, 50/100/100, tagged `minimal-agitation` — not one figure matches, the tag contradicts April's own "pour aggressively so that you agitate the grounds", and the name doesn't match its own numbers (50/100/100 is 1:2:2). **Not cosmetic:** `verified: true` made it a snap target, so `/recommend` could overwrite a candidate's mechanics with numbers nobody published. Peer audit of the cluster: four entries were already `verified: false`; **`rolf-april-v60` re-checked against its cited URL and holds up exactly** (20 g:300 g, 1:15, 92 °C, six 50 g pours at 0:00/0:40/1:10/1:40/2:10/2:40) — left alone. Note its technique is literally "aggressively so that you agitate the grounds", a real instance of the disc-feasibility gap.
- **Also fixed:** the chat renderer understood only bold and italic, so a markdown table printed as raw pipes (`| | |`) — while the chat's own prompt **contains a GFM table as the house example**. Tables + lists now render (own `overflow-x` container per PR #505); the italic branch no longer pairs the underscores in `brew_again and coffee_library`. And emoji are **stripped from the stream**, not merely forbidden in prose — the chat was the only AI surface here with no output-side check, while `loadingInsightLint.ts` has enforced the same rule in code for months.
- **Verified:** `tsc`, 50 src + 372 dataflow tests, corpus validation clean. The anchor test is the reported recipe with its real numbers and it must be **rejected**; route-level tests assert the handler **imports and calls** the validator (#530/#535 shipped twice on producer-only tests); the renderer is checked by **rendering it**, not by reading the source.
- **MEASURED LIVE 2026-08-22 (#545/#546, `chat-agent.yml` runs 32580566969 + 32580909239):** how often the model writes a brewable recipe FIRST try — 4 scenarios × 5 runs on Sonnet, real prompt + tools, graded by the real validator. **Home is decent, the road is weak:** home-standard 350ml **4/5**, big-batch 600ml **3/5**, travel-no-gooseneck 450ml **1/5**, narrowed-to-orea 450ml **2/5**. Every non-first-try run cost one repair round (a visible wait); **2/20 were still broken after repair** (both `dead-gap`) — in production those get NO Brew button. What the prompt rules DID land: disc used **5/5** in both gooseneck-less scenarios, an excluded brewer re-offered **0** times. The guard chain held throughout — nothing broken can reach the timer (separately proven by the deterministic `chat-e2e` CI job, 16/16, validator-neutered counter-proof 7/16). **Next lever when this is worked: the travel/big-batch first-try rate** — the prompt's pour-arithmetic rules aren't landing for large constrained batches; the measurement harness to grade any prompt change now exists (bump `.github/chat-agent-trigger`). NB the sim's "emoji" column (12/20 runs) counted arrows/checkmarks ("Bloom → 108g", "✓") — see the stripper entry below.

**The recipes repeat — found DOWNSTREAM of the menu everyone kept fixing (2026-08-21)**
- **The report:** "Die Rezepte gehen mir sehr auf die Nerven … es ist schlimmer geworden. Nicht nur Brewer recommendations, sondern vor allem die Rezepte selbst." Plus the framing that matters: BrewLog is **matching** (which recipe/brewer for THIS coffee in THIS context) **and exploring** (what if, what else could this bean give) — both were broken.
- **Why four previous rounds missed it.** #480/#483/#486/#523 all targeted the injected MENU, and #523 genuinely fixed it (19/20 distinct menus, pinned). The complaint didn't move because **the repetition lives after the menu**, and nothing in the repo measured after the menu. New `scripts/recommend-variety-sim.mjs` + `.github/workflows/recommend-variety.yml` close that: 20 consecutive brews per scenario **with the production feedback loop** (each brew fed back as a logged session so `recentReferenceNames` / `demoteBrewers` / own-references evolve as they really do — the loop no existing test ran), reporting **distinct (method←basedOn) pairs, distinct headline number tuples, fidelity-snap rate and out-of-menu basedOn rate**. Offline it already showed the shape of the problem: for the natural-Ethiopian scenario the menu varied 14/20 while the LEAD recipe was identical in all 20 brews.
- **Cause A — the cached system prompt carried its OWN recipe menu.** A dozen fully-numbered CHAMPIONSHIP / REFERENCE recipes, the Chemex + Origami fill-in-the-blank templates, 7 numbered iced recipes and 8 numbered cold brews — byte-identical on every call while the per-turn library rotated underneath. Asked for a recipe, the model reached for the list that was always there. **All of it deleted**; concrete numbers now arrive only in the user message. The prompt keeps what a system prompt is for: mechanism, agitation, geometry, capacity, grind reference, and the iced/cold-brew RULES. Deleting the iced numbers required `scoreRecipe` to treat iced like cold brew — a **hard mutual-exclusion filter** (summer-time ⟷ the corpus's 8 iced recipes), verified: summer-time → japanese-iced-v60/kalita/aeropress-bypass, morning-ritual unaffected.
- **Cause B — #527 was only HALF a fix (correction to this file).** It recalibrated the NICHE° table ~−20° and pinned it, but the recipe blocks in the SAME prompt kept the old numbers: Kasuya 390–400° in the table vs **411–421°** thirty lines below; Clever 395–415 vs 421–431; Orea Classic 385–390 vs 406–411; Origami cone 378–388 vs 398–408; a stale "~410° normal V60" inside the fidelity rule (now 380°). A candidate written off the stale copy is ~20° coarse → trips the guard's grind window → gets its whole signature overwritten. The consistency test now scans the WHOLE prompt (values derived in place from published clicks, e.g. Kasuya Super Coarse, are exempt) and asserts the deleted fragments stay deleted. The **Comandante block is now GENERATED** from `grindSettings` via the documented map (clicks = 23 + (° − 380) × 0.3) — it had drifted in the other unit too (Chemex 24 vs measured 28–32, AeroPress 19 vs 16–18).
- **Cause C — the fidelity guard was all-or-nothing.** ONE drifted field (time OR grind OR temp) replaced `waterTempC` AND `grindSize` AND `targetTimeSec` AND the entire pour plan. A candidate that was a deliberate adaptation everywhere except 7°C came back as the literal published recipe — and two candidates that both tripped it came back as the SAME recipe. **The guard meant to stop fabrication was erasing legitimate variation.** Now: one drifted dial is corrected alone (time-only keeps the authored pours for percolation, since the timer re-derives pour timing; immersion-shaped plans still full-snap because their durations must sum); **≥2 dials, or a restructured pour plan, still full-snaps** — new `pourCount` signal, tolerance 2, so the documented "10 pulses → 3 pours" mangle is still caught.
- **Cause D — the own-references block instructed repetition, verbatim.** "repeating a result they already loved is usually the right answer", with the user's exact numbers, every brew, never rotating — while RECENTLY RECOMMENDED in the same message asked for different references. The concrete instruction wins, and it self-reinforces (today's 4★ becomes tomorrow's guaranteed repeat). **Owner's call: baseline to build on.** Reframed as "it already worked — what's still missing from a 5★?", one named variable changed; verbatim repeats need a reason; an entry just recommended is marked "build on it, don't re-serve it".
- **Cause E — `demoteBrewers` exiled the pools it needed to rotate.** It demoted EVERY dominant family, deterministically, every brew. The dominant families are V60 and Clever precisely because they hold the deepest pools (45 and 13 recipes) — the only place `pickRepresentative` has anything to rotate between. Demoting them handed the lead slots to 1–3-recipe brewers (orea-v4-fast has exactly one) where nothing rotates. **Now at most ONE family is mechanically demoted per brew, rotated by the seed**; the prompt note still names them all.
- **Cause F — `amountGuide` pinned the dose too** ("~350g water / 23g dose"), so both headline numbers were constants. Water is the user's choice; the **ratio belongs to the recipe** being adapted, so the dose follows from it. Capacity rules key off water — untouched.
- **Cause G — #520 removed the differentiation surface.** Cutting nine prose fields to one `whyChosen` was right (six rendered nothing), but it also removed the only thing forcing the model to justify two DIFFERENT experiments. New **`experiment`** field: one clause, ≤20 words, and the prompt requires the two clauses to name different variables — "if you cannot state two, you have written one candidate twice".
- **Also:** ~~`temperature: 0.8` stated explicitly for Opus~~ — **REVERTED 2026-08-23 (#551): `claude-opus-4-7` REJECTS `temperature` (`400 invalid_request_error: "temperature is deprecated for this model"`, `x-should-retry: false`), so this one line took `/recommend` down completely for two days. See the Done entry at the top.** The rest of #541 stands: the **rotation seed now mixes a per-request component**, so re-asking gives a fresh menu instead of a byte-identical one (it froze between logged brews — exactly when the user is exploring); **`basedOn` is softly bound to the turn's menu** (`menuBinding.ts`, logged never dropped — nothing is banned) and **"Own experiment" is now a legitimate category** (owner decision), the honest alternative to reconstructing a published recipe from memory; once a bag has 3+ logged sessions (so from the 4th brew) the **SECOND candidate is the exploration slot** (owner decision: stay at 2 candidates, exploration lives inside the pair).
- **The CHAT half — it had NO variety machinery at all.** Full corpus (135 recipes, ~39k tokens) cached and byte-identical every turn, no scoring/rotation/cap; zero anti-repetition language (grep: 0 matches); the recent-brews block existed so it could QUOTE the last recipe ("quote THOSE values"), which increased anchoring. Now: **`todaysAngles.ts`** runs the same scorer, seeded per day and per bag, naming a couple of concrete angles for the rotation bags (rotates daily, holds steady within a conversation, every name a real corpus recipe — tested); **`coffees.whatToExplore` finally reaches the chat** (written weekly by the compact cron, read by the brew flow, never shown to the surface most likely to be asked "what should I try?"); **coach insights injected** (the prompt had claimed "recent research insights" for months while nothing was — now true); **`recentReferenceNames` extracted to `historyUtils.ts`** and injected as "steer away unless asked to repeat"; and an **exploration posture** section — one optional variation line changing exactly ONE variable, grounded in a technique id, base recipe kept conservative. The old "don't free-hand a new milestone list" ban is **scoped back to arithmetic**: parameter-level exploration is always open, and a self-authored experiment is allowed when labelled as one, never attributed to a person, with round cumulative milestones stated as a running total (the server re-checks via `sanitizePourSteps` + `reconcileWaterToPourPlan`). `max_tokens` 2000→2500.
- **Test discipline:** the chat tests assert the route **imports and pushes** each block, not just that the functions work — this repo has twice shipped a function documented as feeding a prompt while nothing called it (#530, #535), both "pinned" by producer-only tests. The variety test now also runs the production feedback shape.
- **Honest correction found by writing a test:** I first asserted the LEAD recipe must vary across fed-back brews, and it failed for the natural-heirloom context — **correctly**. There one recipe scores 9 against 7s, and the owner's rule is that best fit always decides; demoting a clearly better recipe to manufacture variety is the thing that must NOT happen. The assertion moved to the SECOND slot, where variety belongs and the exploration slot lives.
- **MEASURED LIVE (Opus, 2026-08-21, run 32472460280 — 40 real candidates per scenario, feedback loop on):**

  | scenario | distinct (method←basedOn) | distinct headline numbers | distinct methods | fidelity snaps | out-of-menu basedOn |
  |---|---|---|---|---|---|
  | Washed Kenyan SL28 · morning · balanced · 350ml | **24 / 40** | **32 / 40** | 7 | 4 | 9 |
  | Natural Ethiopian heirloom · focus · clarity · 350ml | **13 / 40** | **30 / 40** | 7 | 19 | 6 |

  Both scenarios beat the offline menu-following CEILING (12 and 6) — the model is not just following the rotated menu, it is also writing its own labelled experiments, which is the point. Headline numbers now differ on 30–32 of 40 candidates, so "same parameters every brew" is gone even where a recipe legitimately recurs.
- **HONEST LIMIT — this is an after-measurement, not a before/after.** No `--live` baseline was ever captured (the harness didn't exist before this work), so the improvement is inferred from the offline ceiling, not proven against a pre-change live run. The owner's own brews remain the real verdict.
- **Flagged for the next round, NOT fixed here:** scenario 2 shows **19 fidelity snaps across 40 candidates** (~48%) against scenario 1's 4. Post-#541 a "snap" is usually a SINGLE corrected field rather than a collapsed recipe — its 30/40 distinct headline numbers say the variation largely survives — but the summary doesn't break down which fields fired, and a natural-process light roast tripping the guard that often is worth a look. `reasons` names the fields; log them per run to see the split. Out-of-menu basedOn is 9 and 6 of 40 (~22 % / 15 %) — genuine reaches past the shortlist, allowed by design (nothing is banned), worth watching if it climbs.

**Acaia Bluetooth pairing FIXED — a #408 regression that stopped the scale streaming (2026-08-18/19, PR #537, owner-confirmed on-device)**
- **Report:** "Du hast die Acaia Bluetooth Kopplung kaputt gemacht — klappt nicht mehr." The scale **paired but never streamed weight** (connected, live grams never arrived).
- **Root cause — PR #408 (2026-06-20), the only change to the BLE connection path since the owner confirmed the scale working on 2026-06-17.** On the owner's Lunar/Pearl (the iOS "V2" path) connecting is a two-step handshake in `src/lib/native/acaia/acaia.ts`: `ident()` sends `encodeId` while `recievesNotifications` is false, and only on a SUBSEQUENT `ident()` — the instant the first inbound notification flips that true — does it send the `encodeNotificationRequest` that makes the scale actually STREAM weight. The heartbeat monitor (`startHeartbeatMonitor`) is what re-calls `ident()` to carry the handshake across that boundary. #408 ("independent heartbeat + stall revival") rewrote the monitor to only re-ident on a **3 s stall** (itself gated on `recievesNotifications`, a chicken-and-egg) and otherwise just `heartbeat()` — dropping the per-tick re-ident. So the notification request was never sent on connect. #408's own comment gave the mistake away: it read upstream's re-ident as *"a no-op on the iOS V2 path once subscribed"* — true only AFTER the handshake; DURING it, the re-ident IS the handshake. The device inherited #408 in a later TestFlight build, which is why it "suddenly" broke months later.
- **Fix:** the monitor again does THREE things per tick — (1) re-`ident()` until the scale is subscribed AND the stream requested (restores pairing; a genuine no-op once both flags are set), (2) #408's independent keepalive, (3) #408's stall revival. So it keeps #408's real anti-freeze improvement and fixes the regression it introduced. No protocol bytes changed. The acaia `README.md` monitor note was corrected (it had documented only #408's behaviour) with a "do NOT collapse (1) back into the stall path" warning.
- **Proof (Hard Rule):** new `tests/dataflow/acaia-handshake.test.mjs` drives the REAL `AcaiaScale` through connect + a first notification with a fake transport + fake timers and asserts the weight-stream request goes out promptly, WITHOUT the 3 s stall path — verified it **fails on the #408 monitor and passes on the fix** (both ways, in-session). `tsc` clean; all 391 tests green; CI (`check` + `screenshots`) green; Hetzner deploy succeeded.
- **On-device: owner-confirmed** — "Acaia Kopplung läuft sauber und zuverlässig." **Reached the phone on deploy, NO TestFlight rebuild** — the acaia protocol is web code the remote-URL shell loads live (the native BLE plugin was already in the shell since build 9). Owner force-quit + reopened to drop the cached shell.
- **Lesson:** when deviating from a ported/upstream protocol, a comment justifying the deviation ("no-op once subscribed") can be true in the steady state and false during a transient (the handshake). The safe move is a test that exercises the transient — which also pins the behaviour so a future "restore upstream parity" can't silently re-break it.

**Second review + four owner decisions executed (2026-08-18, PRs #532–#535 — all merged + deployed)**
- **The review method:** six parallel verification agents checked every CLAUDE.md claim against the code (API table, recipe corpus, file inventory, pages/components, prompt wiring, infra/workflows); every reported finding was re-verified in the main session before acting. `tsc` clean, full suite green throughout.
- **The one real functional find: the 2026-08-16 coach-answer/measured-pour "fix" was wired to the wrong consumer.** Both signals were added to `buildHistorySummary` and documented as reaching `/recommend` — but that function's only production consumer is `loading-insights/refresh` (the loading screen). The recipe generator never saw either. Same failure class as the day before, one level deeper: the function was tested, the route was not. **Fixed with owner Go (#535):** `buildMeasuredFeedback()` injects a MEASURED BREW FEEDBACK block (clarification answers + Acaia measurements, current coffee first, "" when none) directly into `/recommend`'s user message; `tests/dataflow/measured-feedback-in-recommend.test.mjs` pins the function AND, source-level, the route wiring. **Lesson: when pinning a data flow, the test must cover the CONSUMER, not just the producer.**
- **#532 doc-truth pass:** the false "→ /recommend" claims corrected; migrations marker-file path documented as primary; `docs/grind-settings.md`'s wrong "update all three" note fixed (explore-agent reads the constants live); `coffees/compact` row corrected (cron batch summarizer, not a dropdown); `deploy.yml`'s obsolete run-0017-on-every-deploy block removed; `validate.mjs` recipe→technique refs hardened W→E.
- **#533 (owner decisions):** the research cron removed from ofelia + the coffee-alert webhook deleted — see the resolved note in the data-flow-audit entry below. `deploy.yml` now restarts ofelia every deploy (it reads its bind-mounted ini only at startup — without the restart the removed cron would have kept firing).
- **#534 (owner decision):** the reference `wendelboe-aeropress` owner-verified → `verified: true`. Topic closed.
- **This doc pass (same day):** inventory completed — `src/lib/{coffee,coach,chat,taste,health,recommend,coldBrew,audio,native}/`, `craftingPhases.ts`, `heroQuestions.ts`, `claude/{recommendPrompt,sessionCorpus,ownReferenceRecipes,methodRotation}.ts`, `utils/{brewMethodKey,agitationGuard}.ts`, `brew/flowAnalysis.ts`, the four native hooks, `ScalePanel`/`ColdBrewSteep`/`BlendComponentsEditor`/`RecommendJobWatcher`/`NativeWidgetBridge`, the full 13-workflow list, the CI check job's real four gates, and the loading-insights/translateNotes model calls were all present in code but absent from this file.
- **Flagged, deliberately NOT acted on (cheap follow-ups, not bugs):** `WEATHER_LATITUDE`/`WEATHER_LONGITUDE` sit in `.env.example` but are NOT passed through `docker-compose.yml` → the greeting's weather always uses the built-in Düsseldorf fallback in prod (zero impact while the owner lives there); `USER_DISPLAY_NAME` is passed through but read by nothing; `docs/coffee-experts.md` (Du 2019 row, missing Harris/Rojewska championship rows, stale header) and `docs/recipes-full.md` (79 of 135 recipes, still lists the deleted composite Wendelboe) lag the corpus — both were already marked "partially stale" here.

**Full-codebase audit + fixes (2026-08-18, PRs #525–#530)** — a second model (Fable 5) re-verified every finding against the code before acting; each fix is its own revertible PR.
- **Passkey registration was an open door (#525, SECURITY).** `POST /api/auth/register` sits under the `/api/auth` middleware exemption and had no auth of its own — it deletes the stored credential and mints a session, so two unauthenticated requests were a full account takeover that also locked the owner out. The repo is public, so the sequence was readable. Now registration is allowed only when no credential exists (first run / post-reset, and reset already requires a session) or the caller holds a valid session (re-enrol); stale registration challenges expire after 2 min. **Proven** end-to-end against a throwaway Postgres driving a real WebAuthn ceremony in headless Chromium: pre-fix a cookie-less caller replaced the stored passkey and got `cf_session`; post-fix `401`, credential untouched. Live-verified on prod (`401`).
- **The `/recommend` grind table was ~+21° stale (#527).** The prompt's `NICHE° GRIND REFERENCE` read V60 396–406° while the measured anchor (grindUnit.ts, the 129 corpus recipes, docs) is 380° — ~6 Comandante clicks coarser than the owner grinds, on every recipe written off the general table; it even contradicted its own "23 clicks" (396° = 28 clicks by the app's own map). Corrected to the measured baseline, the three Orea bottoms pulled into `grindSettings.ts`, and pinned by `tests/dataflow/grind-reference-consistency.test.mjs` so it cannot drift again. Plausible (not proven) cause of the long-open "finishes a minute early" report.
- **Removed the composite "Wendelboe AeroPress" (#528); the surviving REFERENCE-category `wendelboe-aeropress` is OWNER-VERIFIED (2026-08-18) — closed, do not re-raise.** #528 removed the CHAMPIONSHIP-category variant (attribution "Tim Wendelboe — adapted by competitors" / "compilations" — a famous name on numbers no one published, the exact fabrication class the Hard Rule forbids); Harris 2017 + Rojewska 2018 (real single-competitor finals routines) KEPT as `verified:false`. Corpus 136→135, championship 9→8. The review then found a second recipe with the same display name — the REFERENCE-category `wendelboe-aeropress` (`expanded.ts`, 12g:200g/88°C) — and the owner verified it against Wendelboe's own published material himself: now `verified: true` with an owner-attestation source entry. The three other concrete-parameter Wendelboe entries (`wendelboe-v60-light`, `wendelboe-kalita`, `wendelboe-pulse-v60`, `wendelboe-v60-big-batch`) stay `verified:false`, unaudited — the owner has closed the Wendelboe topic; don't bring it back up.
- **Locked methods with no recipe stop inventing + Hatakeyama reachable (#529).** A locked Orea bottom injected an EMPTY recipe block (the corpus has none for Apex/Classic/Open) → the model invented freely; now it falls back to the best-fit portfolio with a prompt note to adapt the nearest cone. And the Hatakeyama 2024 JBrC recipe was unreachable (brewer `origami-air-m`, which no equipment string resolves to) — retagged `origami-cone` (owner-confirmed: the Air M is one dripper that takes V60 cone OR Kalita wave filters; his recipe uses a cone filter), so it reaches every library-started brew.
- **The measured Acaia pour reaches the COACH — but NOT `/recommend` (#530, claim corrected 2026-08-18).** `flowAnalysis` (pourSteadiness = channeling signal, overshoot) was captured on every scale brew and read by ONLY the Summary card. #530 wired `formatMeasuredPour()` into the coach formatter (→ /insights — real, reaches `/recommend` only indirectly via the lagged coach-insight rows) and into `buildHistorySummary` — but `buildHistorySummary`'s only production consumer is `loading-insights/refresh` (the loading-screen pool), NOT `/recommend`: `recommend.ts` never calls it and never reads `flowAnalysis` (verified; `tests/dataflow/measured-pour-in-prompts.test.mjs` pins the function, not the route). **Closed 2026-08-18 (owner Go):** the measured pour now reaches `/recommend` directly via `buildMeasuredFeedback()`'s MEASURED BREW FEEDBACK block (see the coach-question row) — route-level wiring pinned by `tests/dataflow/measured-feedback-in-recommend.test.mjs`. Silent when no scale was connected.
- **Fable-5 counterpoints to the audit (verified, not just inherited):** the `/recommend` background-job store (`jobStore.ts`) is CLEAN, not sloppy (TTL eviction, single-user justification, terminal poll) — it was only undocumented; and the synchronous `/api/recommend` is an orphaned-but-deliberate back-compat wrapper, not dead code. Both now documented.
- **Doc-truth pass (this entry + the corrections above):** recipe counts (135/8/82/45), the grind-wiring note (now test-bound, not "three places you must remember"), the `recommend` row (Opus default per #519, not "runs on Mistral"), and five routes that existed in code but not the API table (`recommend/start`+`/status`, `live-activity/start`+`/end`, `admin/remap-field-zones`).
- **Process note banked (2026-08-18):** times are always the owner's wall clock (Düsseldorf), never UTC; poll GitHub via the MCP tools, never `curl` the API (no token in-session → a curl wait-loop returns empty forever and never fires — it once made a finished deploy look like it ran for 100 minutes). #526.

**The repetition was real and had never been measured — plus three more recipe defects (Aug 2026, PRs #517–#523)**
- **"Die Rezepte sind zu wiederholend" — reported many times, fixed three times (#480/#483/#486), still broken.** Nobody had ever run the selector and counted. Doing that gave **1 distinct menu across 20 consecutive brews**, twice, for two unrelated coffees. Two causes. (a) **Rotating ties cannot change the menu.** It reorders equal-scoring recipes; the one-per-brewer loop then takes each brewer's BEST recipe, which is the same recipe in any order. The menu is a SET and the set never moved — three rounds of work went into permuting something immediately collapsed. (b) **The seed was degenerate:** a ms timestamp used as `seed % groupSize`, and 86,400,000 divides 2,3,4,5,6,8,9,10, so brews a day apart hit the same offset. Fix: `pickRepresentative()` rotates WHICH recipe represents each brewer (V60 slot = Hedrick / Wendelboe / Kasuya on different days), capped at 1 point below that brewer's best so **fit still decides**; `mixSeed()` hashes the seed. After: 19/20, 15/20, 18/20. `tests/dataflow/recipe-menu-variety.test.mjs` pins the variety AND its two costs — a clear winner (score 9 over 7s) still leads all 20 brews, and the menu stays score-sorted (substitution can swap a score 1 apart, so the result is re-sorted; the prompt promises best-fit-first order).
- **"Der Chat kennt nur V60" — a real signal, not a hallucination.** The chat gets the WHOLE corpus flat, and it is **45/136 V60 (33%, twice the next brewer)** while the owner's Orea V4 has **exactly one** recipe — because that is what the world publishes, not what suits his beans. Recipe COUNT read as endorsement. Now grouped by brewer with the imbalance stated outright ("how many recipes a brewer has is not a recommendation"). Nothing removed. That block is also static and ~39k tokens and was riding inside the per-turn context where it could never be cached — it now has its own `cache_control` block and is built once per process.
- **Drip Assist was decorative (#517).** The prompt asked for ~5° coarser and nothing enforced it; worse, the fidelity guard rewrites grind whenever TIME or TEMP drifted and rewrote it to the BARE reference number — so a wrong clock silently undid a right grind. `reconcileToReference` now takes the candidate's `method` and carries `DRIP_ASSIST_GRIND_OFFSET_DEG` (+5) in both the comparison and the snap. Temperature deliberately untouched: no source in this repo for a disc-specific temp, so none invented.
- **The grinder UNIT leaked (#518).** Comandante selected, recipe came back in Niche degrees — with the prompt saying "NEVER Niche°" outright. Third prose instruction to leak in two days. `normalizeGrindToGrinder()` converts using the owner's own measured anchors (380°=23 clicks, 400°=29, ~3.3°/click, both pinned by test); the two scales can't be confused by magnitude, so a bare number is unambiguous. A right-unit value is returned untouched, wording included; unparseable is left alone.
- **`/recommend` back on Opus (#519)** — owner's call after those three leaks. Rationale + what it does NOT rest on: see the AI-models section.
- **The recipe screen's prose was 15 fields; six were rendered by nothing (#520).** `primaryVariable`, `confidenceReason`, `learningValue`, `brewingLesson` (3–4 sentences ×2), `sessionObjective`, `coffeeAssessment` — plus `intent`/`coffeeLayer`/`roasterPriorUsed`, which the prompt requested and the parse schema discarded. `/recommend` does NOT stream, so every sentence is loading-screen time. Kept: `whyChosen` (one sentence, the "Why" row) + `reasoning` (read by brew detail, brew-insight, chat context, coach). Measured on the spike report's 36 candidates: the three dropped rendered fields average 466 chars per candidate vs `whyChosen`'s 182. Removed fields stay OPTIONAL on the type — ~180 stored sessions carry them in jsonb.
- **VPS provider switch verified, not assumed.** `server-diagnostics.yml` now reads `RECOMMEND_PROVIDER`/`COACH_PROVIDER` from the `.env` AND from inside the running container (they can disagree — the `.env` is what the container was STARTED with). Result 2026-08-17: **not present in either → the code default decides → Opus is live.** Keys reported present/absent only; greps anchored to those two names. **Trap found on its first run:** `docker compose exec -T` reads stdin, and the script is fed to the remote bash over stdin — the first exec swallowed the rest and every later section was skipped while the job still exited 0. Every `docker compose exec` there now carries `</dev/null`.
- **"eine Minute schneller fertig" — DIAGNOSED + fixed for the Drip-Assist case (Aug 2026, owner-reported the cause himself).** The pour timeline reserves a flat **33% of total time for the final drawdown** (`buildPourOver`, `pourSequence.ts`) — the dead air after the last pour before the cup is through. The disc distributes water across the whole bed, so it drains almost as fast as it's poured; that 33% drawdown simply doesn't happen with the disc, so the timer ran ~a minute past when the cup was actually done. Fix (disc-only, non-disc byte-identical): the drawdown reserve is now **method-aware** — `drawdownReserveFrac()` returns a thin **7%** for any "… + Drip Assist" method, 33% otherwise; and a disc-**locked** percolation candidate has its `targetTimeSec` **shrunk** at recommend time (`calibrateDripAssistFinish`, ≈ ×0.72) so the shown clock is honestly short. The two compose exactly: the shrunk clock + the 7% render reserve reproduce the bare recipe's pour SCHEDULE unchanged (same bloom, same pour times) and only chop the fictional drawdown tail — proven by `tests/dataflow/drip-assist-drawdown.test.mjs` (the disc cadence `deepEqual`s the bare cadence, total ~0.72×). **Estimate, not measured:** direction owner-confirmed (disc drains ~as poured), the 7% magnitude is a placeholder to firm up with one measured disc brew (timer-promised vs last-drip). **Coverage caveat:** a chat-authored disc recipe (`start_brew`) gets the render-side thin drawdown (it flows through `buildBrewTimeline` with the method) but NOT the recommend-time `targetTimeSec` shrink — so its total clock stays long while its finish is honest. The 33% default for a **bare** V60 is still conservatively padded (the code comment flags ~15–20% as more realistic) — deliberately left as-is this round (owner scoped the fix to the disc only). **Owner rule established here: we do NOT falsify recipes.** The inter-pour gaps are ALSO bare-V60-sized (a bloom+2-pour recipe spreads to a ~95s wait between pours — the owner's "nach Pour 1 nahezu 2 Minuten warten"), but the fix is NOT to rewrite a recipe's cadence — it's to **not OFFER steep-/rest-heavy recipes for the disc**. `selectRecipes({excludeLongWaits})` (set from `dripAssistLocked`) drops any recipe whose DESIGNED wait ≥ `LONG_DESIGNED_WAIT_SEC` (75s), computed from the recipe's own authored `pourSequence` by `longestDesignedWaitSec` (max of any `wait`-action duration and any inter-pour gap), with a fallback to the full set if excluding would empty it. **Researched against the corpus (Aug 2026):** the 45 V60 recipes split cleanly — normal pulse pouring tops out at a ~55–60s bloom rest, then the next tier is 80s+; 75 sits in that gap. Excludes exactly **3**: `kasuya-mugen-flat` (105s draw), `hedrick-bypass-v60` (100s gap), `rao-rule-of-thirds` (80s rest). The five ~60s "drain-fully / steep" designs (`hario-switch-tetsuya` — an immersion-valve brewer mis-tagged v60, `christensen-daily-pour-over`, `gagne-v60-trench-rao-spin`, `wendelboe-v60-big-batch`, `hedrick-flash-brew-iced`) are KEPT at 75s — lower the threshold to 60 to also drop them. Tests: `tests/dataflow/drip-assist-drawdown.test.mjs`.

**The data-flow audit: what was collected and never used (Aug 2026)**
- **Traced every collected field to a consumer.** What holds up: rating + sensory → `extractor`/`patterns`/`brewSignature` → the coach → insight rows → read by `/recommend` AND `/greeting`; measured times → timing calibration; ratings → own-reference recipes + the /taste findings. That loop is real — the consumers were opened and read.
- **The post-rating question was write-only.** `/api/coach-question` asks one question when a rating is ambiguous; the answer was stored as `tasteResult.coachAnswer` and **no prompt builder read it** — while the route's docstring and this file both said it was "read downstream by /recommend + /insights". Now carried by the coach's session formatter and by `buildHistorySummary` (tested) — but note (correction 2026-08-18): `buildHistorySummary` feeds only `loading-insights/refresh`. **Since 2026-08-18 the answer ALSO reaches `/recommend` directly** via `buildMeasuredFeedback()`'s MEASURED BREW FEEDBACK block (route-level wiring pinned by `tests/dataflow/measured-feedback-in-recommend.test.mjs`). It resolves exactly the ambiguity the rest of the line leaves open: "Thin" at 3★ is either sour or weak, and those need opposite corrections.
- **…and it asked the same thing every time.** Two stacked faults, both fixed: (1) "Improved while cooling" — a Yes/No the user just answered — counted as an ambiguity, so it fired on nearly every brew and the model asked about cooling every time; it is no longer a trigger, only context. (2) Nothing stopped a repeat: the remaining signals recur, so a sharp question came back whenever its signal did. The route now loads the already-answered questions, is told asking NOTHING is preferred, and enforces it deterministically (`isRepeatQuestion`, `src/lib/coach/questionRepeat.ts`) because a buried negative is what a model leaks — the Drip-Assist guard exists for the same reason.
- **Four GET routes with no consumer removed:** `news`, `questions`, `hints`, `alerts` — each a thin wrapper over a `src/lib/knowledge/*` getter (`getNews(30)` / `getQuestions()` shuffled to 6 / `getHints()` / `getAlerts(20)` + a `PATCH` → `markRead`). Evidence: grep for `/api/<name>` across `src`, `deploy` (the Ofelia cron config), `.github`, `scripts` and `native` at the pre-deletion commit, excluding each route's own directory — **zero hits**. The plausible consumer, the recipe loading screen, reads the static `COFFEE_HINTS` plus `/api/loading-insights` (a different table). **Deleting rather than reconnecting them was Claude's call, not asked for** — the owner named the four endpoints as a thing to deal with and the removal was proposed on top of that. It is one `git revert` of those four files away: the getters (`getNews`/`getQuestions`/`getHints`/`getAlerts`/`markRead`) are all still in `src/lib/knowledge/`, untouched. **Correction banked:** these four were claimed in-session to have been "unauthenticated" — that was wrong. `src/middleware.ts` gates every path that isn't in `STATIC_PATHS`/`PUBLIC_PATHS`, and none of the four was exempt, so all four required a valid `cf_session` cookie. Removal rests on the one real reason: nothing called them. **Flagged, NOT acted on:** `/api/research` runs every 48 h (`0 6 */2 * *` in `deploy/ofelia.ini`) and WRITES news/hints/questions into the knowledge store; with the read routes gone it now provably writes for nobody — though nothing displayed those rows before the deletion either, so this predates it. Stopping or repurposing it is a product decision. *(Resolved 2026-08-18, owner decisions: the research CRON was removed from `deploy/ofelia.ini` — the route stays but nothing schedules it; and the `webhooks/coffee-alert` receiver + `src/lib/knowledge/alerts.ts` were DELETED — nothing ever read the rows and no delivery path existed. The `coffee_alerts` table remains as dead schema, like `lessons`. `deploy.yml` now restarts ofelia on every deploy so ini changes actually take effect — ofelia reads its bind-mounted config only at startup.)*
- **Still unverified (not claims):** whether the greeting actually names a measured figure, whether the chat sets the disc correctly, and whether typing feels better — all model- or device-side, unreachable from a session.

**The pour analysis stopped overreacting — swirls, and the Drip Assist (Aug 2026)**
- **A swirl is a transient, not a mass event.** Every guard in `flowAnalysis.ts` was built around a disturbance lasting ONE sample: `rejectNonPourJumps` forgave a spike only if the very next sample returned to trend, `heldAt` accepted a target reached if one following sample held it, and overshoot needed one corroborating neighbour. The stored curve is ~2 Hz and lifting/swirling a brewer takes SECONDS, so a real swirl spanned several samples, defeated all three, and was then folded into the baseline as a permanent mass event — dragging the rest of the curve with it. Now the test is physical: **does the reading come back?** Returns within 8 s → replaced by a straight line across the gap (so it also drops out of the reach times and the overshoot, instead of each needing its own spike guard); doesn't return → still a baseline fold, with its own test so the +296.7g bug can't recur. "Returned" additionally requires coming at least halfway back — over an 8 s window plausibility alone tolerates ~124 g, which would have smoothed a mid-size PERMANENT change away (found while writing the test, not before).
- **With the Drip Assist on: show the pour, don't judge it.** The disc is a reservoir — you pour into it, it meters the bed at its own rate, so your hand's rate and the bed's rate are decoupled BY DESIGN and every rate judgement grades the wrong quantity. Owner's call was "show everything, judge nothing": the card keeps Total, Avg pour, Overshoot and the poured grams but drops the verdict colour, the Flow grade tile, Steady/Uneven and the drift line (header reads `measured · disc-metered`); `coachFlow` takes a `rateCoachingOff` flag that silences "Slower"/"Faster" while the grams keep coaching unchanged — water in the disc is water on the scale.
- **The drift line was nagging:** it fired at a flat 3 s, below hand-pouring variance, so it printed on nearly every brew. Now relative to that pour's own intended duration (`intendedPourSec`, half of it, never under 5 s). Tests: `brew-flow-analysis.test.mjs` (12), `brew-flowcoach.test.mjs` (19).

**The corpus outgrew its caps — 184 brews, 63 coffees, 29 roasters (Aug 2026)**
- **The app was still sized for a small log, and quietly threw history away.** Every one of these was a fixed number chosen when the corpus was a fraction of today's: the chat saw **50** of 63 bags (so the oldest unstarred ones had no id and it fell back to a library link instead of opening the recipe — the #413 regression returning silently the moment the library crossed 50), the chat's history was the **5** sessions the client POSTed, `/recommend` got the client's last **100** of 184, and the coach read **150** sessions / **30** of 63 coffee aggregates.
- **Two of those were raised by reading server-side instead of enlarging a payload.** `src/lib/claude/sessionCorpus.ts` `loadRecentSessions()` is one indexed query on `createdAtMs`; `explore-agent` now takes 20 sessions from it and `runRecommendation` takes 400, each falling back to the client array if the read fails. Enlarging the client arrays instead would have re-uploaded every extra session on every turn — the recommend body already carries every recipe of every session. Library cap 50 → 200, coach 150 → 400 sessions / 30 → 80 aggregates, chat roaster priors 5 → 8 and variety priors 6 → 8 (the wider session window feeds them).
- **A bug only volume exposes: timing calibration keyed on the raw method string.** `measuredTimeDelta` + `buildTimingStats` compared `method.toLowerCase().trim()`, so the same brewer arriving as "Orea Classic" (flow picker) and "Orea V4 Classic" (chat) were separate buckets — and the calibration needs ≥2 samples per bucket, so fragmentation switched the learning off rather than degrading it. New shared `src/lib/utils/brewMethodKey.ts` maps a label to its brewer family; **the Drip Assist stays part of the key** (the disc changes flow, so disc and no-disc must not average together) and an unrecognised brewer keeps its own normalised string rather than joining a shared "other". `brewSignature.ts`'s local `normalizeMethod` — which filed ANY disc method under `v60-drip-assist`, so an Orea+disc brew landed in the V60 bucket — now delegates to it (signatures are computed on read, no migration). Tests: `tests/dataflow/brew-method-key.test.mjs`.
- **The user's own well-rated brews are now selectable references in `/recommend`** (`src/lib/claude/ownReferenceRecipes.ts`). Until this, the 136 documented recipes were the ONLY thing a candidate could be `basedOn`; the user's own brews reached the prompt as loose "recent recipes" context, so the single most relevant fact in the app — "this exact bag scored 4.5★ on the Orea Classic, here is the pour plan" — was something the model could read but never build on. A brew qualifies at ≥4★ WITH a reproducible pour plan and real numbers (no plan = a note, not a recipe); one entry per bag×brewer (best rating wins); at most 3, ranked same-bag → same-origin+process → same-brewer; scoped to the brewers the session may use, so a locked method never dangles an out-of-scope brew. Injected next to the corpus block with an explicit framing: a documented recipe is evidence about coffee in general, one of these is evidence about THIS user. **The fidelity guard deliberately does NOT apply** — `resolveReference` matches only VERIFIED corpus recipes and these names never bind to one (pinned by a test, because a corpus rename could create a collision later and silently rewrite the user's own numbers into someone else's recipe).
- **New on /taste: "What works for you" — conditional, in sentences.** First cut was a marginal-average table (temperature / ratio / grind, each averaged over EVERY brew) and it was actively misleading: its rows invited composing the best band from each into one recipe — 89°C, 1:13, 384° Niche, cold and tight and fine at once, a brew nobody would make. A dial only means something CONDITIONAL on the bean, and averaging one across 63 coffees washes the bean out of the number. Worse, the owner's brewing is highly concentrated (148 of ~184 brews at 1:15–1:16), so the "comparison" bands were 7–15 outliers against a block of 148, spread 4.0–4.6 — noise in a bold font. Replaced by `src/lib/taste/brewContextInsights.ts`: segment by origin family × process ("African naturals", "Latin American washed"), then INSIDE one segment compare clear hits (≥4.5★) against clear misses (≤3.5★) — the 4★ middle is dropped on purpose, since splitting a pile of 4s produces two arbitrary halves — and speak only about a dial whose separation beats that dial's everyday scatter (temp 1.5°C, ratio 0.7, Niche 6°, Comandante 1.5 clicks), max two dials per sentence so it can't turn back into a list of knobs. Needs ≥8 brews in the segment and ≥4 on each side. When nothing clears the bar the section SAYS so ("you brew them too consistently for the numbers to disagree") rather than printing four 4.0s. **The lesson worth keeping: a per-dial average across all coffees is an aggregation fallacy — the reader will compose the maxima, and the composition is nonsense.** Tests: `tests/dataflow/brew-context-insights.test.mjs` (mostly assertions that it stays SILENT). **Extended 2026-08-23 (#562):** it now reads the dials the user ACTUALLY turned (`brew.actualTempC` / `grindSettingUsed`, recipe as fallback) instead of the recipe alone, and **water is a first-class separator** competing for the same two clauses — see that Done entry. **The same finding also feeds the daily greeting** (`/api/greeting`, MEASURED CONTRAST block): `formatContextFindingsForGreeting()` narrows the findings to segments that a ★-rotation bag actually belongs to — matched through the SAME `segmentLabel()` the insights were built with, so a finding can never be pinned to the wrong kind of coffee — and prints the figures EXACTLY ("92°C vs 97°C"). The prompt requires naming the figure ("a bit cooler" is worthless; the number is the entire value), forbids rounding/inventing/flipping it, and allows at most one finding per line. Cache key bumped `brewlog.starter.v9` → `v10` so yesterday'"'"'s cached line doesn'"'"'t linger.

**Typing lagged, and the chat lost the Drip Assist (Aug 2026)**
- **Two unrelated reports from one session on the road.** (1) "wenn ich tippe, hört er irgendwann auf zu schreiben, obwohl ich die Tasten drücke" — keystrokes stop landing mid-word. (2) Travelling with no gooseneck kettle: the chat led with V60 only, and the Orea recipe it then wrote arrived on the brew timer with the Drip Assist gone.
- **Latency was the FIELD, not React.** Checked React first and it was clean (a keystroke re-renders only `ChatInput`; `useFieldMotion`'s rAF loop settles and stops). The tell was in the owner's own screenshots: the battery drops 46% → 44% in ~90 s. **Measured** with CDP tracing in a 390×844 @3x Chromium — raster+paint over a 5 s window, medians of 3 interleaved runs: **3904 ms shipped → 413 ms with the discs' `filter: blur(50px)` removed (−89%)**. The blur was ~85% of the app's entire raster cost, on four 96vmax (≈2400 px square at DPR 3) filtered surfaces the compositor re-rasterised continuously. Rendered difference at a frozen animation phase: **mean 3.4/255, max 15/255** — invisible side by side, because blurring a radial gradient that already fades to `transparent 66%` adds nothing. **Two tempting fixes measured and rejected: removing `scale()` from the keyframes (104% — the raster isn't scale-driven) and pausing the Field while the user types (104% — the filter costs the same standing still).** Full numbers + harness traps: `docs/liquid-design.md`.
- **The chat had never heard of the Drip Assist.** The `explore-agent` system prompt didn't mention it once, and the canonical profile stated a gooseneck Stagg as fact — so with "kein Gooseneck" the model improvised. Fixed in the prompt: (a) **a user-stated constraint overrides the profile block for the rest of the conversation** (travelling, no gooseneck, travel grinder — the generalised rule, not a Drip-Assist special case); (b) a **Kettle & pour control** section — no gooseneck ⇒ every pour-over uses the disc, and since it **fits all of his cones (V60, Orea V4, Origami — owner-confirmed)** the brewer is still chosen for the BEAN, with the disc put on it; (c) grind ~5° coarser on the Niche / ~1–2 Comandante clicks (`grindSettings.ts`, direction owner-confirmed, magnitude an estimate); (d) Comandante CLICKS instead of Niche degrees when he's on the travel grinder.
- **Why the disc vanished at the timer:** the brew screen prints `method` verbatim, and the chat's `start_brew` passed `"Orea V4 Classic"` while its prose said "Bloom (Drip Assist)". The prompt + tool schema now require `method` to name the pour-control (`"<brewer> + Drip Assist"`). The plumbing already supported it — `normaliseEquipmentKey` strips the disc token, so `"Orea V4 Classic + Drip Assist"` still resolves to the Orea brewers. `brewIconSrc` did NOT: it short-circuited any "drip assist" string to the V60 icon, so an Orea+disc brew would have drawn a V60. It now strips the accessory first and lets the brewer decide (a bare "V60 + Drip Assist" still lands on the V60 default, so legacy sessions render unchanged).
- **Also fixed:** the brew card's Grind cell truncated "24 clicks" to "24 clic…" — `MiniStat` no longer truncates (a Comandante value is longer than a Niche "388°", and clipping hid the unit), and `stripGrindPrefix` now sweeps the leftover empty parens the chat's "24 clicks (Comandante C40)" form used to leave behind.

**Screens could be dragged sideways and snapped back (Aug 2026, PR #505)**
- **The report:** "some pages are not fixed in the view, I can move the screens and it jumps" (screenshot: the Past Conversations list slid out from under its own header) + "the brew coach / recommendation page with the timer sometimes moves by itself".
- **TWO separate causes, both measured with a headless-browser probe (computed `overflow-x` on every element + driving `scrollLeft` to test real pannability), not guessed:**
  1. **The brew/recommend screens genuinely overflowed.** `CTAWarmth` bled 20% past each side BY DESIGN — ~50px beyond the screen on a phone. Those pixels were never visible, but they made the root scroll container **440px wide against a 390px screen**. Now `-inset-x-5`, exactly the page gutter: edge to edge, not one pixel further (`max-w-[430px]` container, so it spans the container on a wide screen).
  2. **Past Conversations:** a box with `overflow-y: auto` does **NOT** keep `overflow-x: visible` — CSS promotes the visible axis to `auto` whenever the other one scrolls. So EVERY `overflow-y-auto` box in the app was quietly a horizontal scroll container. New `.scroll-y-only` helper in globals.css (`overflow-x: hidden` + `overscroll-behavior-x: none`), applied to the three: past-conversations list, `ChatThread`, `ReferenceCoffeePicker`.
- **THE LOAD-BEARING GOTCHA: `overflow-x: hidden` does NOT mean "not scrollable."** It makes the box a scroll container on that axis and merely hides the scrollbar — WebKit lets a finger drag it and rubber-band back. The real defence is that **nothing may extend past the viewport**. `#app-scroll` already had `overflow-x: hidden` and was pannable anyway.
- **Two tempting fixes that are WRONG here (don't re-propose):** `overflow-x: clip` computes back to `hidden` when the other axis scrolls (verified — the computed value really does come back `hidden`), so it's a no-op dressed as a fix. A blanket `touch-action: pan-y` on the scroll container would stop the drag but **breaks the horizontal chip carousels** (recommend / log / café pages) — `touch-action` intersects down the ancestor chain, so a descendant can't opt back in.
- **Verified:** brew + recommend `#app-scroll` 440→390px and `scrollLeft` pinned at 0; no pannable element left on `/`, `/coffees`, `/taste`, `/cafes`, `/past-conversations`; before/after screenshots differ by ≤5/255 on one channel across 0.7% of pixels (the glow is a hair tighter, nothing moved).

**The chat adds coffees to the library itself + variety/region/roast finally became columns (Aug 2026, PRs #503 + #504)**
- **The complaint:** the owner sent the home chat a photo of two DAK bags (Berry Swirl, Currant Mood). It read both perfectly — roaster, origin, process, tasting notes — then said *"log the bags in your Coffee Library, come back here and I'll set up a coach note for each."* The extraction happened twice and the owner did the typing.
- **The actual cause was NOT the chat's behaviour: there was no door.** A `coffees` row could only be born as a **side effect of `POST /api/sessions`** (`sessions/route.ts:259`). `POST /api/coffees` did not exist; `LightStepScan` only edits a Zustand draft. Nothing in the app could create a coffee without brewing it. (CLAUDE.md's API table had claimed "GET library / POST new coffee" for months — it was wrong.)
- **`POST /api/coffees` (PR #504)** is the missing creator: zod body mirroring the sessions `coffee` sub-object, blend scalars via `deriveIdentitySummary`, `fieldZones` via `mapNotesToZones`, roast date via `guardRoastYear`. Creates with `sessionCount: 0` / `inRotation: true`. **Idempotent** — an existing bag MERGES (fills only empty fields, never overwrites, `created:false`), because the pill's "added" state is local React state and is lost on reload, so a re-tap must be harmless.
- **`add_coffee` chat action:** the chat asks ONCE for what's genuinely missing (roast date above all), then offers one pill — *"Add DAK — Berry Swirl to library"*. One tap creates the coffee AND, when the chat wrote durable parameter-level advice, saves it as that coffee's coach note in the same tap. **That combination is structural, not convenience: a brand-new bag has no id until it exists, so a separate `remember_advice` pill has nothing to target.** The bag photo is attached server-side from the turn's `attachedImageUrl`.
- **THE FRAGILE PART — the id must never drift.** `coffees.id` is a derived `roaster__name` slug, not random; that is what lets a chat-added bag MERGE with the brew logged for it weeks later. The expression was inlined in the sessions route and is now shared as **`src/lib/coffee/coffeeKey.ts`**. Two copies drifting = two rows for the same bag, with no error anywhere. A test asserts byte parity against the original expression (incl. umlauts + ß), and the migration's SQL slug was verified byte-for-byte against the JS one.
- **Migration 0023 (PR #503) — `variety` / `region` / `roast_level` became real columns**, with a backfill from existing sessions (prod: 55 rows → 45/48/49). The owner was rightly annoyed these were missing. Payoff beyond the chat: `/coffees` "Brew this" no longer hardcodes `roastLevel: "Light"` and passes the real variety, so **every** library-started brew now reaches `getVarietyPriorsForBag()` with an actual variety; `/coffees/[id]` falls back to the row when the latest session is a note-less Brew Again.
- **Verified against a throwaway Postgres 16 with the real endpoints** (PIN login, not mocks): add → every field stored, 0 sessions; re-tap → `created:false`, unchanged; merge fills gaps but never overwrites; **brewing the bag afterwards merges onto the same row (still one row) and the variety survives a session that carried none**; headless Chromium renders the library + the zero-session detail page (`Variety SL28, SL34 · Region Nyeri`) with no session in existence.
- **Known, flagged, NOT fixed here:** `/coffees` logs a pre-existing React HTML-nesting warning (`cannot be a descendant of`) — the diff touches no markup there, so it predates this work. Cheap follow-up.

**App icon + home-screen widgets redesigned — saturated "murmuration" field (July 2026, PRs #491 + #492)**
- **Why:** the old icon was a pale washed "blushed" warm gradient (cream→apricot→rose) with the letters on it — owner wanted brighter, on-brand colour, less pastel.
- **Icon (#491):** kept the **BT / TS** caps seal (a touch smaller, cap 300→282, optically re-centred) but swapped the striped warm gradient for a saturated **murmuration field** — a soft diagonal base + large soft colour MASSES pooling in the corners and flowing together (deep magenta lower-left, vivid orange-red upper-right, strong blue lower-right; the cream glow pulled right back), mirroring the live Field's `composeGradient.ts` composition. Anthracite letters. **Design decisions the owner settled on the way (don't re-litigate):** BT/TS caps (a lowercase "Bt/ts" was tried + rejected); the "bigsur" combo (magenta/orange/blue) over berry/grape/sunset alternatives *(superseded for the ICON: it was later re-tinted to "orchid" — pink/magenta/lilac/blue, no orange, owner found the orange too harsh; `generate-app-icon.py` defaults to orchid since. The widgets keep bigsur/berry — see the 2026-08-22 Done entry)*; MAX saturation, not a softer step; and "fields not stripes" (a plain linear gradient was explicitly rejected as "striped" — the murmuration mass composition is the whole point). `scripts/generate-app-icon.py` is the canonical, reproducible generator (variants: bigsur / berry / grape / sunset) and regenerates ALL targets: iPhone AppIcon, **Watch** AppIcon, the PWA/home-screen icon set (`public/icons/*` + `icon-source.svg`), `src/app/icon.png`, and `native/assets/logo.svg` (the Capacitor asset source `npm run assets` reads). Needs `FRAUNCES_TTF` (a Fraunces display TTF) to run.
- **Widgets (#492):** matched the two home-screen widgets (`native/ios/App/BTTSWidget/BTTSWidget.swift`) to the icon — replaced their striped `LinearGradient`s with a reusable SwiftUI `MurmurField` (soft diagonal base + 3 large soft radial colour masses, same composition as the icon). **Rotation widget** (medium) = **bigsur** (ties to the icon); **Scan widget** (small) = **berry** (pink/orange/gold) — a deliberately DIFFERENT flavour combo, since the app's Field is always different (owner-endorsed variety). `ink` switched to anthracite to match the icon letters. Text/layout, deep-links, and the interactive `‹ ›` carousel unchanged.
- **Ship path:** icon touches web assets → merging #491 deployed the PWA icons AND (via a `.github/ios-build-trigger` bump) kicked the iOS TestFlight build; #492 is native-only (`native/**` + `.github/**` are `deploy.yml` paths-ignored, so no web deploy) and its trigger bump kicked a second build carrying icon + widgets together. iOS icon + widgets reach the phone via TestFlight (~15–30 min/build); the PWA home-screen icon is live on deploy (force-quit the PWA to drop the cached old icon). The roadmap's G2 "FINAL SHIPPED STATE" widget-colour lines were updated to the new murmuration palettes (the old rose→peach→coral / raspberry→coral→peach descriptions are stale).

**Origami-wave "full chaos" recipe + the rigid 4 g/s coach — two fixes (July 2026, PRs #488 + #489)**
- **The report:** an Origami-wave recipe with a 115 g bloom held very long, then a single 235 g "Final Pour" labelled ~30 s (the timer promised ~8 g/s), and a settle swirl sequenced AFTER the drawdown ("complete hallucination").
- **Fix 1 — the impossible pour was an IMMERSION-MISROUTE, not a timing bug (PR #488).** `hasImmersionShape()` treated ANY `wait ≥45 s` followed by a water-adding step as a "steep", so a flat-bottom PERCOLATION brew whose long bloom was encoded as a standalone "Bloom Rest" wait rendered through the immersion `StepGuide` — which trusts the model's authored `durationSec` (~30 s) verbatim instead of re-deriving a pourable window. A bloom rest is a pause between pours, not a steep. Now a `wait` signals immersion ONLY when it ENDS in extraction (drain/press/flip/invert/bypass) with NO further pouring after it (order-robust: a real two-stage immersion still trips on its last steep). Verified against the golden corpus — Clever/AeroPress stay immersion, V60 trailing-drawdown stays percolation. **Plus** a deterministic agitation guard `src/lib/utils/agitationGuard.ts` (`stripMinimalAgitationSwirls`, sibling of `dripAssist.ts`) drops swirl/stir/agitate-bed steps from minimal-agitation brewers (Origami/Chemex/Moccamaster, Orea Apex/Open) — killing the settle swirl the flat-bottom brew never wanted AND the one after the drawdown; wired into `/recommend`, no-op for turbulent brewers. Prompt tightened (own commit): percolation `pourSteps` are pours only (no standalone Bloom-Rest/Drawdown wait steps — the timer derives both from `targetTimeSec`), bloom + 3–5 pulses (never one giant final pour), bloom 2–3× dose. Tests: `brew-timeline.test.mjs` bloom-rest golden case + `minimal-agitation-guard.test.mjs`.
- **Fix 2 — the flow coach was HARDCODED to 4 g/s and nagged every legit fast recipe (PR #489).** `coachFlow` targeted the single house constant `POUR_RATE_GPS = 4` for every recipe and flagged >6 g/s as "Slower". A corpus computation (363 pours in 132 recipes: grams ÷ each pour's authored pour time, rests excluded) proves that's wrong — **median 5.2 g/s, p25–p75 = 4.0–6.7, 31 % of all pours >6 g/s**. Kasuya 4:6 pours 60 g in 10 s = **6.0 g/s** flat; Hoffmann V60/Chemex reach 10; only immersion dumps faster. Now the coach derives the target rate PER POUR from the recipe's own authored pour time, threaded `pourSteps → timeline → flowCoach` via the new shared **`intendedPourDurationSec()`** (`pourSequence.ts`): it uses the authored time only when it reads as a real pour (≥ `MIN_POUR_RATE_GPS` = 2 g/s) and falls back to the ~4 g/s house estimate otherwise (a rest folded into a bloom step, or a string recipe with no durations). **So 4 g/s is now the FALLBACK, not the law.** "Slower" fires at >1.5× the recipe's own rate (never below a gentle 4 g/s floor), target clamped to a sane 2–11 g/s band; `expectedGramsAt` ramps over the same intended time. Tests: `brew-flowcoach.test.mjs` — a 6 g/s recipe reads steady (not Slower), still catches >10 g/s, string recipe keeps the ~4 g/s fallback.
- **Honest correction banked:** the 235 g/30 s ≈ 7.8 g/s was *fast* but within a real Origami-wave's range (3–10) — NOT "physically impossible" (an over-claim made mid-session before reading the data). The genuine defects were the single monolithic pour + no drawdown room (#488) and the coach fighting legit fast pours (#489). **Lesson: don't dramatize a number as a hard physical limit before computing it against the actual corpus.**

**Round 3 on the recommendation repetition — the real root cause was the METHOD funnel; final shape is fit-first + freshness tie-breaks, NO bans (July 2026, PRs #486 + follow-up)**
- **Why #480 + #483 didn't fix what the owner sees.** Both rounds rotated *which reference recipes* get injected — but the owner's complaint is the *brewer* ("always V60 and Clever water-first"). Measured with a simulation of the real selection code: at a typical 250–350ml the injected menu contains a V60 recipe AND a Clever recipe every single brew regardless of rotation seed (for some contexts the menu is byte-identical across seeds — the top tie group holds exactly 4 recipes on 4 distinct brewers, so rotating their order changes nothing). Then the SYSTEM prompt's pairing rule ("two candidates = different brewers + different extraction physics", read as percolation + immersion) funnels the immersion slot: at <450ml AeroPress (max 230ml) and Moccamaster (min 500ml) are capacity-forbidden, so **Clever was the only immersion brewer left and won the second slot every brew**; V60 won the percolation slot as the default/most-documented. Three soft "vary your methods" prompt notes did nothing. Second structural truth: the recipe scoring produces WIDE ties (many brewers fit an everyday context equally well), so "best fit" alone genuinely cannot pick a unique winner — something must break ties, and the model broke them by habit.
- **First cut (#486, merged) was a hard rotation BAN — the owner rejected it the same day** ("I don't want anything banned — it should be based on best fit"). Design rule recorded: **best fit always decides; freshness only ever breaks ties. Nothing is ever excluded.**
- **Final shape (follow-up PR):** (a) **system-prompt pairing rule fixed** (`recommendPrompt.ts`): "different extraction physics" explicitly does NOT require a percolation+immersion pairing — two contrasting percolations qualify; house-default pairings (e.g. V60+Clever) are called out as defaulting, the pairing must be re-derived per context. This kills the Clever funnel at its source. (b) **`src/lib/claude/methodRotation.ts` → `buildMethodRecency()`**: brewer families present in ≥3 of the last 6 recommendation sets are named in a `METHOD FIT & FRESHNESS` block — explicitly *not banned*, but they must EARN their slot with a stated fit advantage in `whyChosen`, and equal-fit ties go to the LEAST recently recommended brewer. (c) **menu tie-break** (`demoteBrewers` on `selectRecipes`): those families' recipes are demoted to the back of their EQUAL-score group in the injected menu (never excluded; the score profile is unchanged — asserted in tests), and the note tells the model the library order is meaningful (best-fit-first, freshness-tie-broken). Inactive when a method is locked or for cold brew.
- **Also fixed en route: the #483 "demote recently-seen references" lever was a silent no-op in production.** It matched `basedOn` strings against corpus names EXACTLY, but the model writes short forms ("Kasuya 4:6") vs corpus "Kasuya 4:6 Method — Standard" → nothing ever matched. Now exact-or-containment with a ≥6-char specificity floor (mirrors `resolveReference`). Tests: `tests/dataflow/method-rotation.test.mjs` (fit-first contract: demotion is tie-scoped, nothing excluded, locked/cold-brew inactive, containment matching).
- **Honesty note for the next session:** the freshness levers here are softer than a ban by design. If V60+Clever STILL dominates after this, the escalation path is (1) verify with the recommend-spike harness extended with a method-diversity detector, (2) re-open the tie-break strength conversation with the owner — NOT silently re-adding bans.

**Round 2 on the #480 complaints — rotation was FROZEN, times get MEASURED calibration, swirl-triggered phantom overshoot killed, scan questions answerable again (July 2026)**
- **Rotation (the "same 2 recipes" recurrence).** #480's `rotationSeed = pastSessions.length` never rotated in practice: the client sends `getRecentSessions(100)`, so past 100 sessions the count SATURATES at the cap and the seed froze — identical menu injected every brew. Fix: seed = the LATEST session's `Date.parse(createdAt)` (strictly increases with every logged brew; falls back to the count pre-first-brew). Plus a second deterministic lever: `recentReferenceNames` (the last 6 sessions' candidates' `basedOn`, same source as the prompt note) is passed into `selectRecipes`, and `demoteRecentWithinTies()` moves just-seen references to the BACK of their equal-score group — an equally-good fresh recipe takes the injected slot / per-brewer diversity win. Ties only, a genuinely higher-scoring recipe still leads. Tests: `tests/dataflow/recipe-rotation.test.mjs`.
- **Large-batch times (the "450ml at 3:30 kommt mathematisch nicht hin" recurrence).** #480 deliberately refused to invent a time-scaling slope (Hard Rule) — correct — but left large batches promising a single-cup clock. The honest source exists: the owner's OWN measured brews. New `measuredTimeDelta()` (`historyUtils.ts`): median (actual − target) across past sessions of the SAME method at a SIMILAR volume (±20% or ±60g; ≥2 samples; the method match runs through `brewMethodKey()` since Aug 2026 — an exact string compare split one brewer across "Orea Classic" / "Orea V4 Classic" / "… + Drip Assist" so every bucket sat under the 2-sample floor; the old per-method average mixed single cups in and diluted the batch signal). `calibrateTargetTimes()` in `recommend.ts` raises a percolation candidate's `targetTimeSec` by that measured median (≥+20s only, raise-only, cap +120s; skips iced / cold-brew / immersion). The pour schedule stretches automatically (it derives from `targetTimeSec`); the grind guard stays the extraction lever. Also fixed en route: `isPercolation` never matched "Origami (wave)"-style variant names, so Origami sessions were invisible to ALL timing calibration. Tests: `tests/dataflow/time-calibration.test.mjs`.
- **Phantom overshoot (+296.7g / 66 g/s / "hit 300g 106s early") — the SWIRL case, corrected twice (#483 then #484).** The owner puts the carafe on the scale BEFORE pressing Start (so at Start it's already the baseline), and the false overshoot appears when they **move the brewer for a swirl** mid-brew. **First attempt (#483) was wrong-headed:** it added a LIVE baseline-fold (`isNonPourJump` in `LightStepBrew.pushSample`) that rebased the zero on any physically-impossible jump — but a live stream can't tell a swirl (a transient that RETURNS) from a real vessel change (a jump that HOLDS), so a swirl's ~300g force transient rebased the zero and INJECTED the +296.7g phantom. It also removed the OVERSHOOT tile, which the owner never asked for. **Correct fix (#484):** the live tare baseline is captured ONCE at Start (the carafe is already on) and then stays FIXED — the live path never chases later movements, so a swirl/bump/lift can't be counted as flow. Genuine mid-brew mass events are still handled AFTER the brew by `rejectNonPourJumps()` (`flowAnalysis.ts`), which — unlike a live stream — sees whether a jump returns (swirl → ignore) or holds (real change → fold), plus overshoot's neighbour-corroboration collapses lone spikes. Removed the now-unused `lastRawRef` + `isNonPourJump` import from the brew screen. **The OVERSHOOT tile is BACK** in the Summary Pour-analysis card (accent only when >20g). `analyzeFlow` itself unchanged. Tests: `tests/dataflow/brew-flow-analysis.test.mjs` (10/10). **Lesson: a live sample stream cannot distinguish a transient from a sustained level change — do that reasoning in the post-brew pass, and never let the live baseline chase movements.**
- **Scan clarification questions were visible but UNANSWERABLE.** #471's deterministic Q&A rendered chips + free-text input gated on `msg.chips` — but only region/process carry chips, so the variety/notes/origin questions showed a bubble with NO way to answer. Fix: the answer UI (free-text + "Not sure") renders for every active question; chips additionally when present (`LightStepScan.tsx`). Relevance audit (owner asked): all 5 asked fields land via `applyClarificationAnswer` → `setCoffee` in the coffee draft and are consumed downstream — variety (variety priors in /recommend), tasting notes (Field zones + prompt), process (extraction-budget routing), origin/region (prompt + library) — none obsolete.

**`/recommend` quality: repetitive candidates + wrong large-batch times (July 2026, PRs #480 / #481)**
- **Two owner-reported problems on Mistral `/recommend`.** (1) Recommendations repeated hard across contexts (the Clever water-first showed up in *every* brew). (2) Times didn't work for larger volumes — a 450ml recipe timed at 3:30 that "kommt mit PourFlow nicht hin" (the cone is still full when the timer says done).
- **Repetition — root cause was the INPUT, not Mistral.** `scoreRecipe()` uses a coarse integer scale (roast/process/variety/goal/occasion), so many recipes tie at the top and ties resolved by array order → the same reference recipes were injected every brew and the one-per-brewer diversity portfolio surfaced the same per-brewer winner. Fix attempted (PR #480, 3 levers) — **the rotation lever did NOT work and this line overstated it; see the Aug 2026 entry**: `rotateTies()` rotates WITHIN each equal-score group by a `rotationSeed` (= `pastSessions.length`), which reorders ties but cannot change which recipes the one-per-brewer loop then picks; a `buildRecentRecipesNote()` block lists the last 6 sessions' `basedOn` and tells the model to vary (but not invent); Mistral `temperature: 0.5`. The deterministic guards remain the no-fabrication net. Tests: `tests/dataflow/recipe-rotation.test.mjs`.
- **Times — the FIX IS COARSER GRIND, NOT A LONGER CLOCK (owner's fachlich point: stretching the time = longer contact = over-extracted/bitter).** Root cause: the prompt's "RECIPE FIDELITY" rule (#265) said "keep grind AND time, scale only grams" — right for a +17% bump, wrong for a real scale-up. A 450ml brew off a ~300ml reference kept the single-cup grind + 3:30, so the deeper bed can't drain in time. Fix (PR #480): (a) prompt gains a **BATCH SIZE ADAPTATION** clause — for a batch >~30% larger / ≥450ml, grind coarser ~+20°/doubling of dose (grind-settings.md, the owner's measured scaling), ≤1 extra pour on the reference cadence, and explicitly do NOT stretch `targetTimeSec`; (b) `reconcileToReference()` gains a deterministic large-batch grind guard — ≥1.3× the verified reference water + a single-cup grind → coarsen ONLY the grind to the batch value, time + pour plan untouched. **No fabricated time-scaling slope (Hard Rule):** a universal "X s per 100ml" for pour-over isn't published, so it's deliberately NOT invented — only the documented grind lever is used; the app's timing-history calibration refines the rest. References without published Niche degrees (e.g. Hoffmann V60) fall to the prompt rule. Tests: `tests/dataflow/recipe-batch-grind.test.mjs`.
- **healthsync push: omit `caffeine_ml` instead of fabricating 250ml (PR #481).** A pre-existing commit's last-resort branch invented a volume (espresso 36ml / else 250ml) for a coffee with no real water figure, which fabricated a parameter (Hard Rule) and broke 2 CI tests. Now `healthsyncPush.ts` resolves ml from a REAL figure only (brew water → followed-recipe water) and omits `caffeine_ml` otherwise (the push is skipped for a recipe-less log); the actual reported bug — owner follows the recipe → `brew.waterGrams` empty → every coffee dropped — is still fixed by reading the recipe's water. `node --test` 206/206. (healthsync itself is otherwise undocumented in this file — a one-way caffeine bridge in `src/lib/health/healthsyncPush.ts`.)

**Scan + brew fixes: descenders, flavor input, follow-up questions, pour analysis, English notes (July 2026, PR #471)**
- **Fraunces descenders (`g`/`y`/`j`) still clipped after #467.** #467 zeroed the held `lh-pop-*` keyframe's `filter` but left an identity `transform` (`translate(0,0) scale(1) rotate(0deg)`). On iOS/WebKit a persistent transform — even identity — keeps the inline-block word on a composited layer clipped to its line-height box → descenders cut. Fix: end the keyframes on `transform: none` too (`LiquidHeadline.tsx`). Lesson: for this clip, BOTH filter AND transform must be `none` at rest.
- **Manual flavor entry died after one letter (the "H" chip).** The post-scan "Tasting Notes from Bag" input re-derived the array on every keystroke; the first char made it non-empty and the ternary swapped the `<input>` out for a chip. Fix: a real tag input (removable chips + a persistent input that commits on Enter/comma/blur, never unmounts mid-word) in `LightStepScan.tsx`.
- **Post-scan follow-up questions were random/repetitive and answers "verpuffed".** The vision model free-wrote questions (roast date, harvest year, flavors already entered) and each answer was round-tripped through Haiku to "update the coffee" — answers with no matching field vanished. Replaced BOTH halves with **`src/lib/scan/clarifications.ts`** (pure, deterministic): `buildClarifications()` only asks about fields the scan left empty (variety, tasting notes, region, process, origin — never roast date/score), the client skips any the user already filled in the form, and `applyClarificationAnswer()` maps each answer straight onto its field (no model call → can't vanish; tasting notes MERGE, deduped). Removed the `/api/analyze-bag/clarify` route + the old `getClarificationChips` text-matcher. Tests: `tests/dataflow/scan-clarifications.test.mjs`.
- **Pour analysis showed `+583.5g` overshoot / `56 g/s` / "hit 450g 133s early".** The analysis + live coach compared the RAW Acaia reading to the water targets; an UN-tared scale reads the dripper+carafe+coffee mass (~583g) as poured water, so every target was "reached" at t≈0. Fix (`LightStepBrew.tsx` + `flowAnalysis.ts`): capture a tare baseline at brew start and subtract it everywhere water-vs-target is compared (live coach + stored curve); `analyzeFlow` also defensively subtracts the curve minimum. If the user tared, baseline ≈0 → no change. The top `ScalePanel` keeps the RAW reading (physical-scale mirror), so pour cards show net water while the scale card mirrors the device. Test: un-tared (+583 offset) curve yields the tared numbers.
- **Scanned flavors auto-translate to English** (AI-behavior). Both scan paths — the vision bag scan (`analyzeBag.ts`) and the URL scrape (`analyze-url`) — return `tastingNotesFromBag` in English (e.g. French "Groseille, Rhubarbe, Agrumes" → "Redcurrant, Rhubarb, Citrus"), lining up with the English SCA taxonomy + Field-zone mapping. Still forbids inventing notes. **Retroactive backfill (PR #473, ran on prod 2026-07-03):** `POST /api/admin/translate-flavor-notes` translated the flavour notes already in the DB (`coffees.bag_flavors` + `drip_bags.bag_notes`) — 49 rows, 15 translated, idempotent. Field colours were left as-is (owner's call — the notes→zone mapper reads both languages anyway).
- **Open follow-up (not a bug):** for an un-tared brew the pour cards show water-poured (0→target) while the top scale card shows raw — they differ until Tare. Intentional; flip the ScalePanel to net if the owner asks.

**`/recommend` migrated from Opus to Mistral Large 3 — sovereign EU AI, ≈¼ the per-call cost (June 2026, PR #455)**
- **Why:** real Anthropic usage CSVs showed the bill was **57–63 % Opus**, almost all of it `/recommend`. Moving that ONE call off Opus is the only change that materially cuts cost — a hybrid keeping Opus on recommend would have saved ~12 %. (Don't re-propose hybrid; the numbers killed it.)
- **Validation (3-round spike, NOT guesswork):** `scripts/recommend-model-spike.mjs` (workflow `.github/workflows/recommend-spike.yml`, fired via the `.github/recommend-spike-trigger` bump) esbuild-bundles the REAL recommend prompt (`SYSTEM_PROMPT` + injected corpus) and fires it **byte-identically** at `claude-opus-4-7` vs `mistral-large-latest` vs `claude-sonnet-4-6` across 6 bean profiles × goals × occasions × volumes. Objective detectors: `reconcileToReference` drift (fabrication), staged-temp, schema-valid, **vessel-capacity**, iced `iceGrams`. **Result: Mistral 0 fabrication-drift across 24 samples** (matching Opus), schema-valid throughout, **~$0.05/call vs Opus ~$0.20**. Full results in `docs/recommend-spike-run2.md` + `docs/recommend-spike-run3.md` and **issue #453**. One weakness surfaced: at large volumes Mistral occasionally picked an over-capacity vessel.
- **What shipped:** `src/lib/ai/recommendProvider.ts` is the ONLY place that picks the recommend model — `RECOMMEND_PROVIDER=mistral|anthropic` forces a side, else auto (Mistral when `MISTRAL_API_KEY` present, else Opus, so deploying can't break recommend), and a Mistral error **falls back to Opus per-request**. The prompt is byte-identical for both providers (no `SYSTEM_PROMPT` change). `src/lib/utils/vesselCapacity.ts` + `guardVesselCapacity()` in `recommend.ts` deterministically drop any candidate whose brewer can't hold its `waterGrams` (checked against waterGrams, NOT the drink volume, so iced recipes aren't falsely flagged). Tests: `tests/dataflow/vessel-capacity.test.mjs`.
- **Drip-Assist guard (PR #458):** the Opus→Mistral swap surfaced a real regression — a "V60 + Drip Assist" candidate was recommended unasked. The `/recommend` prompt already bans it (it's the owner's emergency/travel-only brewer, never proactive), and Opus obeyed; Mistral honours that buried negative less reliably. Fixed deterministically with `src/lib/utils/dripAssist.ts` `stripProactiveDripAssist()`, applied right after `guardVesselCapacity` (drop unlocked Drip-Assist candidates; relabel to plain V60 only if that would empty the list; no-op when locked). Tests: `tests/dataflow/drip-assist-guard.test.mjs`. **Lesson:** when migrating off the prompt's tuned model, every *soft* negative instruction in that prompt is a candidate for a deterministic backstop — Mistral leaks the ones Opus held.
- **Env / ops:** `MISTRAL_API_KEY` + `RECOMMEND_PROVIDER` in `docker-compose.yml` + `.env.example`. `deploy.yml` **always-syncs** `MISTRAL_API_KEY` into the VPS `.env` from the GitHub secret on every deploy (PR #457) — strips the existing `MISTRAL_API_KEY=` line, re-appends the current secret, and commits only when the OTHER vars survived (`wc -l > 1`) so it can never clobber the production `.env`. (The original PR-#455 version was append-only and only wrote when no key existed → a **rotated** secret never reached the VPS; that's why the cost first kept landing in the wrong Mistral workspace. Always-sync fixes rotation.) **To re-deploy with no app-code change** (e.g. after rotating the key), bump the root-level **`.deploy-trigger`** marker and merge — `deploy.yml` paths-ignores `docs/**`, `**/*.md`, `.github/**`, `native/**`, so a root file is the deliberate "just deploy" lever (PR #459). **The Mistral key MUST be created INSIDE the cost-separated "BrewLog" Mistral workspace** (switch to BrewLog in the console FIRST, then API-Schlüssel → create) — a key minted while "Default" is active bills the wrong workspace even though `/recommend` is correctly on Mistral. **Rollback = `RECOMMEND_PROVIDER=anthropic`.** This was an owner-approved AI-behavior change (own commit); the spike-validated swap is the sanctioned exception to "don't swap an Opus-engineered prompt".
- **Still on Claude (unchanged):** `explore-agent` (the chat — runs on Sonnet not Opus, AND is a tool-use agent loop = a riskier port for little saving) + all the Haiku endpoints (already ~free). Moving these saves far less.

**Coach (`insights` + `coffeeInsight`) migrated Opus → Mistral Large — LIVE 2026-06-30 (PRs #463 code / #464 docs / #465 deploy)**
- **Status: LIVE on Mistral.** Owner added the `MISTRAL_COACH_API_KEY` secret; the `.deploy-trigger` bump (PR #465) synced it onto the VPS `.env` and the deploy succeeded, so `insights` + the per-coffee card now run on Mistral Large (Opus fallback). No spike was run (unlike `/recommend`) — these are low-volume, low-stakes surfaces and the owner approved the direct swap under the "AI behavior changes: do what the user asks" rule.
- **Why:** owner-requested follow-up to the `/recommend` migration ("Opus ist teuer, lass es uns wechseln"). After `/recommend` left Opus, these two were the only remaining Opus surfaces. Both are single-shot structured-JSON generations (no tool-use loop), so they port like `/recommend` did. The chat was deliberately left on Sonnet (see above). **Expected saving is modest** — the coach runs rarely (cache-aware regen on a new rated session / per-coffee card), not per-brew — but Opus was costly per call and it's clean now. Don't re-pitch migrating the chat; it was the considered, rejected option.
- **What shipped:** `src/lib/ai/coachProvider.ts` — sibling of `recommendProvider.ts`, the ONLY place that picks the coach model. `COACH_PROVIDER=mistral|anthropic` forces a side; else Mistral when `MISTRAL_COACH_API_KEY` present, else Opus (so deploying can't break the coach); Mistral error → Opus per-request. `insights.ts` + `coffeeInsight.ts` route through `callCoachModel`; both `SYSTEM_PROMPT`s byte-identical across providers. Tests: `tests/dataflow/coach-provider.test.mjs` (selection-logic safety).
- **Cost separation:** the coach uses its OWN key `MISTRAL_COACH_API_KEY` in a SEPARATE **"BrewLog-Coach"** Mistral workspace (Mistral isolates usage + billing per workspace — a separate *workspace*, not just a separate key, is what gives the clean split) so coach spend bills apart from `/recommend` (workspace "BrewLog"). Same "create the key INSIDE the right workspace" trap as recommend — switch workspace in the console FIRST, then mint the key.
- **Env / ops:** `MISTRAL_COACH_API_KEY` + `COACH_PROVIDER` in `docker-compose.yml` + `.env.example`; `deploy.yml` always-syncs BOTH Mistral keys into the VPS `.env` (guard raised to `wc -l > 2`). An empty `MISTRAL_COACH_API_KEY` secret → empty line → coach stays on Opus (auto-detect). **Re-activate after a key rotation:** update the secret, then bump `.deploy-trigger` and merge so the key reaches the VPS `.env`. **Rollback = `COACH_PROVIDER=anthropic`.** Owner-approved AI-behavior change (own commit/PR).

**Field "Big-Sur" colour + murmuration motion + blue berries + dark fonts + rotating backgrounds (June 2026, PRs #445 / #446 / #447 / #448 / #449)**
- **Big-Sur intensity (#445/#446).** The Field deliberately held itself back to stay cream-dominant; the owner wanted the opposite (a vivid magenta→blue wallpaper feel). Cranked the six richness dials in `composeGradient.ts` (`BLOB_ALPHA 0.62→0.85`, `BLOB_SAT_BOOST 12→24`, `BASE_DESAT 8→0`, `RADIAL_ALPHA_BOOST 0.12→0.20`, cap `0.9→0.97`, `BASE_DIM 6→3`) + lifted the saturation floors of the vivid zones in `zones.ts`. Applies to every coffee instantly (hues read live, no DB change). Added the cool `cool-berry` zone; a CRON_SECRET admin route (`/api/admin/remap-field-zones`) + the `field-zones-remap` workflow (fired via a `.github/field-zones-remap-trigger` push, since the integration token can't `workflow_dispatch`) re-mapped all 43 coffees so blackcurrant/Kenyan bags carry it.
- **Berries blue, not purple (#447).** `cool-berry` hue 250–290 read purple → retuned to 210–244 (cornflower BLUE). Owner rule: "blue for berries, purple is for grape/plum" — grape/plum stay warm-red in `fruity-deep` (no separate purple zone, by choice).
- **Dark fonts (#447).** `light-muted-foreground` 40 % grey → 16 % near-black (safe — pills use the cream `text-on-dark` token, never grey); `.label-eyebrow` 0.7→0.9; raised the faint `text-light-foreground/40`–`/50` labels on the detail pages.
- **Directional composition + murmuration motion (#448).** Owner: the blobby look was "too crazy bunt"; the Mac wallpaper is 2–3 colours as broad DIRECTIONAL gradients flowing as ONE coherent mass. `composeGradient.ts`: 5 scattered radial hotspots → a diagonal linear base + 2 corner masses + a pale light-ribbon. `FieldBlobs.tsx`: 4 independent drifters → all masses on ONE shared `field-flow` sweep (coordinated, turns direction) + small per-mass `murmur-*` drift. New `curatedFields.ts` (`CURATED_FIELDS` + `getGeneralField()`) cycles the general/home background through elegant 2–3-flavour combos per app-open, wired via `FieldContext.tsx`.
- **Motion-visibility fix (#449).** The first murmuration cut paired huge soft masses with a tiny slow sweep (±8vmax/120s) → optically frozen ("keine Bewegungen mehr"). Opened the travel up (`field-flow` 70s, ±18vmax + ±13° rotate; `murmur` ±13vmax; discs 96vmax/blur 50px) — same coordinated sweep, now legible. Owner-confirmed "Love it." All dials documented in `docs/liquid-design.md`. **iOS:** all web code → reaches the Capacitor shell on deploy, nothing native touched.

**Brew-timer rework: pour-timing math + weight on every step + TIME-based steps + real native screen-on (June 2026, PRs #438 / #440 / #442 / #443)**
- **Lesson (the why — read before touching this screen again).** Active-step selection went through FOUR iterations because each "fix" added cleverness and a new failure mode: #438 weight-gated the step (wait for the poured grams) → a weight DIP jumped it backwards → #440 tracked the running MAX ("peak") to stop that → a tap/swirl FORCE-SPIKE froze into the peak → permanent "Overshot" → #442 median-smoothed the peak → laggy/stiff. #443 DELETED the whole weight-gating apparatus and went TIME-based — the simplest thing — and the owner confirmed it "works beautifully". When a fix needs another fix, step back and delete. (Two more lessons banked from this arc: a stubborn *symptom* can have an upstream root cause — the "flaky Acaia" was actually the screen sleeping, see the wake-lock bullet — and a Capacitor npm plugin in package.json is NOT necessarily linked into the iOS binary.)
- **Pour SCHEDULE math (`buildPourOver`).** The inter-pour window was split EQUALLY, ignoring pour size — a 180 g pour (~45 s at 4 g/s) got the same slot as a 50 g pour and the step flipped before you'd finished pouring (the "200 ml pour disappears after 15 s" bug). Now the time between pours is distributed PROPORTIONALLY to each pour's grams (time follows water). Invariants preserved so nothing else shifts: bloom@0, the final pour still lands at `target − reserve`, total time unchanged, and EQUAL-size pours reproduce the old uniform schedule byte-for-byte. The 0.33 drawdown reserve is NOT changed (no fabricated parameter). NB: this also shifts the haptic/watch step boundaries (`boundariesFromTimeline`) to the corrected times.
- **TIME-based active step (`getActiveIdx`) — the freeze removed (#443).** The active pour step advances purely on the recipe's TIME schedule (`getActiveIdx`, `src/lib/utils/pourSequence.ts`), NOT on weight. Every step shows a LIVE weight box vs its target, so the card needn't wait for the pour — pour slow and you still read what you've poured against the goal on the next card (owner's call). The displayed weight + the live coach read the RAW live `scale.weight` (it drops/spikes naturally, then settles — no peak, no median, nothing to "freeze"); `coachFlow` selects the SAME time-based index (`activeStepAt`) so its numbers match the card. Drain is time-based too. **DELETED in #443** (do not reintroduce): `weightedActiveIdx`, `peakGramsRef`, `settledGrams`, `REACH_TOL_G`, `AGITATION_DWELL_SEC`. The earlier weight-gating/monotonic-peak (#438/#440) and the #442 median each over-corrected (see the Lesson above). The top `ScalePanel` shows the same raw live weight.
- **Weight box + always-on countdown on EVERY step.** Swirl/agitation and drain steps now show the cream `WeightHold` box (live g/s + current/target grams) — same readout the pour card's `CoachCue` already had. The drain card was rebuilt (Now / Drain / total / Hold / "Finish in" countdown). Every active card carries a live countdown — the trailing end-swirl gets a "Drain in" footer so it's never inert/"lost". **No top "ON THE SCALE" pill** (that `LiveScaleTotal`, deleted in #432, was NOT restored — the weight lives inside each card). The swirl button stays.
- **Overtime colour readable on warm Fields (`CircularTimer`).** Timer digits stay anthracite (amber numerals were unreadable on a warm/dark coffee Field); the overtime "Done" button is a SOLID amber fill + cream text. Amber ring + "+over" label keep the signal.
- **Real native screen-on, via an app-local plugin (#443).** Held on mount→unmount (gated to skip the cold-steep branch), not just once the timer starts. The screen-off bug — and with it the "flaky Acaia" — had a hidden root cause: the npm `@capacitor-community/keep-awake` was in package.json but **never linked into the iOS binary** (absent from `native/ios/App/CapApp-SPM/Package.swift`), so its `keepAwake()` was a silent no-op → `useWakeLock` fell back to the Web Wake Lock API → WKWebView doesn't support it → the screen slept → WKWebView JS suspended → the Acaia's 1 s heartbeat `setInterval` paused → the scale dropped. Fix: a new **app-local** plugin `ScreenAwakePlugin` (`native/ios/App/App/ScreenAwakePlugin.swift`) that flips `UIApplication.isIdleTimerDisabled`, explicitly registered in `MainViewController.capacitorDidLoad` + added to the App target via `add_watch_target.rb`'s `APP_SWIFT_FILES` — the same guaranteed-compiled-in pattern as BrewWatch/Widget/LiveActivity. `useWakeLock` calls `window.Capacitor.Plugins.ScreenAwake.keep()/.allow()` (typed accessor, mirrors `brewWatch.ts`), keeping the Web Wake Lock fallback + the visibility/15 s re-assert (iOS clears `isIdleTimerDisabled` on background). Scale/BLE code untouched — it stabilized purely from the screen staying on. **Needs a TestFlight rebuild** (native change), unlike the rest of this rework. See `docs/ios-shell-roadmap.md`.
- **End-swirl reliability (`/recommend` prompt, AI-behaviour, #438).** The corpus has end-swirls (61/136 recipes settle the bed with a swirl/tap before drawdown) and the render pipeline preserves them — but the model writes its own `pourSteps`. Tightened the prompt so the `basedOn` reference recipe's settle swirl/tap is carried into `pourSteps`; minimal/reduced-agitation recipes (Orea/Origami/Chemex/Moccamaster) still emit none. The swirl appears when the recipe calls for one, not always.
- **Architecture reminder:** all of the above is web code → reaches BOTH the iOS Capacitor remote-URL shell AND the Safari PWA the moment the Hetzner deploy finishes (no TestFlight rebuild). The native Apple-Watch app (build 19) only runs the step schedule the phone hands it via `useBrewStepWatch`/`brewWatch.ts`; this work didn't touch that path or the watch binary — only the boundary TIMES changed. **"Acaia connection worse" — SOLVED in #443:** it was the screen sleeping (the keep-awake plugin was never in the binary → JS suspends → the scale's 1 s heartbeat pauses → BLE drops); the app-local `ScreenAwakePlugin` fixed it, owner-confirmed. **Still open / unreproduced:** "chat recipes show no pour guidance" could NOT be reproduced from the code (chat V60 + AeroPress both produce correct guidance through `buildBrewTimeline`); if it recurs, a specific recipe is needed to repro. Tests: proportional-spacing cases in `pourSequence.test.mjs` (the `weightedActiveIdx` cases were removed with the function in #443).

**Brew-from-chat: pour guide + scale coaching + reliable recipe linking (June 2026, PRs #410 / #411 / #413)**
- **#410 — AeroPress/immersion pour guide was blank.** The home chat's `start_brew` handed the brew timer the model's RAW recipe; unlike `/recommend` it wasn't sanitized, so drifted step actions ("Steep"/"Plunge"/"Press") never matched the renderer's exact `BrewStepAction` vocab → the immersion `StepGuide` mis-routed and showed nothing. Extracted the sanitizer into shared `src/lib/utils/pourSteps.ts` (`normalizeStepAction` + `sanitizePourSteps` + `pourSequenceFromSteps`), now used by BOTH `/recommend` (`sanitizeRecipe`) and explore-agent's new `cleanChatRecipe()` (which also derives the `pourSequence` backstop + snaps headline water via `reconcileWaterToPourPlan`).
- **#411 — live Acaia scale coaching now works on AeroPress/immersion too.** `coachFlow` was hard-gated to pour-over (`hasGramsCurve`); dropped that gate so it coaches any step with a cumulative-grams target. `StepGuide` now receives `coach` and renders the same `CoachCue` ("Overshot +Xg" etc.) the V60 view shows — on the water-pour steps; steep/press stay time-only. Only when the scale is connected.
- **#413 — chat wouldn't link a written recipe to the brew (showed a library link / "Brew again").** Root cause: `start_brew` needs the coffee's `id`, but since #247 (June 6) the chat only got ★-rotation bags — a named-but-unstarred bag had no id. Now injects the 50 most-recent library bags (merged with rotation, deduped) so every owned bag is brewable by id, + hardened the prompt so a written recipe MUST use `start_brew` (never `brew_again`/a library link). NOTE: a bag older than the ~50 most-recent AND unstarred still has no id → falls back to a library link; a `find_coffee` lookup tool is the documented follow-up if that ever bites.

**Cold Brew as an occasion + occasion/time rework (June 2026, PRs #396 / #398 / #399 / #400 / #401)**
- **Occasions are now 6:** Morning Ritual, Deep Focus, Social, Experiment, Summer Time, **Cold Brew**. **"After Dinner" removed** (unused). Defined in `LightStepContext` `OCCASIONS`.
- **The standalone Cold Brew page + nav item were deleted** — Cold Brew is an occasion. `src/lib/coldBrew/coldBrew.ts` is kept and **repurposed** as the steep-reminder engine (schedules a single iOS local notification at the finish time via the shell's `@capacitor/local-notifications`; localStorage-persisted).
- **Time buckets renamed Quick/Normal → Normal / Special.** "Special" = a fast shot (≤~150s, the old Quick). Long steeps come ONLY from the Cold Brew occasion, whose Time card is hidden (steep = the time, `timeAvailable: "long-steep"`).
- **Cold-brew recipes** (8, sourced) added/retagged — see the `recipes/` note. Vessel-correct: `cold-brew-jar` brewer for big batches, Clever capped ≤450ml, AeroPress ≤200ml; `/recommend` carries hard vessel-by-volume rules + honours the exact custom volume (e.g. 900ml → a 900ml jar, never a 900ml Clever). Hot-Bloom variant + 20:80-saline finish are prompt-only.
- **Brew step renders `src/components/flow/ColdBrewSteep.tsx`** when the recipe is a cold steep (occasion `cold-brew` OR `targetTimeSec ≥ 3600`) instead of the live pour timer: recipe card (built from the SAME Light primitives as the hot-brew card — `label-eyebrow` + `font-mono-num` 4-up stat grid) → "Start steep" (schedules the reminder + parks a flow-draft snapshot) → big `font-mono-num` countdown ("Xh Ym left") → **"Leave it steeping"** exits Home so you can brew other coffee meanwhile.
- **Parked + resumable:** `startColdBrew` snapshots the whole flow draft into the cold-brew record; `flowStore.resumeColdBrew()` re-hydrates it. The **`NavigationOverlay` shows a live "Cold brew · Xh Ym left / ready — log it" entry** whenever one is steeping — tap to return and log it. One cold brew at a time; resuming replaces the current in-progress draft.
- Recommend page shows steep time in **hours** (`formatDuration`) and a plain method-step list (not the pour-math table); grams/temp on both the recommend + steep cards are finite-guarded so a bad model value renders "—", never `NaNg`.
- **Architecture reminder (why these shipped instantly):** the iOS app is a **Capacitor remote-URL shell** that loads the live web app — so every screen/recipe change here is web code that reaches BOTH the iOS app and the Safari PWA the moment the Hetzner deploy finishes. Only genuinely native capabilities (the closed-app steep notification, BLE, widget, Live Activity) need a TestFlight shell build. (The Apple Watch app was retired 2026-08-29 — see the top Done entry; do NOT re-add it.) See `docs/ios-shell-roadmap.md`.

**Auto-refreshed loading-screen insight agent (June 2026, PRs #342 / #345 / #346 / #347)**
- The recipe-crafting loading screen (`LightStepRecommend`) rotated a static `COFFEE_HINTS` array; now a scheduled agent grows/swaps the pool **monthly, full-auto, no human review**. Because the owner chose no review and "never fabricate" is non-negotiable, a **machine gate replaces the human one**: every candidate — from the verified corpus, brew aggregates, AND live `web_search` — must be grounded in a cited source (numbers + mid-line proper nouns must appear in it), pass `src/lib/insights/loadingInsightLint.ts` + a model claim-check, before insert. Web lines carry a verbatim quote as their grounding; residual web-fabrication risk is accepted for this low-stakes surface.
- `loading_insights` table (migration 0018, **also self-bootstrapped** via `CREATE TABLE IF NOT EXISTS` in the refresh route — the integration can't dispatch the migration workflow, `403`). GET `/api/loading-insights` is defensive (→ seed on any failure); `LightStepRecommend` merges pool + seed, and the seed is the unbreakable floor (instant, offline-safe, never regresses).
- Runs via `.github/workflows/loading-insights-refresh.yml` (monthly cron + `workflow_dispatch`): SSHes in with the **deploy key** and triggers `docker compose exec -T app curl` from inside the container — zero new secrets, reads the container's own `CRON_SECRET`, no Ofelia restart.
- **The Dockerfile now installs `curl`** (`node:20-alpine` shipped without it). This ALSO unbroke the existing Ofelia crons (`/api/research`, `/api/conversations/cleanup`, `/api/coffees/compact`), which had been silently failing on `curl: not found` (Ofelia only Slacks on error, which isn't wired). Don't remove curl.
- Full reference: **`docs/loading-insights.md`** (architecture, the gate, the sources, ops, tuning dials). Tests: `tests/dataflow/loading-insight-lint.test.mjs`.

**Recipe-fidelity system + extraction-budget framework + primary-source audit + deploy fix (June 2026, PRs #265–#278)**
- **Recipe fidelity (two layers).** (1) Prompt rule in `recommend.ts` (#265): when a candidate is `basedOn` a documented recipe, preserve its grind/cadence/temp/total-time and scale ONLY the grams — adding 50ml must never add 1:15. (2) Deterministic backstop `src/lib/claude/recipeFidelity.ts` (#266): `reconcileToReference()` runs after parsing and snaps a drifted candidate's mechanics back to the VERIFIED scaled corpus recipe. Verified-refs only, 0.5–2.5× scale only, skips iced/bypass. Tests: `tests/dataflow/recipe-fidelity.test.mjs`. Root cause was the Kasuya Super-Coarse "+50ml→+1:15 / lost super-coarse grind" mangle (the model rewrote the recipe at generation time; the timing math was innocent).
- **Extraction-budget framework (#268, mirrored to explore-agent #269).** Layer-2 of `recommend.ts` reworked around one dial — *how much input does this coffee need* — with a strict precedence so factors can't contradict: **GOAL > ROAST > PROCESS > FRESHNESS**. Conflict rule for the stale-natural case (process says coarser, age says finer): grind follows the drawdown-time target, temperature + agitation close the gap (temp stays an extraction lever, never a flow one). Fixed two self-contradictions (washed "high solubility" vs needs-most-coaxing; natural pour-count "fewer" vs "5 pours"). Naturals default to fewer pours on clarity/balanced; a body/sweetness goal may use more pours AT a coarser grind (Kasuya percolation-cycles effect).
- **Time buckets.** `LightStepContext` offers **Normal (~3–5 min)** and **Special (a fast shot, ≤~150s)** — renamed June 2026 from the prior Quick/Normal (#273 had collapsed Quick/Normal/Unhurried → Quick/Normal). "Special" = the old "Quick" fast path; long steeps come ONLY from the Cold Brew occasion (which hides the Time card and sends `timeAvailable: "long-steep"`). `recommend.ts` vocabulary matches: `normal` 180–330s, `special` ≤150s, `long-steep` = cold brew (steep = the time); legacy `quick`→special, `unhurried`→slow end of normal.
- **Primary-source recipe audit (#267, #270–#277).** Every named-expert recipe re-verified against the originator's own video/page (owner-supplied transcripts for the YouTube-blocked ones). Corrected the worst fabrications — Gagné AeroPress (80°C low-temp → real HOT 100°C), Rao (equal-thirds "Rule of Thirds" → his spin method; he opposes >2 pours), Hoffmann AeroPress (inverted → UPRIGHT Ultimate), Hoffmann Moccamaster (8:00 → measured 3:30/750g), Hatakeyama (unsourced → real 2024 JBrC 15g:240g/85°C/coarse). Confirmed-correct: Hoffmann V60 1-Cup, Clever, Immersion Iced, Kasuya, Du, Medina, April/Rolf. New entries: Hoffmann Japanese Iced V60 (pour-over), Nemo Pop WAC 2025, Stanica's separate inverted-Melodrip recipe. **NOTE: Stanica won WAC 2024 with the UPRIGHT Flow-Control recipe (`wac-2024-stanica`); his inverted-Melodrip recipe (`stanica-inverted-melodrip`, reference.ts) is a DIFFERENT recipe — both kept, cross-referenced in their notes.** Corpus 125 → 128. Owner-supplied parameters; Niche degrees derived from published Comandante clicks where given.
- **Deploy concurrency fix (#278).** `deploy.yml` had no `concurrency` guard, so two push-triggered deploy runs could race on the VPS git checkout → `cannot lock ref 'refs/remotes/origin/main'` (a red but harmless failure — the parallel run for the same commit deployed fine). Added `concurrency: { group: deploy-hetzner, cancel-in-progress: false }` (queue, don't cancel) + on the VPS clear any stale ref lock and `git reset --hard FETCH_HEAD` instead of `origin/main`.

**Removed all staged-/multi-temperature recipes (June 2026)**
- Staged temperature (cool-bloom-then-hot, descending-temp pours, etc.) needs water at two temperatures during one brew — not practical for everyday brewing. Killed by request.
- **Recipes removed (9):** `wbrc-2022-hsu` (the one that kept surfacing), `wbrc-2025-peng` three-roast staged, `the-peak-staged-temp`, and 6 experimental cold-bloom / multi-temp entries in `markusAdditions.ts`. Corpus 133 → 124.
- **Techniques removed (2):** `staged-temperature` and `three-roast-layering` (the latter only ever exemplified by Peng). 18 → 16. Dangling exemplars re-pointed (melodrip → Wölfl, who genuinely uses one; low-mineral-water → Du only).
- **Prompts scrubbed:** `recommend.ts` + `explore-agent` now carry an explicit "NO STAGED TEMPERATURE — every recipe uses one constant temperature; achieve aromatic preservation via grind/ratio/low-mineral water/minimal agitation" rule, and `pourSteps[].temperatureC` is instructed to be omitted. Variety recipe-pair lists, the grind-reference block, the quick-time list and the expert-name lists all had Hsu/Peng pulled.
- **Docs mirrored:** `coffee-experts.md`, `recipes-full.md`, `grind-settings.md` updated. (Behavioral change — its own commit.)

**Coach workflow + /taste layout + per-coffee insight + brew detail 2×2 (PR #215, June 2026)**
- **Three-action insight workflow.** `insights.status` column (migration 0014) with state machine `new → trying → confirmed` or `→ doesnt-apply`. **Try it** queues a quiet reminder for the next time the user opens `/brew/new` on a matching coffee. **Confirmed** boosts `/recommend` + `/greeting` weight (also bumps `source = 'user-confirmed'`). **Doesn't apply** soft-dismisses and is preserved across regenerations so the same observation isn't re-pitched. The orchestrator (`src/lib/claude/insights.ts`) ONLY replaces `status='new'` rows on regeneration — user-acted rows are kept verbatim, and re-emitted similar observations inherit the existing status (text-match on the first 80 chars).
- **`/taste` page restructured.** Coach section at the top: top 3 `status='new'` insights as cards. When a card is processed (Confirmed or Doesn't apply count as solved — Trying doesn't), the next from the 5–8-deep queue slides in. **"What you brew" is now ALWAYS VISIBLE** — flavor wheel, top flavors, rating trend, body / acidity, best origins / processes / methods. No collapsible. The two-paragraph card layout (observation row, suggestion row, different weights) is intentional for scanning.
- **`/coffees/[id]` single coach card** between the Roaster section and the Personal Notes section. **Rotation-only** (out-of-rotation pages stay clean — no card). *(As shipped in #215 this matched by `citationFields` overlap with the coffee's attributes — that mechanism surfaced other bags' insights on the wrong coffee and was REPLACED by migration 0015: the card now reads the coffee's own `coach_insight` column via `/api/coffees/[id]/insight`. Don't reintroduce overlap matching.)*
- **`/brew/new` Context step quiet reminder.** Read-only card above the selectors, surfaces when the chosen coffee's own coach card is `status='trying'` (same per-coffee source as above — post-0015, no attribute matching). The user already chose Save-to-try; this is the nudge when they reach for that coffee.
- **`/brew/[id]` 2×2 stat grid.** Dose | Grind on row 1, Water | Temp on row 2. Same `Stat` primitive as before. Iced brews still get an extra Ice + Final cup row underneath.
- **Recipe intro widened.** `recommend.ts` prompt: `reasoning` is now ONE substantive sentence (40–60 words) grounded in a named coffee-science principle. Not a headline fragment, not a 6-sentence wall.

**Bag scanner roast-date current-year guard (PR #216, June 2026)**
- The bag scanner consistently parsed month-and-day-only stamps as last year's date (a Friedhats Quiquira bag from 2 weeks ago was logged as 14 months old, cascading through the welcome haiku + `/recommend` freshness + zone classifier).
- **Prompt fix:** `analyzeBag.ts` builds `USER_PROMPT` fresh per call so today's date can be injected. Explicit ROAST DATE RULES section forces the model to use the current year on ambiguous month/day stamps, only drop back a year if that would put the date in the future, never use a "best before" date as roastDate, always return ISO `YYYY-MM-DD`.
- **Defensive post-process:** `guardRoastYear()` — if the returned date is more than 11 months in the past AND shifting it forward by one year lands within the fresh-bag window without going into the future, take the bump. Catches the case where the model still gets it wrong despite the prompt rule.
- **Data fix:** Friedhats Quiquira + Policarpo Yossa Rojos (both in rotation) bumped from `2025-05-18` → `2026-05-18` in both `coffees.latest_roast_date` and per-session `coffee.roastDate` JSONB on 2026-06-02.

**Conversation 1-week TTL + Ofelia cleanup cron (PR #217, June 2026)**
- New `POST /api/conversations/cleanup` endpoint deletes archived conversations whose `archivedAt < now() - 7 days`. Messages cascade-delete via the existing `conversation_messages.conversation_id` FK (`onDelete: 'cascade'`).
- New Ofelia job in `deploy/ofelia.ini` runs daily at 04:00 UTC, same `CRON_SECRET` bearer pattern as `/api/research` and `/api/coffees/compact`.
- **NEVER touches active conversations** (`archivedAt IS NULL`) — that's the live thread on /home; auto-deleting it after a quiet week would surprise the user.

**Deploy workflow hardening (PR #218, June 2026)**
- The deploy after PR #216 failed with `Error response from daemon: removal of container <id> is already in progress` (exit code 123). Root cause: a manual `docker compose up -d ofelia` (run earlier to pick up the conversation-cleanup cron) triggered a compose reconciliation that began removing the app container; the workflow's hand-rolled `docker rm -f` then fired while removal was in flight and the daemon rejected the second rm.
- **Fix:** drop the hand-rolled rm sequence; let compose drive the recreate via `docker compose up -d --force-recreate --no-deps app`. `--force-recreate` handles "container already exists" + "removal in progress" daemon states gracefully (compose serialises with its own teardown); `--no-deps` keeps postgres + caddy + ofelia untouched. Five-attempt fallback retry loop with growing backoff preserved for daemon wedges.

**Security scrub + visibility flip (PR #219, June 2026)**
- Repo was public the whole time without anyone realising. CLAUDE.md leaked the literal Hetzner IP (`89.167.31.219`), the deploy SSH paths, the user's email, and the Düsseldorf location. The "main is branch-protected" claim was also false (GitHub's free plan + private repo can't enforce branch protection).
- **CLAUDE.md scrubbed:** Hetzner IP → reference to `DEPLOY_HOST` GitHub Actions secret. "Düsseldorf tap" → "hard local tap" (chemistry is what matters). Branch-protection claim updated to reflect reality (PR flow is by convention).
- **`deploy/README.md` scrubbed:** same IP replaced with placeholder in three places (server header, ssh example, DNS A-record example).
- **Stale Pages marketing site deleted** from `docs/`: the `index.html` / `about.html` / `how-it-works.html` / `open-source.html` / `the-gap.html` files referenced a Firebase stack that hasn't existed since the Postgres migration, AND they were stored in git as JSON-wrapped base64 (got mangled in a fetch-write roundtrip). GH Pages was disabled in repo settings anyway.
- **Caveat:** the IP still exists in git history from the public period. Real security boundary is the Hetzner SSH key + Caddy + firewall, not the IP being secret.
- Repo flipped private momentarily for the scrub, then back to public. Dependabot alerts + Secret scanning enabled on the Security tab.

**Step-by-step brew timer + recipe names + chat→brew (PR #195 → #200, May 2026)**
- **BTTS chat branding + no pour-arithmetic** (#195): the home chat brands itself only as "Better taste than sorry / BTTS" (no "BrewLog" in replies — internal code/headers unchanged) and is instructed to present verified recipes rather than improvise pour math.
- **Step-by-step, method-aware timer** (#197): the active-brew guide used to stall on "Step 1 of 1" whenever a pour string carried inline annotations (e.g. staged per-pour temps `70 (@70°C) – …`). `parsePourSteps` is now annotation-tolerant; `BrewRecipe.pourSteps` carries structured steps; immersion/AeroPress/inverted/iced route to an action-aware `StepGuide` (setup card, steep countdown, flip/press/drain cue at the right moment); recommend emits structured `pourSteps` (sanitised post-parse, graceful fallback to the string).
- **The no-go — chat reported a wrong grind** (#198): `buildRecentRecipes` (and offline cache, brewSignature, timing stats) read `primaryRecipe` instead of the brewed candidate, so the chat stated the primary's numbers (398° vs the 405° actually brewed). Fixed with one shared `resolveBrewedRecipe()` used everywhere. **Recipe-driven agitation:** the swirl/stir button shows only where the recipe calls for it (no stray swirl on reduced-agitation recipes). **Recipe name** (`title` + `basedOn`) now shows on the brew screen, brew detail, recommend screen, and in the chat context ("Most Recent Brew" block).
- **Chat → direct brew** (#199, fixed #200): the chat's "Brew X" button (`start_brew` tool → `startBrewFromChat`) drops the user straight into the timer with the chat's *exact* recipe — no context, no re-recommendation. Use case: a few grams left, brewed via a one-off chat recipe not worth saving. (#200 fixed the button being a no-op — `start_brew`'s `destination` is set from the tool name, not its input.) ⚠️ The chat-emitted recipe must equal its prose; enforced by prompt only — flag if a mismatch ever appears on the PWA.

**Recipe corpus expansion + selection fairness + flow fixes (PR #190 → #193, May 2026)**
- **Recipe pool 19 → 133** (#191, #192): added `markusAdditions.ts` (51 user-supplied recipes, category "experimental") on top of the corrected championship/reference sets. Source-audit corrections to existing entries (Hoffmann V60/Clever/Iced/AeroPress, Wallgren, Du/Hsu/Wölfl/Peng) against primary transcripts. Niche grind re-baselined to the measured V60 anchors. `docs/recipes-full.md` is the full mirror (all 133 with brew steps).
- **6th goal `aromatic`** (#192): the flow already offered "Aromatic / Floral" but the recipe `Goal` type + `normaliseGoal()` didn't know it (silently fell back to `balanced`). Added to the type/normaliser; remapped 10 iced/cold-bloom/staged-with-cool-finish recipes onto it.
- **Equal best-match selection** (#192): removed the `+1` championship-pedigree and `+0.5` verified bonuses in `scoreRecipe()`. All 133 recipes rank purely on context match. **Method lock:** `context.preferredMethod` hard-filters `selectRecipes` to that brewer (`brewersFromMethod`) and returns the best N FOR THAT METHOD; no lock → one-per-brewer diversity portfolio.
- **Alternative-recipe temp/grind bug** (#193): Brew/Log/Summary/`/brew/[id]` resolved the chosen recipe by method NAME — when two candidates shared a method (common with the method-lock), they always loaded candidate 0's temp/grind. Fix: `BrewLog.selectedCandidateIdx` carries the explicit index through the whole flow (name-match → primaryRecipe fallback for legacy).
- **Drip Assist demoted to emergency-only** (#193): "V60 + Drip Assist" is selectable in the method picker ("no gooseneck? emergency only", maps to v60), but not in onboarding and never recommended proactively. Legacy compat fields kept so old sessions render.

**Offline Brew Mode — re-brew a known coffee without a network (PR #184 + #185)**
- The brew process itself was already client-only (timer, pour guide, tasting log — no network; pour math is local). The two gaps were getting a recipe (the `/api/recommend` Opus gate) and saving (`/api/sessions` POST). Offline mode closes both by **reusing a previously-brewed recipe** and **queuing the save**.
- **Scope:** offline you open a coffee from the locally-cached library, pick one of its **two best-rated** past recipes (auto-used if there's only one), brew with the full timer + pour guide, log, and the save is buffered + auto-synced on reconnect. NO offline AI, NO offline bag scan, NO brand-new coffees offline. Online flow is **unchanged** (KI still generates fresh recipes) — the recipe picker is offline-only, so the validated Opus path is untouched.
- **Cache (`src/lib/storage/offlineLibrary.ts`, IndexedDB):** per coffee, its richest identity + Field zones + up to 2 deduped best-rated recipes, derived from the session feed. Warmed in the background on every online `/coffees` load (full-feed fetch → top-2 per coffee) and refreshed per-coffee on `/coffees/[id]`. Never blocks the visible list.
- **Offline entry:** `/coffees` reads from cache when offline; tapping a coffee opens an **inline recipe-picker sheet on the list** (NOT the detail route — `/coffees/[id]` is server-rendered, so its RSC isn't reliably cached offline, whereas `/coffees` + `/brew/new` are precached). Picking seeds the flow via `startBrewAgainOffline()` and jumps straight to Step "brew", skipping context + recommend. `/coffees/[id]` also has an offline slim view (picker) for the case it IS cached.
- **Save queue (`src/lib/storage/saveQueue.ts`):** `LightStepSummary` enqueues the exact POST body when offline (or on a network throw); success screen says "saved offline — will sync". `flushQueue()` drains it and **never drops a brew** on failure (a stuck save stays visible + retryable).
- **Sync trigger (`ConnectionStatus`, the fix in #185):** iOS Safari PWAs fire the `online`/`offline` events unreliably (so does next-pwa's `reloadOnOnline`), so flushing is driven off `navigator.onLine` re-checked on **mount + `visibilitychange`** (reopening / foregrounding the app reliably fires) — the `online` event is a bonus trigger only. The pill surfaces Offline / Syncing / "didn't sync — tap to retry".
- **Durability:** `flowStore` persists to `localStorage` (was `sessionStorage`) so a mid-brew reload doesn't lose the draft. `NavigationOverlay`'s "New Session" calls `reset()` so a fresh session never resumes a stale draft.
- **PWA:** `next.config.mjs` `fallbacks.document: "/offline"` catches uncached offline navigations. App shell (`/`, `/coffees`, `/brew/new`) precached via the existing `cacheOnFrontEndNav`.
- **Cache caveat:** the offline cache only contains coffees whose library/detail you opened online at least once. A coffee never seen online won't be brewable offline (list empty-state / `/offline` handle it gracefully).

**BTTS Light migration — complete (PR #65 → #137)**
- Every visited surface lives in the `(light)` route group with the BTTS Light theme — Cream background, Fraunces 40 px hero, Chivo body, anthracite foreground, generative Field background.
- Light primitives stack: `LightShell`, `LightFlowShell`, `Field`, `Card`, `Section`, `Footnote`, `Chip`, `Hero`, `CTA`, `CTAWarmth`, `ActionPill`, `ChatInput`, `ChatThread`, `NavigationOverlay`, `StarRating`, `CircularTimer` (fork), `CoffeeBeanGlow` (fork).
- Atomic cut-over (PR #95) renamed `(light)/brew/preview` → `(light)/brew/new` and deleted ~4,300 lines of Dark step code (`Step*.tsx` + `FlowShell.tsx` + Dark `CircularTimer`).
- `[data-light-scope]` CSS shim in `globals.css` adapts shared Dark-era components (`BrewMethodIcon`, original `CoffeeBeanGlow`) via `filter: brightness(0)` so they read as anthracite inside the Light tree without being forked.
- Migrated this arc (Sep–May 2026): `/brew/[id]` (#122), `/coffees/[id]` Field adoption (#123), SessionCard rewrite (#120), `/cafes` + `/cafes/place/[slug]` + `/cafes/coffee/[id]` (#134), `/login` + `/onboarding` (#135), `/taste` (#136). `/cafes/map` (Nearby) is also fully Light — warmed Positron tiles via the `[data-light-scope]` sepia filter — pin to a specific PR pending.
- Cleanup pass (#137): removed `BottomNav`, `RadarChart`, Dark `Chip` (all orphaned post-migration); `loading.tsx` flipped to Light with explicit cream bg so route transitions don't flash dark; `ScrollContainer` simplified (no allowlist, no nav-padding reserve); `--nav-bottom-padding` CSS var dropped.
- PWA chrome aligned (#113–#119): `themeColor: #D4B8C9` (mauve) on viewport + `manifest.json`, `manifest.background_color: #F3E5DC` (cream so the splash matches the Field base), `appleWebApp.statusBarStyle: "default"`, `html { background-color: #F3E5DC }` so the Light cream is the baseline even if the Field gradient is thin at the very top pixels.

**UI polish across the Light surfaces**
- Chip vocabulary unified across SessionCard, `/coffees/[id]`, `/brew/[id]`, `LightStepSummary`, Café Library, Café detail — all static tags now share the `Chip` primitive's default cream-glass look (`px-3 py-1.5`, 12 px, `bg-light-card-default` + backdrop blur, anthracite text). Bordered outline variant was a regression noticed in #124 and reverted in #126.
- Coffee Library card architecture (#127, #132, #133): full-bleed 96 px bag-photo strip on the left, content middle, right column with brew count over the Brew CTA (centered, not edge-aligned). Brew CTA gated on `inRotation` so out-of-rotation rows don't dangle an action they shouldn't trigger (#117).
- Coffee Detail (`/coffees/[id]`) reads its coffee's Field via `useFieldConfig` (#123). Hero scrim flipped to cream-to-transparent (#121) so anthracite titles stay legible against any bag photo. Rotation toggle gates the "Brew this" CTA (#117) — out-of-rotation = bag-not-on-counter, no shortcut shown.
- SessionCard (`/coffees/[id]` All-brews list) rewritten as a Light card (#120): brew method headline + recipe meta (date · dose · water · time) + flavor chips; swipe-to-delete button fades in proportional to swipe progress (#124) so it doesn't bleed through the translucent cream at rest.
- Brew Session Detail (`/brew/[id]`) inherits the cup's Field via `lib/field/cache.ts` (#125) — `/coffees/[id]` pre-warms the cache for all its sessions when it loads, `/brew/[id]` reads synchronously on mount, so navigating between the two paints the same Field with no flash through default. Back arrow routes to `/coffees/[coffeeId]` (#126) instead of the library root, with router.back() as a fallback for legacy sessions whose `CoffeeIdentity` was persisted before `coffeeId` existed.

**Late-May follow-ups (PR #139 → #145)**
- Login wordmark renders as the Home hero ("Better taste / than sorry." in Fraunces 3xl, full anthracite) across all three login states (#139, #140). Login CTAs (Use Face ID / Unlock / Set up Face ID) are pill-shaped (rounded-full + h-14) matching the onboarding primary buttons.
- SessionCard background dropped from 55 % to 30 % cream (#139) so the flavor chips inside (still at 55 %) pop visibly against the card — fixes the white-on-white screenshot.
- Café Coffees-tab cross-link (#141): tapping a coffee that exists in the library routes to `/coffees/[coffeeId]` instead of the café-specific aggregate. Café-only coffees still land on `/cafes/coffee/[key]`.
- Scan edit affordance (#143): the `EditableRow` rows on the bag confirmation step always render with a visible underline + pencil icon. Roaster + Coffee rows always show (were `!== undefined`-gated, so unextracted bags had no editable rows). Lets the user shorten long names like "El Congo by Carlos Montero – Don Eli" → "El Congo" at scan time.
- Orea V4 four-bottom variants (#144): new SVGs `orea-classic.svg` / `orea-open.svg` / `orea-apex.svg` / `orea-fast.svg` in `public/brew-icons/`. `BrewMethodIcon.brewIconSrc` switches on the variant keyword inside the orea branch; legacy "Orea V4 Wide" falls back to Classic. `BREW_METHODS` replaced the single `orea` entry with four kebab-case ids aligned to the `LightStepContext` picker labels.
- **"I've been here" mode (#145)** — `cafe_visits` table + `/api/cafe-visits` + modal on `/cafes/place/[slug]` with binary thumbs rating. Visits appear in the café's timeline alongside brew sessions, and roll into `/api/cafes` so visit-only places appear in the Café Library.
- Database renames (#142, migration 0010 applied 2026-05): all variants of `Rösterei Vier / The Commonage` collapsed to `RVTC` across `coffees.roaster`, `sessions.coffee.roaster` JSONB, and the `roasters` priors cache.

**FlavorWheel Light palette (#128–#131)**
- Direct conversion (no theme prop). The wheel is intrinsically monochrome — `scaFlavorWheel.ts` gives every category a near-identical dark gray, so differentiation comes from icons + label opacity + active/has-sel tonal lifts, not from per-category brand color.
- Canvas → transparent so the Field paints through. Category segments → cream-glass at 55 % (`bg-light-card-default` token), taupe lift for has-selection, anthracite press at 18 % alpha for active. Outer sub-category ring mirrors the same three states at slightly weaker alpha so the inner ring stays primary. Whole-wedge tonal state — tapping a category darkens both rings together.
- Thin cream ring divider between inner and outer rings (#130); divider stroke weights dropped to 0.8 / 0.5 viewport units (#131) to match the label typography weight.
- Icons refactored to `currentColor` with the wrapping `<g>` setting `color`, single icon-path source.

**Generative Field v1.1 (PRs #78 → #100)**
- Each coffee gets its own background gradient composition derived from tasting notes.
- 7-zone perceptual palette (fruity-bright, fruity-deep, floral, nutty-cocoa, spice-earth, sweet-caramel + `cool-berry` — blue-violet/indigo for blueberry/blackcurrant, the one cool zone, added in the "Big-Sur punch" pass); composition stored as weighted JSON in `coffees.field_zones`.
- Haiku call (`src/lib/field/mapNotesToZones.ts`) maps tasting notes → zone weights on first scan.
- `LightFlowShell` rotates Field 25° per brew step (scan 0°, mode 0°, context 25°, recommend 50°, brew 75°, log 100°, summary 125°) — visual progress signal.
- Brew-Again paths (ActionPill on home, `/coffees` list, `/coffees/[id]`) lift `fieldZones` from `coffees.field_zones` so the cup-specific Field travels with the user into the flow.
- Backfill (`scripts/backfill-field-zones.mjs`) ran on production: 23/23 existing coffees mapped (no skips).
- Variety fallback (Phase 4 — derive Field from variety knowledge for coffees without tasting notes) deferred as currently moot.

**Coffee Rotation marker (PR #97)**
- `coffees.in_rotation boolean` column — user-toggleable "currently brewing this bag" flag.
- Star toggle on `/coffees/[id]` (optimistic PATCH).
- Greeting prompt's library snapshot prefixes rotation entries with `★ IN ROTATION |`; `ROTATION DISCIPLINE` block in the system prompt instructs Haiku to prefer rotation bags as the day's invitation.

**Greeting Haiku (PR #87, #96)**
- Time-of-day discipline block in the system prompt (fixed "Late night" at 18:44).
- Library snapshot uses `formatLibraryForPrompt` (with rotation prefix + usage signal) instead of bare roaster+name.
- localStorage cache keyed by `brewlog.starter.v4.<date>.<bucket>` — regenerates 5× per day at tod-bucket boundaries instead of once per calendar day. Bumping the version is the canonical invalidation lever for any greeting prompt change.

**Nearby map split (PR #101) — Light-finished**
- `/cafes/map` (headlined "Nearby") is its own dedicated route, now fully Light. `/cafes` is the tabbed Café Library list (Cafés + Coffees tasted out). `NavigationOverlay` "Nearby" → `/cafes/map`; "Café Library" → `/cafes`.
- Tiles served by Carto Positron, warmed in CSS via `[data-light-scope] .leaflet-tile-pane { filter: sepia(0.4) hue-rotate(-15deg) saturate(0.7) brightness(1.04) }` so they read as cream rather than cold gray.
- Floating header sits on top of a cream→transparent scrim so the page title stays legible without a hard banner edge.
- Fixed flex-collapse bug: Leaflet needs `h-dvh flex flex-col` + `flex-1 min-h-0` to get a non-zero container.

**Core brew flow**
- Full 7-step brew flow: mode → scan → context → recommend → brew → log → summary
- AI bag photo extraction (Claude Vision → Zod-validated session)
- Follow-up clarification step on bag extraction — deterministic + field-targeted (`src/lib/scan/clarifications.ts`, PR #471): only asks about fields the scan left empty, maps each answer straight onto its field (no model round-trip)
- URL-based coffee product page analysis
- Brew timer: circular, pour-over sequence + prose-step guide (AeroPress / immersion etc.)
- Screen wake lock during active brew (`useWakeLock`)
- Bloom duration from roast date (Hoffmann/Rao: 50s fresh / 45s peak / 30s old)
- Pour timing formula: `remaining / (n-2)` — last pour lands at `target - drawdownReserve`
- Proportional drawdown reserve: `targetTimeSec * 0.33`
- **Immersion timer precision** — per-step durations sum exactly to `targetTimeSec`; no absolute timestamps
- **Background-safe timer** — `CircularTimer` uses `Date.now()` anchor; snaps via `visibilitychange` on iOS
- **Step-change alerts** — 2-tone Web Audio cue (880 Hz → 660 Hz) on each auto-advanced step; `navigator.vibrate(80)` on Android
- **"Brew this" entry shortcut (May 2026)** — both `/coffees` (library list) and `/coffees/[id]` (detail) expose a one-tap button that jumps straight to step 3 (Context) with the coffee preloaded. Same `reset → setCoffee → setMode("home") → setSkipScan(true) → setStep("context") → push /brew/new` pattern as the home page's "Brew Again" carousel. Detail page uses the latest scanned `CoffeeIdentity` from sessions; library list synthesizes from the aggregate (roastLevel defaults to "Light"). Server-side, `/recommend` re-hydrates only `commonNotes` + `writtenSummary` from the coffee row (`run.ts`) — the IDENTITY (variety/region/roast) comes from the client, which since migration 0023 reads it off the real columns instead of synthesizing (the old `roastLevel: "Light"` hardcode is gone).

**Data & persistence**
- Session save: Zod validation → Postgres JSONB (null-safe)
- Session GET: single indexed query on `createdAtMs DESC`
- Coffee library with detail pages (rating history, brew signatures, notes)
- Roaster profiles with AI-generated style summaries
- Zustand flow store with localStorage persistence (survives a mid-brew reload — see Offline Brew Mode)

**AI features**
- Brew recipe generation: 2–4 candidates with reasoning (`recommend.ts`)
- Post-brew Escher insights: terrain/pattern prose analysis
- Cross-session pattern extraction (pace, craft approach, occasions)
- Brew signature: weighted averages per coffee/method combo
- Taste profile page with AI-written summary
- Taste-match finder: scores past sessions against current coffee
- **Explore chat coach upgrade (May 2026)** — the chat no longer relies on a hardcoded user profile. (Built on the then-standalone `/api/explore`; that route has since been REMOVED and every one of these injections lives on in `/api/explore-agent`, the home-chat endpoint.) Each turn injects:
  - **Recent recipes block** — `buildRecentRecipes()` shows the actual dose/water/ratio/Niche degrees/temp/target+actual timing/flow/Drip Assist/water source for the last 5 brews, so timing questions get answered with real numbers
  - **Live preferences block** — `loadUserProfile()` reads the `preferences` table; canonical equipment + grind settings live in a separately cached system block that invalidates only when onboarding changes
  - **Coffee library block** — `loadCoffeeLibraryCompact()` lists the last 30 bags with roast-date freshness, so "which bag should I open next?" gets a specific answer
  - **Roaster priors block** — up to 5 unique roasters from recent sessions hydrated via `getRoasterPrior()` so it can reference Friedhats' clarity bias, April's minimal-agitation rule, etc.
  - **BrewLog feature awareness** — system prompt now knows about Match, Taste, Cafés and points the user at them when relevant
  - **Self-aware capabilities (May 2026 follow-up)** — `## Your Capabilities` block in `src/app/api/explore-agent/route.ts` lists every tool the agent has (`search_places`, `fetch_page`, `analyze_image`, `suggest_navigation`) plus voice in/out (ElevenLabs Scribe STT + TTS). When the user asks "what can you do?" the chat answers from the list instead of hallucinating.
  - **Reasoning on internal picks (May 2026)** — Response Style now includes "Show your reasoning when you compare or pick": when the user asks the chat to choose between things they already own (their bags, past sessions, kit), it must briefly name each candidate and what it brings to the criterion before declaring the pick. Prevents the failure mode where "Direct, confident" + "Brevity first" collapsed to a one-line declaration with no explanation.
- **Place search — English DB + diacritic-tolerant fold (May 2026)** — `places.city` is stored in English/ASCII (Cologne, Munich, Dusseldorf, Vienna, Prague, Bucharest, Lisbon, …). Both the home chat (`searchPlaces` in `/api/explore-agent`) and the `/cafes` map (`/api/places` GET) accept any spelling: a `fold()` helper in `src/app/api/explore-agent/route.ts` strips diacritics and collapses German digraphs (`ue→u`, `oe→o`, `ae→a`, `ß→ss`) on both query and DB rows in memory, so "Düsseldorf" / "Dusseldorf" / "Duesseldorf" all match the same row. The chat's system prompt instructs it to translate any German city name (Köln→Cologne, München→Munich) before searching. Map search additionally splits the query on whitespace and ANDs tokens, so "Kolo Berlin" finds Kolo in Berlin.
- Deep-research cron, every 48 h (Ofelia) — currently writes for no consumer, see the `research` API-table row
- Knowledge base: insights, hints, news, questions
- **Structured knowledge layer (May 2026)** — `src/lib/knowledge/{recipes,varieties,techniques}` carries the science/expertise corpus that backs `/recommend` and `/explore-agent`. **Human-readable mirror:** @./docs/coffee-experts.md (mirror only — update the TS files first, then mirror). Replaces ad-hoc recipe paragraphs that were embedded directly in the prompts. **Recipes** = 145 total (8 championship + 92 reference + 45 experimental/user-supplied in `markusAdditions.ts`; the reference set includes 10 OREA V4 Wide recipes in `oreaWide.ts`, 2026-08-31, that took Orea from 1→11 across all four bottoms): WBrC/WAC champions (Kasuya 2016, Du 2019, Medina 2023, Wölfl 2024, Stanica WAC 2024, Nemo Pop WAC 2025), Hoffmann V60/Clever/AeroPress/Moccamaster/Iced (+ Japanese Iced V60 pour-over), Hedrick Flash Brew Iced, Kasuya 4:6, April V60 (Rolf), Gagné, Perger, Rao, Hatakeyama 2024 JBrC, Wallgren Kalita-sieve, Asser Daily Pour-Over (Christensen), plus dozens more — and 8 cold-brew long-steeps (June 2026, occasion `cold-brew`; jar/Clever/AeroPress vessels). (All named-expert entries re-verified against primary sources June 2026 — see the recipes/ note above.) NO staged/multi-temperature recipes in the corpus (every recipe brews at one constant temperature; the cold-brew Hot-Bloom variant is the one prompt-only exception). Each entry: structured pour sequence with per-step durations, attribution, sources, `verified` flag distinguishing canonical vs reconstructed details. Ranked equally on best-match (no pedigree/verified bonus); a locked method hard-filters selection to that brewer (see recipes/ note above). **Varieties** = ~25 WCR-grounded cultivar priors covering Bourbon family, Typica family, Ethiopian landraces, Geisha, SL series, F1 hybrids, Pacamara, Mokka. Genetic / agronomic facts sourced to the WCR Arabica Coffee Varieties Catalog; cup descriptions from Royal Coffee's *Green Coffee Book*. Pink Bourbon flagged per WCR's 2024 finding that it is genetically distinct from Bourbon despite the marketing name. **Techniques** = 25 atomic brewing moves (16 named-expert: Peng Melodrip, Wallgren sieving, Rao spin, water-first, low-mineral water, etc. + 9 general/foundational: bloom, pulse-pouring, immersion-steep, central/spiral/continuous-pour, machine-drip-brew, batch-scaling, flat-bed-pour) cross-referenced to exemplifying recipes — the brain can cite mechanism by id and reach a worked example. Every recipe's `techniques` field references a real id, and every technique's `exemplifiedBy` references a real recipe id (both directions enforced by `tests/recipes/validate.mjs`). All three modules injected per turn; system prompts are NOT touched (cache hits preserved).

**Auth & infra**
- WebAuthn (passkey) auth — register, login, re-enroll
- JWT session cookie via `jose`
- PWA (manifest, service worker, offline)
- Auto-deploy via GitHub Actions → SSH → Hetzner VPS

**Places & cafés**
- Café map with Leaflet, place search, detail pages
- `/cafes` collection: visit count, avg rating, coffees tasted, last visited
- External sessions show "The Brew" / "Would you drink this again?" wording
- Production `places` table: **6,202 rows** (verified 2026-05-09 via `SELECT count(*) FROM places`). The bulk dataset lives **only in the production DB** — no seed file in Git. For a fresh count rerun the same query on the VPS.

**Coffee alerts — REMOVED (2026-08-18, owner decision)**
- The incoming webhook (`/api/webhooks/coffee-alert`) + `src/lib/knowledge/alerts.ts` were deleted: rows were written that nothing ever read, and no delivery path (push/UI) existed. `coffee_alerts` stays as dead schema; `COFFEE_ALERT_WEBHOOK_SECRET` dropped from compose + `.env.example`.

### ❌ Not Done / Known Gaps

**Open items:**
1. `/coffees` "Show only rotation" filter — list shows the star indicator (#117) but no toggle yet to filter the list to rotation bags only
2. Aromatic Goal validation — PR #72 added the intent to `/api/recommend`; it was never sample-validated against a delicate coffee on the deployed PWA. (The old "validate before shipping AI changes" rule that demanded this was retired by the "AI behavior changes: do what the user asks" section — this stays only as an open quality-check idea, not a blocker.)
3. **Cafe visit notes edit UI** — `cafe_visits.notes` column exists but UI only lets you delete + re-add, no inline edit. Cheap follow-up.
3b. **`/coffees` HTML nesting warning — likely stale.** A React `cannot be a descendant of` warning was observed while render-testing PR #504. Re-checked 2026-08-18: today's card markup has the full-card tap `<button>` and the Brew `<button>` as SIBLINGS (page.tsx ~312–411), no nested interactive elements found on the page. Either it was fixed since, or the warning came from elsewhere — re-verify with a render (browser console) before chasing it; drop this entry if it doesn't reproduce.
4. **Branch protection / required checks** — CI now runs on every PR (`.github/workflows/ci.yml`: a `check` job = `tsc --noEmit` + `node --test`, and a `screenshots` job, see CI section below). What's still NOT done is wiring these as *required* status checks — branch protection isn't enabled (GitHub free plan limitation noted in the Git section), so green CI is advisory, not enforced. The PR flow stays by convention.
5. **Drip Assist demoted to "emergency / travel only"** in the user profile, but it's still selectable in `LightStepContext` as one of the brewer options (PR #193 kept it for legacy session render compat). It's no longer at risk of being recommended proactively: as of PR #458 a deterministic guard (`stripProactiveDripAssist`, `src/lib/utils/dripAssist.ts`) strips any unlocked Drip-Assist candidate from `/recommend` output — added after the Opus→Mistral swap leaked one. Not a gap anymore, kept here for the history.
7. **⏳ COST-TRACKING — Opus→Mistral saving, verify over full July (owner wants this followed up).** Grep tag: `COST-TRACKING (Opus→Mistral)`. Goal: a MEASURED June-vs-July comparison at similar brew volume — no guessing (this session burned trust by inferring a wrong go-live date; don't repeat).
   - **Go-live dates (exact, owner-confirmed — do NOT re-summarise as vague "June"):** `/recommend` → Mistral **2026-06-30**; coach (`insights` + `coffeeInsight`) → Mistral this session (deploy 2026-06-30/07-01, PR #465). So the app ran recommend+coach on **Opus for essentially all of June**.
   - **June baseline (Anthropic console, owner screenshots, Opus 4.7-dominant):** two orgs — "default" **$44.92** (Jun 01–24) + "brewlog" **$11.35** (Jun 25–30) ≈ **~$56** on Claude for the month.
   - **Post-switch measured (hard):** Mistral workspaces for a normal **2-recipe day** = "BrewLog" **€0.04** (recommend) + "BrewLog-Coach" **€0.0005** ≈ **~4 cents**. Anthropic **2026-07-01**: Sonnet **$0.67** + Haiku **$0.02**, **Opus $0**.
   - **Honesty caveat (load-bearing):** the Anthropic org total is NOT "app runtime" — it mixes app-recipes (Opus until 6-30), coach, the **chat (Sonnet 4.6, still on Claude — the pink)**, and possibly **Claude Code development** sessions if they bill to the same account. The CLEAN numbers are the **Mistral workspace totals** + **Anthropic filtered by the app's production API key**. Don't post a whole-org "$56 → cents" without that split.
   - **To close:** end of July, pull Mistral (BrewLog + BrewLog-Coach) July totals + Anthropic July (should be ~Opus-free), compare to June at similar volume → the real saving multiple. Defensible per-call framing so far: recommend ~€1/recipe on Opus → ~€0.02 on Mistral (≥10×), no measurable quality loss.
8. **Palette — NOT a gap. The app is already colourful (do not describe it as "cream/bland").** The generative **Field** background paints a saturated, full-bleed **directional** gradient on *every* screen — `src/lib/field/zones.ts` spans fruity reds/oranges (0–30), deep red/burgundy (350–375), floral pinks (320–355), warm caramel (30–50), and the cool **blue** `cool-berry` (210–244). Since the June-2026 Big-Sur + murmuration pass it's cranked to full saturation and composed as a diagonal blend + corner masses + a pale light-ribbon (the "Mac wallpaper" look), flowing via the `FieldBlobs` murmuration sweep. Cream is only the card-glass + base *under* that gradient; the dominant visual is the colour. The app must never be called cream-dominant.

**Permanent gaps**
- Photo uploads: stored under `bags/` — old sessions scanned before this convention have no `bagPhotoUrl`
- Step alerts during background are missed — iOS suspends JS; no workaround without server-push notifications
- Single-user app by design (no multi-user isolation needed)
- Knowledge base needs seeding on new installs: `node scripts/seed-insights.mjs`
- Firebase migration scripts exist but are one-shot: `migrate-firestore-to-postgres.mjs` + `migrate-storage-to-s3.mjs`

---

## Partnership Rules

- **Flag proactively.** If something is inefficient (wastes tokens/time), insecure, or messy process-wise — raise it in the conversation. Don't silently tolerate it. The user is non-technical and cannot spot these issues on their own; it is your job to surface them.
- Examples worth flagging: files stored in odd formats, unused endpoints, duplicated code paths, secrets in the wrong places, stale dependencies, missing error handling at system boundaries, slow API calls that could be cached, confusing UX that you happened to notice while editing nearby code.
- Flag once, explain the trade-off plainly. Per the Operating-mode section, if the fix is safe and revertible just do it and report it in the same breath; only wait for a yes/no when it's genuinely irreversible. Don't hoard issues for a big cleanup later.
- **Always local time (Düsseldorf), never UTC.** Every time you state — a deploy that finished, a cron schedule, "since when is this live", a timestamp out of a workflow log — gets converted to the owner's wall clock before it reaches him (CET / CEST, UTC+1 in winter, UTC+2 in summer). GitHub, Docker and the workflow logs all speak UTC; that is an implementation detail, not something to pass through. Cron expressions are still WRITTEN in UTC because the scheduler demands it — say the local time next to them.
- **Translate, don't jargon-dump.** The user reads everything you write. Plain English, no unexplained acronyms or shorthand. If a technical term is unavoidable, define it inline once.
- **Build everything new on the Design System.** Any new page, component, primitive, or modal MUST compose from the documented Light tokens (`text-light-foreground`, `text-light-text-on-dark`, `bg-light-card-default`, `bg-light-surface`, `bg-light-destructive`, `light-accent-overtime`, `light-scrim`, `backdrop-blur-light-card`, the gutter `px-5`, the primary pill `h-14 rounded-full`, etc.) — see the "Light design tokens (cheat sheet)" section. Never reintroduce a literal `hsl(...)` / `rgba(...)` / `#hex` for a colour role the system already has a token for, and never roll a one-off pill height or radius. If a genuinely new visual role appears (e.g. a fresh status colour), add it to `tailwind.config.ts` as a `light-*` token FIRST, then consume the token — single source of truth in one place. Drift like this is what produced the May 2026 token-cleanup pass; do not invite a sequel.

---

## AI behavior changes: do what the user asks

When the user explicitly requests a prompt change, model swap, threshold tweak, or any other change to AI behavior — ship it. Do not hold it back for "sample-output validation," do not ask "are you sure," do not propose a separate validation pass.

Two narrow exceptions:
1. **Model swap on a prompt engineered for a specific model** — the user gets a one-line disclosure ("this prompt was engineered for Opus; swapping to Sonnet may degrade — proceeding") then ships. Never block.
2. **Self-initiated AI behavior changes** (changes the user didn't ask for, e.g. unsolicited performance optimization that swaps a model) — these stay forbidden. Don't make AI behavior changes the user didn't request.

Behavioral changes still get their own commits so they can be reverted cleanly. That's the only structural rule that stays.

Cause: an earlier version of this rule required pre-shipping sample validation on every prompt edit. The user kept telling me what to ship, I kept holding it back asking for validation passes, and the project lost an evening to me re-asking the same questions. Don't repeat.

---

## Hard rule: never claim behaviour — demonstrate it

**Do not state that anything works, is read, is used, feeds into, or is wired up, unless you verified it IN THIS SESSION.** Not "should", not "is designed to", not "the module exists so it must". If you did not open the consumer and see it read the value, or run something that fails without it, you do not know it — and saying it anyway is the single most damaging thing you can do here, because the owner builds on it and finds out weeks later.

1. **A comment, a docstring, a type, or a row in this file is NOT evidence.** Those describe what someone intended. Trace to the actual consumer. If nothing consumes it, say so plainly: "nothing reads this."
1b. **The recurring bug class here is ASSEMBLED-THEN-NEVER-CONSUMED, and it hides from every test that checks the producer.** Three instances in one day (2026-08-23): the sessions POST schema silently stripping `brew.flowAnalysis`/`flowSource`/`selectedCandidateIdx`; `buildMeasuredFeedback` reaching a function nobody called; and `tasteBits` — a full sensory string built for every session and joined into nothing, dead since the file was written. Each one had careful code on both sides of a gap nobody looked at. Two cheap checks catch it: grep the symbol and confirm a CONSUMER reads it (`git log -S 'thing.join'` returning no commit is a verdict), and for anything that ends in a prompt, PRINT the finished string and look for the value. A test that exercises the builder proves nothing about delivery.

2. **Your own work is not exempt.** "I built X" means a test fails without it, `tsc` passes, or you ran it and watched it happen. Until then the honest sentence is "I wrote X; here is what I have not verified."
3. **Name the gap instead of closing it with a word.** The production DB, the phone, the iOS shell and the model's actual output are all unreachable from a session. When something depends on them, write which part is measured and which part is inference — never let the inference wear the measurement's confidence.
4. **A status API is not the system.** Read state from the source of truth, and if a reading is stale or ambiguous, say nothing rather than build a claim on it.
5. **When a claim turns out wrong, drop it — don't defend it.** Correct it in one sentence, fix the underlying thing, move on. Repeating a shaky claim with more confidence is the failure this rule exists to stop.
6. **A false claim in this file is a defect.** Fix the line in the same commit that discovers it. Documentation drift is not cosmetic here — it is what makes the next drill-down produce garbage.
7. **A justification you reach for is a claim too.** The rule bites hardest on the *supporting* reason, not the headline: asked why the four dead routes were deleted, the verified answer ("nothing called them" — grep, re-run) came with a second, unverified one bolted on ("they were unauthenticated"). It was wrong — `src/middleware.ts` gates `/api/*` and none of the four was exempt, so all four required a valid session cookie. **Reading a route's own file tells you nothing about whether it's public in this app.** One route file was open, the middleware was not. Verify every reason you give at the same standard as the conclusion, and give one checked reason rather than three plausible ones.

Cause for this rule: on 2026-08-16 the owner drilled into four separate areas and found each one hollow, every time because something ASSERTED behaviour that had never been wired. `/api/coach-question`'s own docstring said the user's answer was "read downstream by /recommend and /api/insights" — no consumer existed anywhere; this file repeated the claim. The flow-analysis guards were documented as rejecting scale bumps, but were built around a single-sample outlier and folded any real swirl into the baseline. Four GET routes sat in the API table above as if something read them; nothing did, while a cron kept writing rows for them every 48 hours. And a "What works for you" section shipped that same day averaged each dial across 63 different coffees, inviting the reader to compose the best band of each into a brew nobody would make. The owner's words: *"whenever I drill in, it's always just garbage — I have the feeling that tomorrow I'll find out everything from today is bullshit."* He is right that a claim costs him more than a gap does: a gap he can see, a false claim he only discovers by hand, later, after building on it.

## Hard rule: never infer repo state from partial evidence

Migrations files, seed scripts, `.env.example` and code comments only show what lives in Git. They do NOT show what's actually in the production DB, what was seeded manually on the VPS, or what happened outside the repo. Do not extrapolate.

1. **Never quote a row count, table size, or dataset size from a migration file alone.** A migration showing 33 INSERTs does not mean the table has 33 rows. The bulk data may have been loaded directly on the VPS with no script in Git. If asked, say "I can only see what's in the migrations; for the real count, run `SELECT count(*) FROM <table>` on the VPS."
2. **Search broadly before answering.** Before claiming "X doesn't exist" or "the repo only contains Y": grep across `scripts/`, code comments (real numbers often live there), `meta/_journal.json` (can diverge from the file list — manual `psql` migrations don't register), and any data files (`*.csv`, `*.json`, `*.sql`). Only after that, answer.
3. **Mark inference as inference.** "X lives only in production" is not the same as "I found nothing in the repo that explains X." Stating the first when only the second is true is hallucination. Use phrases like "no evidence in the repo" or "inference, not verified" — not assertive claims.
4. **Flag your own inconsistencies immediately.** If you produce two different numbers for the same thing in one session (12 vs. 13, 33 vs. 34), call it out openly and re-verify — do not silently overwrite the earlier number.
5. **When the user pushes back ("that's wrong, we have X"), do not defend.** Re-open the search, surface the path that led to the wrong conclusion ("I only checked Y, that's why I missed Z"), correct cleanly. Apologize once, fix, move on.

Cause for this rule: claimed "~33 cafés in the places table" based on counting INSERTs in three migration files, when the production DB actually holds 6,202 places (verified 2026-05-09) loaded outside the repo. The Drizzle `meta/_journal.json` only registers `0000_init` — all place migrations are applied manually via `psql` on the VPS, and the bulk dataset has no import script in Git at all. Do not extrapolate from migrations to reality.

---

## Hard rule: never fabricate parameters or facts — research before stating

A "fabricated parameter" is any specific value, number, or product claim stated without a named, in-session-verified source. Zero tolerance:

1. **Recipe parameters** (pour counts, temperatures, target times, dose/water ratios) for any named brewing method or expert (Hoffmann, Kasuya, Wendelboe, Peng, Stanica, Perger, Rao, etc.). If the user asks for "Hoffmann's recipe," **fetch or quote the actual published recipe**. Never reconstruct from memory. Never trust your recollection of a recipe you've seen before.
2. **Hardware facts** (burr type/size, dial scale conventions, click counts per turn, ppm specs, brewer geometry). Look up the official spec before stating it. The Niche Zero has 63 mm Mazzer **conical** burrs, NOT flat — do not invent geometries to explain observed differences.
3. **Quantitative extrapolations.** Do not scale grind clicks by an invented slope, do not interpolate temperatures, do not estimate timings, do not "approximate" recipe shifts when the user scales up or down. If you do not have a published slope from a named source, say so and ask the user to measure empirically.
4. **The codebase is NOT a source.** A number existing in a `*.ts` file is downstream of real sources. If a codebase value disagrees with the original publication, the codebase is wrong — not the publication. Validate against the brewer's blog, video, or competition publication before treating a codebase value as authoritative. (This includes `src/lib/knowledge/recipes/*` — those entries were transcribed once and can be transcribed wrong.)
5. **Aggregators are NOT primary sources.** Recipe aggregators (timer.coffee, honestcoffeeguide, fluentincoffee, ECT brew demos, etc.) are useful as **index pointers** — they often link to the originator's actual video or blog. They are never sufficient on their own to set `verified: true`. The originator's own publication (their YouTube channel video with timestamp, their blog post URL, their book with edition/page) is the only valid primary source. Aggregator transcriptions degrade — they paraphrase, round, or substitute (timer.coffee mapping Hoffmann's "freshly boiled" to "95 °C" is a documented example).
6. **`verified: true` means content-cross-checked in-session.** Not "the citation sounds attributed enough," not "the source name looks right." If you cannot retrieve the primary source and confirm the parameters match in this session, the entry stays `verified: false` until someone can. YouTube blocks WebFetch — that is not a reason to mark something verified; that is a reason to keep it unverified and note the constraint.
7. **Peer-data audit when adjacent data is changed.** When fixing one recipe / variety / technique entry, audit the **peer entries by the same author or in the same cluster** at the same time. If you rewrite Hoffmann's V60 1-Cup against his actual video, you have inherited a working source-verification process — apply it immediately to his AeroPress / Clever / Moccamaster / Iced entries before they bite. Do not "fix one and walk away" when the same fabrication pattern is sitting one entry below in the same file.
8. **Retroactive audit when a Hard Rule is enacted.** When a Hard Rule is added, ALL pre-existing data in scope falls under it retroactively. Schedule the retroactive sweep with the rule, not weeks later. If you add a new Hard Rule, run the cross-check of every existing entry before closing the commit — discovering the gap weeks later means weeks of downstream consumers built on wrong data.

When in doubt, say **"I don't know — let's measure"** or **"let me look that up first."** Do NOT estimate, approximate with "~", or hedge with "around" — those are hallucinations wearing humility costumes.

Cause for this rule: in the May 2026 Niche ↔ Comandante calibration session, fabrications cumulatively contaminated the empirical baseline the user was trying to establish: (a) invented a "flat vs conical burr geometry" difference between the Niche Zero and the Comandante to explain visible grind-distribution differences — both grinders use conical burrs, the difference is burr size + RPM, (b) reconstructed Hoffmann's *A Better 1 Cup* V60 recipe from memory with wrong pour count (2 large pours instead of 4 pulses), wrong temperature (92 °C instead of "freshly boiled" for light roasts), and wrong total time target (3:30 instead of ~3:00) — and then trusted the matching wrong values in `src/lib/knowledge/recipes/reference.ts` instead of validating against Hoffmann's published recipe, (c) proposed quantitative grind shifts ("~395°", "~25 clicks", "+5–10° coarser per scale-up", "+2 clicks for a doubled brew") with no source behind any of them. The user spent an evening calibrating against a recipe that was not actually Hoffmann's, contaminating the data the codebase update was supposed to be grounded in. Do not repeat. If you cannot cite a source for a number, you do not have the number.

Cause for sub-rules 5–8: a follow-up audit of all 19 named-expert recipe entries in the codebase found that 18 had at least one parameter disagreeing with the originator's actual published recipe, and 8 had parameters so far off they were "different recipes wearing the original author's name" — Gagné's AeroPress was attributed as low-temp/long-steep when his actual published recipe is hot + long; Rao's "Rule of Thirds" was a three-pour recipe attributed to him when Rao publicly opposes three-pour V60 patterns; Peng's 2025 WBrC ratio was logged as 1:4 when his actual brewing ratio is 1:14; Stanica's championship location was Bucharest when WAC 2024 was in Lisbon; Wölfl's 2024 championship city was Copenhagen when WBrC 2024 was in Chicago. Every one of these errors would have been caught earlier if (a) peer-data had been audited when adjacent recipes were touched, (b) aggregator transcriptions had not been silently promoted to primary status, and (c) a retroactive sweep had run when the Hard Rule was added. Do not repeat.

---

## Conventions

### Code
- **TypeScript strict** — no `any`, no `@ts-ignore` without comment
- **Tailwind only** — no inline styles except `safe-area-inset-*` (and the gradient exception below)
- **Nothing may extend past the viewport, and `overflow-y-auto` needs `.scroll-y-only`.** Two traps that both let a finger drag a screen sideways (PR #505): (a) `overflow-x: hidden` does NOT mean "not scrollable" — the box is still a scroll container on that axis, so any decorative bleed past the screen edge becomes draggable in WebKit; keep bleeds flush with the gutter (`-inset-x-5`), never beyond. (b) A box with `overflow-y: auto` does NOT keep `overflow-x: visible` — CSS promotes the visible axis to `auto`, so every new vertical scroller must also carry the `.scroll-y-only` helper. Don't reach for `overflow-x: clip` (computes back to `hidden` when the other axis scrolls) or a blanket `touch-action: pan-y` (breaks the horizontal chip carousels).
- **Tailwind only scans `src/{app,components,pages}`** — `tailwind.config.ts` `content` paths do NOT include `src/lib/**`. Utility class strings that live solely in lib (including arbitrary values like `bg-[linear-gradient(...)]`) are silently never generated; the styles vanish at runtime with no error. If you need a shared visual constant in lib, export it as a raw CSS value and apply via inline `style={{ background: ... }}` at the call site — see `src/lib/theme/gradients.ts`. PR #40 fixed a regression where the /explore user-message cream pill never rendered because of this trap.
- **No external UI libraries** — every component is bespoke
- **Refs over state** for values that don't need to trigger renders (timers, wake lock, callbacks)
- `useCallback` deps must be accurate — don't omit to silence linter
- **Zod schemas** on all API POST routes; strip nulls with `deepStripNulls()` before parsing
- **Never import from `app/api/*/route.ts` in client components** — Next.js App Router enforces a strict server/client boundary. Shared types go in `src/lib/types/`.

### Database (Postgres + Drizzle)
- **BEFORE any SQL migration or UPDATE/DELETE: run a COUNT query first.** Verify the number of affected rows is what you expect. Never write a broad WHERE clause (or no WHERE clause) without checking the row count first. If the count is surprising, stop and ask.
- **Never reset or wipe existing data to fix a single row.** Target the specific row by id or a precise unique condition. Resetting a column for all rows with `address IS NOT NULL` when only 1 row needed fixing is not acceptable.
- JSONB columns for nested objects (coffee, brew, result, etc.) — preserves TypeScript types unchanged
- Session timestamps: `createdAt` (timestamptz) + `createdAtMs` (bigint, indexed DESC for feed order)
- Upload paths must start with `bags/` or `uploads/` (enforced in upload route)
- Numeric fields (ratingSum, avgRating, cuppingScore) stored as `numeric` in Postgres, use `String()` when inserting

### AI models
- **`claude-opus-4-7` — recommend (`/api/recommend`). BACK ON OPUS Aug 2026** (owner's call). `src/lib/ai/recommendProvider.ts` now DEFAULTS to Opus; Mistral is opt-in via `RECOMMEND_PROVIDER=mistral` (the key stays in the env so flipping back is one variable, not a re-key — pinned by `tests/dataflow/recommend-provider.test.mjs`, because the old rule selected on key-presence and would have made the rollback a silent no-op). **Why:** three prompt instructions leaked in two days on real brews — the banned brewer (#458), the Drip Assist grind offset (#517), the grinder UNIT returning Niche degrees for a Comandante (#518). All three are now deterministic guards, but the pattern decided it: guards only catch rules someone already thought to write. **What this does NOT rest on:** the #453 spike measured FABRICATION (0 drift/24 samples), never instruction-following or repetition — so this reverses a decision whose evidence never covered the failures it is being reversed for. Judgement, not measurement. **Repetition will NOT improve from this** — it is input-side (coarse integer scoring → wide ties → order decides), see PRs #480/#483/#486. *(Correction, 2026-08-21: "input-side" was right about the SCORING but incomplete about the input. The biggest single input was the SYSTEM PROMPT's own fixed menu of a dozen numbered recipes — cached, byte-identical, and therefore the most salient thing in the call whatever the rotated library said. That is now deleted; see the Done entry.)* Cost returns to ~$0.20/call from ~$0.05. **`callAnthropic` sets NO sampling parameter, and must not — `claude-opus-4-7` 400s on `temperature` (\"deprecated for this model\", `x-should-retry: false`). #541 set `0.8` and killed every recipe request for two days (#551); pinned by `tests/dataflow/recommend-request-shape.test.mjs`, which asserts the real serialized request body. Mistral's `temperature: 0.5` is fine — that path accepts it.** History of the migration below, still accurate for how the provider module works: migrated off Opus June 2026 (≈¼ the per-call cost; spike-validated, 0 fabrication over 24 samples — see the Done entry + issue #453). Routed via `src/lib/ai/recommendProvider.ts` (the ONLY place that picks the recommend model): `RECOMMEND_PROVIDER=mistral|anthropic` forces a side, else Mistral when `MISTRAL_API_KEY` is set, else Opus; a Mistral error falls back to Opus per-request. `SYSTEM_PROMPT` unchanged — both providers get byte-identical input. Mistral call runs at `temperature: 0.5` (anti-repetition, PR #480); Opus fallback uses its default. Capacity guard in `src/lib/utils/vesselCapacity.ts` + `recommend.ts`; large-batch grind guard + tie-rotation + recent-recipes note in PR #480.
- **Coach** — insights (coach observations, `src/lib/claude/insights.ts`) + per-coffee coach insight (`src/lib/claude/coffeeInsight.ts`, also behind `/api/admin/prewarm-coffee-insights`). Migrated Opus → **Mistral Large** June 2026 (PR #463) via `src/lib/ai/coachProvider.ts`: Mistral when `MISTRAL_COACH_API_KEY` (separate "BrewLog-Coach" workspace) is set, else `claude-opus-4-7`; `COACH_PROVIDER=anthropic` rolls back; auto-fallback to Opus on a Mistral error. NOTE: recommend's + the coach prompts were engineered for Opus; their Mistral swaps were owner-approved exceptions — do NOT swap any OTHER Opus-engineered prompt without the one-line disclosure per the "AI behavior changes" section.
- `claude-sonnet-4-6` — analyze-bag, explore-agent, escher (post-brew insight helper), coach-question (post-rating micro-dialogue), loading-insights/refresh (the insight agent's generation + claim-check calls)
- `claude-haiku-4-5` — brew-insight, taste-summary, research (dormant), analyze-url, coffees/compact, roasters/generate, translate (tasting-notes ↔ SCA helper), scan/translateNotes (the flavour-note backfill), greeting (daily starter), field/mapNotesToZones (notes → Field zones), loading-insights/refresh (one helper call)

### Git / Deploy
- **"Done" means shipped** — merged to `main`, auto-deploy runs on Hetzner, live on the iPhone PWA. "Pushed to a feature branch" is NOT done. Stopping at a branch leaves the user staring at the still-broken app, re-reporting the bug, and re-fixing what is already fixed. That is chaos and it is not acceptable.
- **Workflow is PR-based.** Every change goes: feature branch → PR → squash-merge to `main` → auto-deploy. Use the GitHub MCP tools (`mcp__github__create_pull_request`, `mcp__github__merge_pull_request`, `mcp__github__enable_pr_auto_merge`). The PR flow is **by convention** — branch protection is not currently enabled, so direct pushes to `main` are technically possible but should never be done.
- **Auto-merge is enabled on the repo.** For PRs that pass CI without review gating, call `enable_pr_auto_merge` (mergeMethod: SQUASH); GitHub merges as soon as checks go green. If checks are already clean, call `merge_pull_request` directly.
- **Poll GitHub through the MCP tools, never `curl` the API.** There is no `GITHUB_TOKEN` in the session environment and api.github.com is not reachable unauthenticated from here, so a `curl … | jq`-style wait loop returns an empty body forever and simply never fires — on 2026-08-18 that made a deploy look like it was still running 100 minutes after it had gone green in 2m38s. `actions_get(get_workflow_run)` is the authenticated read; use it directly rather than backgrounding a shell poller.
- **Read CI state from the RUN or JOB endpoint, never from `check_runs`.** The commit's `check_runs` list lags by minutes and shows a finished job as still queued/in-progress — on 2026-08-16 that made a `screenshots` job get called "hanging", then "effectively dead", then retracted, three times, for a job that had completed in 2m16s. The reliable reads are `actions_list(list_workflow_runs)` → the run's `status`/`conclusion`, and `actions_get(get_workflow_job)` → per-step `conclusion` + `started_at`/`completed_at`. Both are also how to confirm a deploy actually landed (`Deploy` workflow, job `deploy`, step "Deploy to Hetzner VPS"). A merged PR is not a deployed PR — check the run.
- **After a squash merge, the local branch is stale.** The squash lands as one NEW commit, so the branch it came from shares no commit with `main` and further work on it conflicts on the next PR. Restart it: `git checkout -B <branch> origin/main` (cherry-pick anything not yet merged). Two conflicts on 2026-08-16 came from committing onto an already-merged branch.
- **Session-level harness instructions are compatible.** If a system prompt tells you to develop on a feature branch — fine, that's the actual flow. Just don't stop at the branch: open the PR, merge it, confirm `main` advanced.
- **Auto-deploy** — GitHub Actions runs on every push to `main`. No manual steps needed unless the action fails (fallback: SSH to VPS and run docker compose manually per the Infrastructure section).
- Commit message: imperative, lowercase prefix (`fix:`, `feat:`, `remove:`, `docs:`, `build:`)
- Always `npx tsc --noEmit` before commit
- No staging environment — once merged to `main` it is live within minutes

### Light design tokens (cheat sheet)
- **Tokens:** `text-light-foreground` (anthracite `hsl(0 0% 14%)`), `text-light-muted-foreground` (now near-black `hsl(0 0% 16%)` — darkened from 40 % grey in June 2026 so secondary text reads on the saturated Field; pills never use it, they use the cream `text-on-dark`), `bg-light-card-default` (cream glass 55 %), `bg-light-card-selected` (warm taupe), `bg-light-surface` (opaque cream, modal/sheet surfaces), `border-light-foreground/15`, `shadow-light-card-pressed`
- **Cream highlight on dark elements:** `text-light-text-on-dark` (single token; replaced the two pre-token literals `hsl(36 55% 96%)` and `hsl(30 40% 97%)` so CTA / ActionPill / ChatInput buttons / ConnectionStatus all share the same cream)
- **Card variants:** default cards use `bg-light-card-default` (55 %); SessionCard (chips inside) uses `bg-[hsl(36_55%_96%/0.30)]` — the lower opacity so child chips visibly contrast
- **Destructive (delete, error):** `bg-light-destructive` (warm rust) — `text-light-destructive` for error copy
- **Amber accent (overtime):** `light-accent-overtime` — used by `CircularTimer` when elapsed > target; inline SVG strokes mirror the value via two module-level constants `ANTHRACITE` / `OVERTIME` since SVG strokes can't read Tailwind tokens
- **Floating-chrome elevation:** `shadow-light-float` — warm-dark soft outer lift (distinct from the inset `shadow-light-card-pressed`). The home chrome is **all-dark + lifted**: the Burger, the `+`/clear/cancel round controls, the chat input bar, and the Action Pill are solid `bg-light-foreground` + `text-light-text-on-dark` (icons/text) + `shadow-light-float`, so they float above the living Field instead of muddying into it as glass. In-bar buttons (send/mic/remove-X/coffee chip) invert to cream-on-dark (`bg-light-text-on-dark text-light-foreground`). The same dark+lift swap is on the `h-11 w-11` header buttons across every `(light)` route + the NavigationOverlay close. Detail-page photo-hero buttons + pop-over menu *surfaces* (AttachmentSheet etc.) stay cream glass — they get only the lift.
- **Glass blur:** `backdrop-blur-light-card backdrop-saturate-150` — the canonical pair on every glass surface (Card, Chip, ChatThread, AttachmentSheet, NavigationOverlay sheet, ReferenceCoffeePicker, ConnectionStatus). Don't write `backdrop-blur-[14px]` — it bypasses the token. (NOT the home chrome anymore — Burger / `+` / chat bar / Action Pill are now solid-anthracite floating chrome, see above.)
- **Photo scrim:** import `gradientCreamScrim` from `@/lib/theme/gradients` for the cream→transparent vertical fade overlaid on bag photos (consumed by `/coffees/[id]`, `/brew/[id]`, `/coffees/drip/[id]`)
- **Hero question:** `font-fraunces font-semibold text-[40px] leading-[1.05] tracking-[-0.01em]`
- **Headline (route title):** `font-fraunces text-3xl text-light-foreground leading-none`
- **Wordmark (Home + Login):** `<h1 className="font-fraunces text-3xl leading-[1.05] text-light-foreground">Better taste<br />than sorry.</h1>` — exact same markup at both entry points
- **Eyebrow:** `text-light-muted-foreground text-xs tracking-widest` uppercase (no `font-medium` — that's an outlier)
- **Chip / tag (two sizes — `Chip` primitive `size` prop):** base `inline-flex items-center rounded-full font-medium leading-tight backdrop-blur-light-card backdrop-saturate-150 bg-light-card-default text-light-foreground`. **`default`** = `px-4 py-2 text-[13px]` for a pick-one chip that IS the question's primary control (Sensory rows, SensoryToggle Yes/No, grinder picker). **`sm`** = `px-3 py-1.5 text-[12px]` for multi-select tag rows, dense pickers, card-footer actions, secondary inline confirms (FlavorWheel picks, candidate role picker, country/process/roast pickers, Coach actions, onboarding equipment multi-select). Rule: stands alone as the answer → `default`; one of many / dense / secondary → `sm`. Never mix the two on one row.
- **Selected state = anthracite FILL (Aug 2026).** A *selected* `Chip` / `Card` (and the inline method pickers in `LightStepContext` + `LightStepLog`) is `bg-light-foreground` + `text-light-text-on-dark` + `shadow-light-float` — the CTA / Action-Pill language, a figure-vs-ground flip against the Field. Replaced the old warm-taupe glass (`bg-light-card-selected` + `shadow-light-card-pressed`), which read too close to the cream-glass default over the saturated Field (owner report). `Card` carries a `group` marker so `CardTitle`/`CardSubText`/`CardIcon` flip their own colour to cream via `group-aria-pressed:text-light-text-on-dark[/opacity]`; **any custom (non-primitive) child rendered inside a selectable `Card` must add that variant itself** or it stays anthracite-on-anthracite (the custom-amount input in `LightStepContext` does). A selected row carrying a `BrewMethodIcon` (PNG art inverted to anthracite by the `[data-brew-method-icon]` shim) flips back to the white original via a higher-specificity globals.css rule (`[data-light-scope] [aria-pressed="true"] [data-brew-method-icon] { filter: none }`). The `bg-light-card-selected` taupe token is NOT removed — it still styles non-button surfaces (the "Drip bag" badge, the saved `CoachCard` surface, the active brew-step "Now" cards, `active:` press feedback).
- **Primary CTA pill:** `w-full h-14 rounded-full bg-light-foreground text-light-text-on-dark font-semibold` + `active:scale-[0.98] transition-transform` — both `h-14` AND `rounded-full` are required; `py-3.5 rounded-2xl` is NOT a CTA, it's a card
- **Page gutter:** `px-5` (20 px). Don't use `px-8` — it's not a defined gutter width
- **Light scope marker:** `[data-light-scope]` attribute set by `LightShell` wraps the whole `(light)` route group; the `globals.css` shim catches inline `var(--card)` etc. for un-migrated components — but NOT hardcoded hex like `#2A241C`, which needs explicit Light tokens at the source

### Voice & tone
- **Reference:** @./docs/voice-and-tone.md — full voice/tone guide for UI copy AND AI-generated text. Covers what BTTS sounds like (knowledgeable friend, not coach; pragmatic; editorial), what it doesn't (no apology, no emoji, no hype-default adjectives, no HTTP codes user-side), per-surface worked examples (errors / empty states / destructive confirms / coach cards / chat replies), and a self-test before shipping copy.
- **When to consult:** any new UI copy, any prompt change for `/api/greeting`, `/api/recommend`, `/api/brew-insight`, `/api/insights`, `/api/explore-agent`, or any rewrite of error/empty-state strings.
- **Per-surface examples carry file paths.** Update the doc examples when the source surface's copy changes.

### iOS PWA / install gotcha
- iOS caches `apple-mobile-web-app-status-bar-style` **at PWA install time** — changing the meta tag does NOT update an already-installed home-screen app. To pick up the change the user must delete the PWA from the home screen AND clear Safari → Advanced → Website Data for the domain, then re-install.

---

## Explicitly NOT Wanted

- **No token usage logging** — `logTokenUsage` / `usageLogs` collection was removed; don't re-add
- **No Zod `.transform()` that produces `undefined`** — breaks Firestore writes (null → strip at source instead)
- **No external component libraries** (shadcn, radix, headless-ui, etc.)
- **No changes to unrelated files** when fixing a bug — surgical edits only
- **No emojis in UI** — design is editorial/premium
- **No separate "total" row** in pour sequence tables — drawdown end = total time = done
- **No temperature-for-timing advice** — grind coarser/finer to fix timing; temp is for extraction chemistry only
- **No Vercel** — deleted. Do not reference Vercel URLs or Vercel deployment in any context.
- **No `npm run dev` assumptions** — app is always tested on the deployed Hetzner PWA

---

## iOS Shell Project (in progress)

Multi-session arc to turn BTTS into a native iOS app via a Capacitor remote-URL shell distributed through TestFlight internal testing — closes the documented "Step alerts during background are missed" gap and unlocks Acaia BT + widget + Live Activity + Apple Watch on later milestones.

**Working doc:** @./docs/ios-shell-roadmap.md — read its "Multi-session execution model" and the latest "Session log" entry before touching anything in `src/lib/native/`, the `native/` directory, or `.github/workflows/ios-*`. (The former `src/hooks/useBrewStepNotifications.ts` no longer exists — lock-screen step notifications were removed by owner request 2026-06-15; see the history note at the top of `src/lib/native/brewNotifications.ts`.) Every session that advances the project updates the doc (session-log entry + any new Stolperstein) in the same commit as the code.

---

## Liquid / motion design (in progress)

The "fluidity pass" — the living Field background (static base gradient + drifting colour blobs + film grain + finger-following bloom) and the liquid welcome-haiku (scattered per-word spring entrance → dissolve → per-word touch lens). All motion runs on the GPU compositor (CSS `@keyframes` + CSS vars written by one rAF loop); React is never in the per-frame loop.

**Working doc:** @./docs/liquid-design.md — read its "Tuning dials" table before any "make the background bigger / the haiku slower / the glow stronger" change, and its top rule before debugging "motion is dead in the PWA." Touch points: `src/components/ui/light/{Field,FieldBlobs,FieldGrain,FieldBloom,HaikuStarter}.tsx`, `src/hooks/{useFieldMotion,usePresence}.ts`, `src/lib/field/composeGradient.ts`, and the motion blocks in `globals.css`. Every advancing session updates the doc (session-log entry + dials kept honest) in the same commit as the code.

**Load-bearing rule:** continuously-iterated `@keyframes` (blob drift, haiku entrance) live **co-located in the component** via `<style jsx global>`, NEVER in `globals.css` — the installed PWA serves a stale cached `globals.css` even after the JS updates, so keyframes there silently don't animate (the "haiku moves, background dead" bug). After any deploy, force-quit + reopen the PWA to drop the cache.

---

## User / Equipment Profile

| Device | Details |
|--------|---------|
| **PRIMARY** | V60 size 2 (Hario Drip Assist **retired from daily use** — emergency-only when no gooseneck kettle is around, e.g. travelling. Selectable in the flow as "V60 + Drip Assist" but never recommended proactively; PR #193) |
| Other | Orea V4 Wide, Origami Air M (resin / AS-resin "Air" line — lighter, lower thermal mass than ceramic; takes both V60 conical and Kalita Wave flat-bottom filters), Clever Dripper, Kalita Wave, AeroPress, Moccamaster, Chemex |
| **Kettle** | Fellow Stagg EKG — gooseneck, precise temp control, 60-min hold |
| **Grinder** | Niche Zero — uses **degree (°) settings**, continuous (no clicks) |
| Travel grinder | Comandante C40 MK2 — uses **clicks**, not degrees |
| **Scale** | Acaia Lunar (2017) + Acaia Pearl (älter, exaktes Jahr nicht bestätigt) — 0.1 g precision. |
| **Water** | BWT Bestmax Premium V (bypass 0): ~370 ppm hard local tap → **~220 ppm** filtered (GH 5–6 / KH 4 °dH), daily driver for naturals/honeys · **clarity blend** 1:2 filtered+distilled = **~73 ppm** (KH ~1.3 °dH) for washed florals & championship methods (Kasuya/Wölfl) |

### Hard rule: single-user PROJECT, not a product — no onboarding

This app has exactly **one user (the owner, roitsch@gmail.com) and always will.** It is a personal project, not a product. Consequences that OVERRIDE any "make it configurable / generic" instinct:

- **There is no onboarding flow to rely on.** The `(light)/onboarding` page is deprecated; do not route new behaviour through it or assume the user will (re-)run it. The user cannot re-select equipment through a wizard — there is no settings screen.
- **The profile is CODE-CANONICAL.** The owner's equipment, grinder, water, and taste are the source of truth in code: `CANONICAL_PROFILE` (`src/lib/claude/userProfile.ts`) for prompt text and `CANONICAL_EQUIPMENT` (`src/lib/knowledge/recipes/helpers.ts`) for recipe-brewer filtering. When the kit changes, edit those constants — never wait on a DB/onboarding round-trip. `/recommend` unions the stored `preferences.equipment` with `CANONICAL_EQUIPMENT` so a stale DB row can never hide an owned brewer (this was the cause of Origami/Chemex being filtered out of recommendations — PR #250).
- **Don't add per-user generality** (multi-tenant isolation, per-user onboarding gates, "first-run" UX). It's wasted complexity for a one-person project.

**Live data:** the production DB (Postgres on the VPS) is NOT reachable from a Claude Code session — only the repo is. Work from the code: read the schema, the constants, and the canonical profile to know how things behave. Do NOT guess at, extrapolate, or fabricate row contents (see the "never infer repo state from partial evidence" Hard Rule). If a specific live value genuinely matters and can't be determined from code, say so plainly and ask the owner — don't invent tooling to paper over it.


**Taste:** silky, balanced, floral/fruity (elegant); light roast SO; avoids anaerobic/infused/dark. **Not at home: pineapple-forward coffees** — a FLAVOUR filter, so a washed or honey bag can be loudly pineapple and sail past the process/roast avoids; soft, not a ban (a cup out at a café is fine); and it governs SELECTION only, never how an owned bag gets brewed. Canonical in `CANONICAL_PROFILE`.
**Grind quick ref:** @./docs/grind-settings.md
**Grind source of truth (code) — TWO layers since 2026-08-23.** The owner's OWN measured grind wins where it exists: `src/lib/claude/measuredGrind.ts` injects a MEASURED GRIND block into `/recommend` from `brew.grindSettingUsed`, per brewer family and batch size (≥3 logged brews required). Everything below is the FALLBACK for a brewer with too little history — and it is where a recipe's own published grind still comes from. `src/lib/constants/grindSettings.ts` is the canonical per-method default table — `docs/grind-settings.md` mirrors it for humans. Each entry in `src/lib/knowledge/recipes/` carries its own `grind.nicheZeroDegrees` translation specific to that recipe (e.g. Wölfl 2024 = 401–411°). Different scopes — `grindSettings.ts` = "default for this method", recipes = "what this specific published routine calls for".

**Wiring (corrected Aug 2026).** The chat (`/explore-agent`) reads the measured table live via `formatGrindSettingsForPrompt()` (`grindSettings.ts`). `/recommend` still carries a HARDCODED `NICHE° GRIND REFERENCE` block in `recommendPrompt.ts` for prompt-cache stability — but it is no longer free to drift: it had silently fallen ~+21° behind the measured anchor (V60 read 396–406° while the anchor is 380°, and the block even contradicted its own "23 clicks"), and is now corrected to the measured baseline AND pinned to `grindSettings.ts` by `tests/dataflow/grind-reference-consistency.test.mjs`, which fails CI on any disagreement. The three Orea bottoms now live in `grindSettings.ts` too (they had lived only in the stale prompt copy). **To re-calibrate degrees end-to-end, update `grindSettings.ts` + `docs/grind-settings.md`; the `/recommend` block must then be edited to match or the consistency test fails** — so the two can no longer silently diverge. `/explore-agent` needs no edit (it reads the constants).
